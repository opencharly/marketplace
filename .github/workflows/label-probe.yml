name: label-probe
on:
  pull_request:
    types: [labeled]
jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - run: echo "PLAIN labeled workflow FIRED (action=${{ github.event.action }} label=${{ github.event.label.name }})"
