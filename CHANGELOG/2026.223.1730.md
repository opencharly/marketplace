# 2026.223.1730 — regenerate skill projections for the k8s→kubernetes substrate rename

Regenerated by `charly marketplace generate` from the superproject candy sources
after the deploy substrate kind `k8s:` became `kubernetes:` (the cross-repo
cutover consuming spec `v0.2026223.1018`): the kubernetes skill (`kind:
kubernetes` cluster template, the `deploy:` inner deploy-knobs block, no
`--target` flag), plus every sibling SKILL.md whose source referenced the old
kind word. The hand-authored `README.md` skill index was swept claim-keyed in
the same cutover. Placeholder CalVer — the pr-validator finalizes it at merge.
