# 2026.229.1246 — the ollama skill corpus, regenerated for the command:ollama plugin

**This is a REGENERATION, not an authored edit.** All files carry
`<!-- GENERATED by `charly marketplace generate` — DO NOT EDIT -->` and their source of truth is
`candy/ollama/charly.yml`, `candy/charly-ollama/charly.yml`, and `candy/plugin-ollama/charly.yml` in the `charly` repo.

## What arrives

- `ollama/skills/ollama-cli/SKILL.md` (NEW) — the `charly ollama` management CLI
  skill, from candy/plugin-ollama's `ollama-cli-skill` entity.
- `ollama/skills/ollama/SKILL.md` — the new "The `charly ollama` management CLI"
  section plus the OLLAMA_VERSION pinning note.
- `ollama/skills/ollama-layer/SKILL.md` — the `OLLAMA_VERSION` var table, the
  runtime-context check list (including the new `ollama list` check), and the
  corrected `supervisord`-only dependency claim (the candy has carried no `cuda`
  dependency since the GPU-agnostic cutover — the skill table was stale).
