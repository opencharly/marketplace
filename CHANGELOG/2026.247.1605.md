# 2026.247.1605 — chore: align wl-pixelflux corpus refs to v2026.247.1546

## Summary

Aligns the two stale layer-wl-* pixelflux refs in this repo's corpus mirror (candy/charly-marketplace/charly.yml) to v2026.247.1546 — the merged newest tags of layer-wl-record-pixelflux / layer-wl-screenshot-pixelflux. The stale refs (.1823/.2104) resolve their content at pod-selkies v2026.243.1737, one of the dual-pin warning sources across the selkies graph (opencharly umbrella pin-hygiene workstream, recorded in plan/crabbox-loop-cutover.md). The docs repo mounts this corpus via its marketplace submodule, so no docs-side content change is needed.

## How tested

```
charly box validate   # repo marketplace, this head
charly box validate: OK — checked 1 candy, 7 distros, 7 builders; 0 warnings, 0 errors
```

The corpus mirrors the checked entity closures; the aligned refs resolve to the merged tags (verified via gh api tags), and the repo-level validate is 0 warnings / 0 errors. The full downstream graph gate (the cachyos import) re-runs at the distro re-vendor PR once all coordinated pin alignments land.

## Rulebook compliance

| Rule | How this PR satisfies it |
|---|---|
| R0 — skills first | git-workflow skill loaded before the pin change. |
| R1 — RCA every anomaly | Warnings traced to these stale corpus refs; fixed at the source. |
| R2 — finish the whole cutover | Both stale refs aligned to the merged newest tags. |
| R3 — no duplication | One canonical newest version per candy. |
| R4 — no workarounds | Pin to the merged tags; no suppressions. |
| R6 — git safety | No force-push; branch-per-change PR. |
| R7 — prove behaviour | box validate 0 warnings / 0 errors pasted (real run). |
| R9 — binary equals source | Fresh binary via the charly repo's bin. |
| R10 — fresh disposable proof | Resolution validated at 0/0 on the committed tree. |

## Change classification

Dependency pin alignment (corpus mirror, 2 lines). **Attribution:** fully tested and validated (repo composition resolved at 0/0 per the pasted output; the downstream graph gate is the coordinated re-vendor).

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*