# 2026.215.0759 — Context-engineering modernization: skill dedupe, progressive-disclosure splits, reminder-layer removal

Documentation-only cutover applying Anthropic's Claude-5 context-engineering
guidance to the core skills — the plugins leg of the two-leg landing (the
superproject leg rewrites CLAUDE.md to rulebook density and deletes the three
reminder hooks).

## Progressive-disclosure splits (new layout convention)

A skill may now be an entry `SKILL.md` (frontmatter + overview + when-to-use +
reference index) plus plain-markdown `references/*.md` files read on demand —
convention documented in `/charly-internals:skills`, Authoritative-copy
registry extended to cover owner text living in a references file. Seven
skills split (entry line counts in parens, frontmatter byte-identical in all):

- `check` 1758 → entry (119) + references/{beds-and-r10 530, verb-catalog 304,
  live-probe-verbs 296, authoring-gotchas 369, cross-deployment-probing 173}.
  The R10 gate-by-change-class matrix, Flag discipline, and the 10 Testing
  Standards stay sole-owned here, content-complete, heading text unchanged.
- `agents` 1499 → entry (53) + references/{agent-roster 205,
  orchestration-model 237, program-discipline 169, parallel-bed-testing 600,
  hooks-and-lifecycle 375}.
- `git-workflow` 901 → entry (66) + references/{branch-and-pr-loop 179,
  multi-repo-coordination 279, validator-and-calver 290}.
- `plugin` 723 → entry (70) + references/{boundary-law 207 (the kernel/plugin
  boundary law, sole-owned and content-complete; the v2 target-architecture
  roadmap compressed to its end-state statement — K-wave classification
  retained, per-row LOC ledger dropped in favor of the tracked cutover plans),
  authoring-recipes 356, cue-schema-contract 79}.
- `go` 680 → entry (169) + references/{source-map 405, schema-change-recipe
  79, implementation-insights 27}.
- `generate-source` 544 → entry (89) + references/{task-emission 116,
  multistage-and-intermediates 193, labels-and-caching 154} (split only — the
  style exemplar, no rewording).
- `install-plan` 526 → entry (85) + references/{step-kinds 224, deploy-targets
  142, emit-opts-and-testing 88}.

## Dedupe vs the rulebook (Authoritative-copy registry enforcement)

- The forbidden-excuse catalog now has ONE owner: `cutover-policy` "No
  exception clause" (previously four copies: the rulebook, twice in
  cutover-policy, once in strict-policy). All other sites point.
- The 10-file copy-pasted `Live-deploy verification is mandatory` trailing
  block (build, image, vm, vms-catalog, charly-update, start, charly-config,
  charly-status, deploy, cutover-policy — plus go via its split) is replaced
  by a one-line pointer to `/charly-check:check` + `/charly-internals:disposable`.
- `disposable` and `go-quality` drop their restated R10 fraud-clause /
  gate-matrix copies, keeping pointers plus each skill's unique content;
  `strict-policy` keeps its operationalization (RDD risk table, the
  ad-hoc-command → `charly`-verb replacement table, forbidden-pattern
  catalogs) and defers mandate wording to the rulebook; `install-plan`'s
  boundary-law paraphrase and `go`'s R9 restatement become pointers.

## Reminder layer removed from harness adapters

- Hooks doctrine everywhere now reads: hooks enforce deterministic command
  mechanics (the two PreToolUse gates, `pre-commit-gate.sh` +
  `pre-push-gate.sh`); agents judge policy and proof; there is no reminder
  layer. Hook inventories updated 5 → 2 (`agents`, `skills`,
  `cutover-policy`).
- `kimi-user-config.toml`: the UserPromptSubmit / Stop reminder wiring
  removed; the two gate delegations kept.

## Other

- Multi-agent model instantiation updated: Fable 5 orchestrator + Sonnet 5
  teammates/validators (previously Opus 4.8 teammates).
- Moderate de-shout across the emphasis-heaviest skills (cutover-policy,
  strict-policy, git-workflow, agents): decorative all-caps and repeated
  threat framing removed; each gate stated once, plainly; normative content
  unchanged.
- Dated incident narratives compressed to present-tense standing rules with
  measured data retained (store-lock maxjobs figures, exit codes, error
  strings); dropped narratives include the migration-ledger variance
  blow-by-blow (now "measured variances of roughly ±80% recur"), the
  bed-ownership empirical-verification dates, the crash-looped-image and
  check-anti-pattern anecdotes, and named cone/PR incident labels.
- Two stale cross-references fixed en passant: a dangling "AI iteration loop
  semantics" pointer in `check` and a dangling "(above)" freshness-guard
  pointer in `go`.
