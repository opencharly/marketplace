# 2026.235.2052 — feat: advance the charly pin to the cutover-pilot main (c35813d); drop the moved ripgrep skill

## Summary

The candy cutover pilot's **ripple** (charly#384 merged): the charly pin advances from `45cdc36` to `c35813d` (the pilot main — `candy/ripgrep` moved to the standalone `opencharly/layer-ripgrep` repo, kind-prefixed naming). The regenerated corpus at the new pin drops the moved candy's skill projection: `tools/skills/ripgrep/SKILL.md` (the only delta — the generator's discovery rework, Phase 3 of the cutover, will walk the standalone repo at the next regen).

## How tested

**The regeneration at the new pin (pasted):**

```
$ git -C charly checkout c35813d90495614536b9bba083fad30acf34e7bf && git -C charly submodule update --init
$ cd charly && go build -buildvcs=false -ldflags "-X main.BuildCalVer=marketplace-ci" -o ../bin/charly ./charly
$ ../bin/charly marketplace generate --root . --out ..
marketplace generate: wrote 406 artifact(s) across 26 family(ies)
$ git status --porcelain
 M charly
 D tools/skills/ripgrep/SKILL.md
```

The regen delta is EXACTLY the moved skill's projection (1 deletion); everything else is a no-op. The repo's own deploy workflow re-runs this drift gate on the PR (the live gate).

## Rulebook compliance

| Rule | How / N/A |
|---|---|
| R0 | N/A — no skill-owned surface edited (the corpus is generated; the source skill lives in the standalone repo) |
| R1 | N/A — no surfaced failure |
| R2 | the moved skill's corpus projection at the NEXT regen (Phase 3's ref-driven discovery) is the named follow-on batch |
| R3 | the generator (charly candy/plugin-marketplace) remains the single corpus owner |
| R4 | N/A — the pin advance is the sanctioned sync |
| R5 | the moved skill's old projection deleted in this PR (the cutover's legacy sweep) |
| R6 | status checked; no force-push; feat-branch + PR landing |
| R7 | the regen output pasted above + this PR's own deploy drift gate |
| R8 | the corpus regenerated live (pasted) |
| R9 | N/A — no deploy target change |
| R10 | the regen ran on the committed tree at the pinned charly |

## Change classification

- **Change class**: charly pin advance + regenerated corpus (1 file)
- **Verification gate**: the regeneration no-op proof (pasted) + this PR's deploy drift gate
- **Attribution tier**: analysed on a live system — the regen executed live with retained output
- **Landing**: fresh `charly/pr-validator`; tag-on-merge

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*