# 2026.254.1332 — refs: bump pod-charly-hooks v2026.254.1321; regen — wonky manifests by generation

## Summary

Second half of the wonky-source rebrand cutover: the plugin-manifest descriptions are GENERATED from the owning candy's claim, and pod-charly-hooks' source rebrand landed (tag v2026.254.1321). This PR bumps the refs pin and regenerates, so the manifests carry the wonky brand **by generation** — permanently (survives the daily refresh and any future regen; no hand-edited generated content anywhere).

- refs list: `@github.com/opencharly/pod-charly-hooks:v2026.247.1133` → `v2026.254.1321` (the only refs change, +1/−1).
- Regenerated corpus (4 files, +5/−5): `.claude-plugin/marketplace.json`, `kimi.plugin.json`, `package.json` — descriptions now `…for OpenCharly — The wonky DevOps tool for you and your agents: runtime commands, …` at every former old-brand occurrence (incl. kimi's shortDescription).

## How tested

From the pushed tree (branch feat/corpus-wonky @ 3946edd, charly stamped 2026.254.0903 from d507ca5a):

```
$ charly -C /tmp/market10 marketplace generate --root /tmp/market10 --out /tmp/market10
marketplace generate: wrote 417 artifact(s) across 28 family(ies)   (rc 0; diffstat 4 files +5/-5)
$ grep -c wonky: marketplace.json=1 kimi.plugin.json=2 package.json=1   # old brand: 0 in all three
$ (determinism) second run → all 4 changed-file md5s + porcelain md5 byte-identical; 0 untracked
```

Scope audit: refs list + the three manifests only; nothing unexpected; working tree clean after push; branch verified upstream.

## Rulebook compliance

- R1 — the #295 override (hand-edited generated manifests) is permanently resolved: the wonky description now flows from the pinned source claim by generation.
- R3 — the refs list is the ONE input; the corpus mirrors it byte-for-byte (determinism).
- R6 — no force-push/amend; single clean commit.

## Change classification

- **Change class:** corpus regeneration + refs pin bump (generated content).
- **Verification gate:** generation determinism + per-manifest wonky grep + zero-old-brand audit.
- **Attribution tier:** analysed on a live system — the verb ran live with retained output; the docs corpus sync (next landing in the batch) carries the manifests into the site pipeline.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*