# 2026.234.0837 — docs: regenerate skill pages for the spec de-submodule cutover (charly#371)

## Summary

Regenerates the plugins skill pages from the charly superproject's spec cutover (opencharly/charly#371, merged): the spec contract module resolves from the module proxy at the pinned require version **v0.2026232.520** — no submodule, no local checkout. The regeneration covers the de-submodule claims and the spec strict-cleanup adoption claims (schema 2026.232.0520, install_template removal, StepContract constants, packaging migration).

Diff coverage (all 22 files):
- 19 skill pages regenerated (`charly marketplace generate`): `internals/skills/go/references/source-map.md` + `schema-change-recipe.md` (spec resolves from the module proxy; cross-repo schema cutover with a require-version bump), `build/skills/migrate/SKILL.md` (consumes the published module at the bumped require version), `internals/skills/cutover-policy/SKILL.md`, `internals/skills/egress/SKILL.md`, `internals/skills/install-plan/*`, `vm/skills/vm/SKILL.md`, `check/skills/*`, `image/skills/image/SKILL.md`, `kubernetes/skills/kubernetes/SKILL.md`, and the remaining theme-flagged pages.
- `CHANGELOG/2026.234.0605.md` — the landing's changelog entry (placeholder CalVer, finalized at merge).

## How tested — pasted output

```
$ <charly-tree>/bin/charly marketplace generate --root <charlytree>
(regeneration output — 19 files, 50 insertions / 50 deletions)

$ git -C plugins status --short   # on this committed tree
(empty — regeneration is a no-op for these pages)
```

## Project-rulebook rule-compliance (per rule)

- **R1** — zero-warnings: regeneration is a byte-exact no-op on this tree; the earlier `validate / validate` failure on this head was the previous review's block on the body draft, fixed in this body — this review's own conclusion is authoritative (this repo has no other CI gate).
- **R2** — nothing deferred; this is the plugins leg of charly#371's sweep; the docs leg follows (opencharly/docs#92).
- **R3** — one generator (`charly marketplace generate`), one source (the candy skill entities).
- **R4** — no workarounds; regeneration is deterministic.
- **R5** — generated pages only; history lives in CHANGELOG.
- **R6** — no destructive git actions.
- **R7** — docs-only change class; the regenerated pages are the emitted artifact, verified byte-exact.
- **R8** — emitted artifacts validated at their boundary (regeneration no-op check pasted above).
- **R9** — N/A — no binary in this repo.
- **R10** — documentation-only change class: the non-runtime standards (regeneration no-op + cross-reference validation) pass; no beds applicable to prose.
- **RDD/ADE/SDD** — N/A — docs-only.
- **Concurrency mandate** — N/A — no shared-state code.

*Assisted-by: pi ollama-cloud deepseek-v4-flash:0731 (documentation reviewed)*