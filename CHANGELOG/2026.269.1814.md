# 2026.269.1814 — docs(marketplace): regenerate for layer-charly-kubernetes v2026.269.1757 (kindcluster sibling)

## Summary

Regenerate the marketplace corpus for the `layer-charly-kubernetes` refs-list bump.

`layer-charly-kubernetes` v2026.269.1757 (PR #3) now names the **`kindcluster`
local-cluster sibling** in its `kubernetes` + `check-k8s` skills. Bump the refs-list
pin in `candy/charly-marketplace/charly.yml` and carry the regenerated corpus in the
same commit (the drift gate requires it).

The regenerated diff is **exactly** the pin plus the two affected `SKILL.md` files:

```
 candy/charly-marketplace/charly.yml   | 2 +-
 kubernetes/skills/check-k8s/SKILL.md  | 7 +++++++
 kubernetes/skills/kubernetes/SKILL.md | 2 ++
 3 files changed, 10 insertions(+), 1 deletion(-)
```

## How tested

Regenerated with the real pinned refs (the generator resolves every `@github` ref
from the refs list — no mocks):

```
$ charly -C . marketplace generate --root . --out .
… vm/skills/vm/SKILL.md
… vm/skills/vms-catalog/SKILL.md
regen-rc=0

$ git diff --stat
 candy/charly-marketplace/charly.yml   | 2 +-
 kubernetes/skills/check-k8s/SKILL.md  | 7 +++++++
 kubernetes/skills/kubernetes/SKILL.md | 2 ++

$ grep -n 'local-cluster sibling' kubernetes/skills/check-k8s/SKILL.md kubernetes/skills/kubernetes/SKILL.md
kubernetes/skills/check-k8s/SKILL.md:27:The **local-cluster sibling** is `kindcluster` (`kind: kindcluster`, a
kubernetes/skills/kubernetes/SKILL.md:17:A **local-cluster sibling** reuses this same Kustomize + `kubectl` machinery: …
```

**Generator stability** (why the CI drift gate — pinned charly `v2026.266.1654` —
will agree with a corpus regenerated by a newer binary): the regeneration was a
**no-op except** for the pin-driven skill changes — zero spurious diffs anywhere in
the other ~397 artifacts across 26 families. Identical generator output on both
versions is what the gate checks.

The repo's CI (`deploy.yml`) regenerates the corpus with the pinned charly and fails
on any drift (including untracked artifacts), then runs the per-harness catalog
sanity check.

## Rulebook compliance

- **R0 (skills first):** loaded `/charly-internals:marketplace` and `/charly-image:layer`.
- **R1:** the minimal diff was verified to be the pin + exactly the two regenerated
  skills — no unrelated corpus drift was accepted.
- **R3 (no duplication):** the generated corpus is produced by the ONE generator; no
  hand-edit of a `SKILL.md`.
- **R6 (git safety):** session worktree; no direct push to `main`.
- **R7 (prove the gate):** the regeneration is deterministic and its output is the
  committed tree (CI's drift gate re-proves on the pinned charly).

## Change classification

- **Change class:** generated corpus + refs-list pin (`candy/charly-marketplace/charly.yml`).
- **Verification gate:** `charly marketplace generate` (clean, minimal diff, pasted
  above) + the repo's CI drift gate.
- **Attribution tier:** `fully tested and validated` — the real generation ran on the
  pinned refs and the resulting tree is the commit.

*Assisted-by: OpenCode DeepSeek V4.1 Flash (deepseek-v4.1-flash) (fully tested and validated)*