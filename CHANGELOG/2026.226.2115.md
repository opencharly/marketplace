# 2026.226.2115 — add the dsh + dsh-cli skills; update the nodejs skill for the Node 22 floor

The dsh cutover (opencharly/charly#271) added two new marketplace skills
and updated one:

- `tools/skills/dsh/SKILL.md` — the dsh candy's skill: the npm-installed
  deepseek-harness CLI + the socat-exposed web service (loopback-bound
  `dsh web` on 127.0.0.1:3080, socat forwarder on 0.0.0.0:3081).
- `tools/skills/dsh-cli/SKILL.md` — the plugin-dsh management plugin's
  skill: the compiled-in `charly dsh` command tree + the `dsh:` check verb
  methods.
- `coder/skills/nodejs/SKILL.md` — the DEB row now names the NodeSource
  node 22.x install (npm bundled by NodeSource, the separate apt `npm`
  dropped) — the deepseek-harness dsh floor.

All regenerated via `charly marketplace generate` from the candies'
`skill:` entities. Regeneration is a no-op on a clean tree.

Placeholder CalVer — the pr-validator finalizes it at merge.
