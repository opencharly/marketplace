# 2026.219.2151 — drop the hardcoded catalog counts from the marketplace prose

Drop the hardcoded skill/plugin counts from the marketplace prose: the plugins README's per-plugin Skill count column and "all 25 plugins", the versa README's tool/skill counts, and the jupyter README's "11 MCP tools". The skill bodies are regenerated from the cleaned candy configs, so the per-image counts are gone from the generated corpus too. The live census now lives in the generated provider index.
