# 2026.254.1306 — refs: bump layer-charly-build v2026.254.1251; regenerate the corpus (docs-skill sweep + drift-gate manifest truth)

## Summary

Regenerates the corpus after the layer-charly-build docs-skill sweep (#11, merged → tag v2026.254.1251): refs pin layer-charly-build v2026.250.1404 → v2026.254.1251; `charly marketplace generate` (binary 2026.254.0903) on the updated refs list. Diff: 5 files, +150/−15 — `build/skills/docs/SKILL.md` (+155/−15: the swept `docs:` node section, `assembleCatalog` one-assembly discovery, the sanitizeSegment 'by construction' path-scheme paragraph, hermetic-test section), the refs list (+2/−2), and the three generated manifest files (+/− lines each).

## Documented drift-gate truth (R1) — manifest descriptions

PR #295 (the wonky manifest rebrand) hand-edited the three generated manifest files WITHOUT regeneration; the generator source (the pinned corpus claim at pod-charly-hooks:v2026.247.1133 — verified verbatim in the refs cache) still carries the old brand, so this regeneration flips the manifest descriptions back to the generator truth. That is the drift gate's compliance requirement (regen == committed; generated files are never hand-edited). The SITE brand is unaffected — opencharly.ai's wonky tagline projects from the charly README/config, not these manifests. The permanent fix is the **wonky-source rebrand cutover** (this exact batch): opencharly/pod-charly-hooks changes the owning candy description to the wonky tagline (its own PR, in flight now), then THIS repo bumps the refs pin to that merge tag and regenerates -- the manifests then carry the wonky brand by generation, permanently. Landed in this same project; not a hand-revert. -charly-hooks) and bump the refs pin — a named follow-up landing, not a hand-revert here.

## How tested

From the pushed tree (branch feat/corpus-docs-skill @ fe8760d6):

- `charly -C /tmp/market9 marketplace generate --root /tmp/market9 --out /tmp/market9` → clean exit twice; second run byte-identical (porcelain md5 + diff md5 unchanged; 0 untracked) — determinism no-op honored.
- Sweep markers present in the regenerated SKILL.md (grep: `assembleCatalog`/`sanitizeSegment`/`docs: node` lines added), diff audit shows only the 5 expected files.
- Working tree clean after push; branch verified upstream.

## Rulebook compliance

- R1 — the #295 override surfaced by the regen is documented here (not hidden); no hand-revert of generated files (never hand-edit generated content).
- R3 — the regenerated corpus mirrors the single source set (refs list + owning repos).
- R6 — no force-push/amend; single-purpose commit.

## Change classification

- **Change class:** corpus regeneration (generated content) + refs pin bump.
- **Verification gate:** generation determinism (two runs byte-identical) + scoped diff audit.
- **Attribution tier:** analysed on a live system — the verb ran live with retained output; the named follow-up (source-side description rebrand + refs bump) carries the permanent wonky manifests.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*