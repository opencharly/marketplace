# 2026.234.1709 — docs: regenerate — sweep remaining evaluator-merge references

## Summary

Follow-up regeneration from `charly/candy/charly-internals/charly.yml` after the stale-actor sweep (charly PR #375, merged).

### What changed

Every remaining "the evaluator merges / tags / rewrites" reference in the skills corpus now names the actual actors of the redesigned flow:

- **git-workflow SKILL.md** — "Every repo is tagged at merge" bullet: `if the evaluator skipped it` → `if tag-on-merge skipped it`; trigger description updated.
- **git-workflow references/validator-and-calver.md** — CalVer section heading and merge-time wording: generated and applied by tag-on-merge, not the evaluator.
- **git-workflow references/multi-repo-coordination.md** — B5/B6 landing wording: native auto-merge + tag-on-merge.
- **cutover-policy SKILL.md** — fresh-release-tag bullet: applied by tag-on-merge at merge.
- **internals/agents/pr-validator.md** — description + Phase 3 + output footer: native auto-merge + tag-on-merge.

(`branch-and-pr-loop.md` and `agent-roster.md` were already reconciled on `main` via plugins #222 — they carry no net change in this PR.)

## How tested

### 1. Regeneration — raw output (executed)

```
$ bin/charly marketplace generate 2>&1 | tail -3
  plugins/vm/skills/ubuntu-debootstrap-vm/SKILL.md
  plugins/vm/skills/vm/SKILL.md
  plugins/vm/skills/vms-catalog/SKILL.md
$ echo $?
0
```

### 2. Sweep — raw output (executed)

```
$ grep -rnE "the evaluator (merges|tags|rewrites|stamps|renames)|evaluator merges|evaluator tags|by the evaluator" internals/
[no output]
$ echo $?
1        # zero hits
```

Disposition of the single surviving line that matches an overbroad `evaluator.*merge` pattern (`internals/agents/pr-validator.md:4` description): it reads "...On PASS the org-wide workflow enables native auto-merge (squash), and tag-on-merge writes the CHANGELOG and CalVer-tags — NOT this agent" — the evaluator is explicitly NOT the merge/tag actor; the sentence names the workflow and tag-on-merge. Not a stale reference; read and retained.

### 3. Live mechanism evidence

Run 32581903742 (umbrella tag-on-merge sweep, 2026-08-22 15:30):

```
tag / tag-and-changelog   Created CHANGELOG/2026.234.1251.md
tag / tag-and-changelog   Created CHANGELOG/2026.234.1110.md
tag / tag-and-changelog   Created CHANGELOG/2026.234.1005.md
```

CHANGELOG entries written from merged PR bodies; tags `v2026.234.1251` etc. present on the umbrella remote.

## Rulebook compliance

| Rule | Status | Evidence |
|---|---|---|
| R0 (skills) | ✅ | Source updated in charly; regeneration here; owning-skill git-workflow honored |
| R1 (RCA) | ✅ | Stale evaluator references caught by validator, swept in this regeneration |
| R2 (no split) | ✅ | Fixed in-tree, same cutover |
| R3 (no duplication) | ✅ | One canonical landing flow wording |
| R4 (no workarounds) | ✅ | N/A — no workaround introduced |
| R5 (delete legacy) | ✅ | Stale references swept; executed raw grep output pasted above with disposition |
| R6 (git safety) | ✅ | feat/ branch, no force-push |
| R7 (prove the gate) | N/A — docs-only change; the gate for this class is generation + the org-wide pr-validator (this head's `validate / validate` run is the gate's verdict on this PR itself) |
| R8 (generation) | ✅ | Regenerated from source via `charly marketplace generate` (output pasted); no hand-edits |
| R9 (binary) | N/A — no Go/runtime code changed; docs-only regeneration |
| R10 (change-class gate) | ✅ | Docs-only; no bed applies (N/A); live mechanism evidence pasted |

## Change Classification

- **Change class**: docs (regenerated corpus)
- **Verification gate**: `charly marketplace generate` + org-wide pr-validator
- **Attribution tier**: `analysed on a live system`

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*