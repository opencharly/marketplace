# 2026.226.0813 — the git-workflow skill: drop the removed makepkg/localpkg gotchas + name the `TZ=UTC0` calver invocation

Two fixes to `internals/skills/git-workflow/references/multi-repo-coordination.md`, landed via two validator review rounds:

- **The `pkg/arch` makepkg/localpkg build gotchas are removed** (R5 — the nFPM packaging cutover removed the source-build machinery: `sdk`'s `BuildLocalPkgOnHost` is gone, the main repo's `local_pkg` blocks use the download leg, and `charly box pkg` / `task pkg:*` are replaced by the `charly generate-packages` plugin). The still-valid submodule-refresh gotcha (a clean gitlink auto-merge does not check out the new submodule content — the bed silently builds stale source) is preserved, generalized to any disposable bed that builds from a submodule.
- **The `scripts/calver.sh` claim now names the `TZ=UTC0` invocation** (internal-consistency fix): the page claimed the CalVer derives from the HEAD commit's UTC time via bare `git log -1 --format=%cd`, while its B2 section claims a bare `%cd` uses the commit's own TZ offset — both true only because `scripts/calver.sh` sets `TZ=UTC0` (verified against the superproject's `scripts/calver.sh` line 29: `TZ=UTC0 git log -1 --format=%cd --date='format-local:%Y %j %H %M'`). The claim now states the invocation and that without `TZ=UTC0` the bare `%cd` uses the commit's own TZ offset.

**Scope note:** this PR originally carried a 14-skill regeneration for the nFPM packaging cutover; those generated files landed on `main` first via the rebrand cutover (`c251846`, `v2026.227.0745`), which regenerated the whole skill projection — 13 of the 14 are byte-identical to `main`. The only genuinely-absent delta is this hand-authored `references/*.md` file, which regeneration does not overwrite. The PR is re-scoped to that delta + this CHANGELOG.

Placeholder CalVer — the pr-validator finalizes it at merge.
