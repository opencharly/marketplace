# 2026.226.0813 — regenerate skills for the nFPM packaging cutover + the qemu `vm start` idempotency doc

Regenerated from the superproject's `candy/*/charly.yml` manifest edits via `charly marketplace generate` (the sdk-adopt cutover's candy changes) plus the vm skill's new qemu idempotency documentation. Fourteen skill files changed:

- `vm/skills/vm/SKILL.md` — new `vm start` idempotency documentation: the qemu backend's already-running guard is `qemuAlive` (read `<stateDir>/qemu.pid` → `Signal(0)` → `/proc/<pid>/cmdline` contains `qemu-system`), making `vm start` a clean no-op for a running VM — load-bearing for the rebuild path (`vmRebuild`). The bug this fixed: the qemu backend used to blindly re-execute the stored command, so after `vm create` the guard started a SECOND QEMU, which failed to lock the pidfile (`Resource temporarily unavailable`) and corrupted the VM's state — every `charly check run <vm-bed>`'s `update` step failed with this error, even running sequentially.
- `build/skills/build/SKILL.md`, `tools/skills/charly/SKILL.md`, `check/skills/check/references/beds-and-r10.md`, `internals/skills/agents/references/parallel-bed-testing.md`, `internals/skills/install-plan/SKILL.md`, `internals/skills/install-plan/references/step-kinds.md`, `image/skills/layer/SKILL.md`, `core/skills/clean/SKILL.md`, `distros/skills/github-runner/SKILL.md`, `distros/skills/githubrunner/SKILL.md`, `internals/agents/pr-validator.md`, `internals/skills/git-workflow/references/multi-repo-coordination.md`, `internals/skills/go/SKILL.md` — the nFPM packaging cutover's skill updates: the `localpkg:` candy map and the `charly box pkg` / `task pkg:*` native-package build are replaced by the `charly generate-packages` plugin (nFPM, `sdk/packagekit`) + the per-distro package repos; the `pkg/arch/calver.sh` path becomes `scripts/calver.sh`; the `release-packages` workflow becomes the `release-binary` workflow; the `--dev-local-pkg` dev-bed path builds via the plugin from the in-development binary instead of from `pkg/<fmt>` source.

This is the `plugins` leg of the sdk-adopt cutover (superproject → plugins). Regeneration is a no-op on a clean tree. Placeholder CalVer — the pr-validator finalizes it at merge.
