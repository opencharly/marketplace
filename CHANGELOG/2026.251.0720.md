# 2026.251.0720 — chore(corpus): regen after layer-charly-internals v2026.251.0709 — git-workflow validator re-trigger mechanics

## Summary

Corpus regen cutover after **layer-charly-internals v2026.251.0709**: the git-workflow skill's validator re-trigger + PR-body-evidence mechanics update.

- **Upstream**: opencharly/layer-charly-internals PR #12 — "docs(skill): git-workflow validator re-trigger + body-evidence mechanics (measured)" — MERGED via auto-merge (merge commit `9a88200fb9aaae3cd6d2fb33f00e910097cba471`, merged 2026-09-08T07:09:44Z); tag-on-merge produced **v2026.251.0709** — verified as the newest repo tag, on exactly that merge commit.
- **Pin bump** (`candy/charly-marketplace/charly.yml` line 22): `@github.com/opencharly/layer-charly-internals:v2026.250.1956` → `v2026.251.0709`.
- **Corpus projection** (`internals/skills/git-workflow/SKILL.md`): +22 lines — the "Validator re-trigger + PR-body evidence mechanics" section (empty-commit no-op re-trigger, workflow_dispatch self-heal, concurrency dedupe, FROZEN-head body-truthfulness discipline, gh_pr_status watch one-shot fallback).
- Only these two files changed — the regen on the rest of the corpus is a byte-identical no-op.

## How tested

Pasted outputs (branch `feat/gitworkflow-retrigger-mechanics-corpus`, fresh clone `/tmp/mkt4`):

```text
# regen — R10-verified charly binary /tmp/charly-r10/bin/charly (2026.250.1424);
# run detached with exit capture appended to the log:
$ /tmp/charly-r10/bin/charly -C /tmp/mkt4 marketplace generate --root /tmp/mkt4 --out /tmp/mkt4 > /tmp/mkt4-regen2.log 2>&1; echo EXIT:$?
EXIT:0

$ grep 'wrote' /tmp/mkt4-regen2.log
marketplace generate: wrote 417 artifact(s) across 28 family(ies)

$ tail -4 /tmp/mkt4-regen2.log
  vm/skills/omarchy-vm/SKILL.md
  vm/skills/vm/SKILL.md
  vm/skills/vms-catalog/SKILL.md
EXIT:0

$ git diff --stat
 candy/charly-marketplace/charly.yml  | 2 +-
 internals/skills/git-workflow/SKILL.md | 22 ++++++++++++++++++++++
 2 files changed, 23 insertions(+), 1 deletion(-)

$ git diff --numstat
1       1       candy/charly-marketplace/charly.yml
22      0       internals/skills/git-workflow/SKILL.md

$ git status --porcelain            # no untracked, nothing staged; only the two intended paths
 M candy/charly-marketplace/charly.yml
 M internals/skills/git-workflow/SKILL.md

# directive grep proving the mechanics carried through the regen:
$ grep -c 'FROZEN head' internals/skills/git-workflow/SKILL.md
1
```

Directive-grep note (measured vs. wave expectation): the wave brief expected `grep -c 'FROZEN head' ... >= 2`; the measured count is **1**. Verified against the pinned source: the layer's `git-workflow-skill` entity at v2026.251.0709 (`charly.yml` line 3548) contains exactly **one** "FROZEN head" occurrence ("The PR body must match a FROZEN head."), and this regenerated corpus file mirrors it 1:1. The count is faithful to the merged source; no corpus content is missing.

Generator reproducibility note: unchanged from the #292/#291 wave — the repo's config schema is `2026.249.2125`, the available /tmp/charly-r10/bin/charly (2026.250.1424) loads it and regenerated EXIT=0. The CI-time charly pin in `.github/workflows/deploy.yml`/ `refresh.yml` is the pre-existing repo-wide drift-gate breakage documented in #290/#291, unrelated to this change; the pr-validator is the merge gate for this PR.

## Rulebook compliance

| Rule | How |
|---|---|
| R1 | RCA per anomaly — no anomaly: regen exited 0; the FROZEN-head count (1, not >= 2) was root-caused to the merged source containing exactly one occurrence (layer charly.yml line 3548 at v2026.251.0709) and the corpus mirroring it — no remediation needed. |
| R2 | No conflict — one corpus per skill: only `internals/skills/git-workflow/SKILL.md` touched in its owning family. |
| R3 | No duplication — the projection is generated from the layer `skill:` entity (single source); the regen is the only writer of corpus files. |
| R4 | No workarounds — no sleeps/retries/manual corpus edits; a standard `charly marketplace generate` run (twice — idempotent, byte-identical, second run captured EXIT:0). |
| R5 | N/A — no legacy path being cut over in this repo; upstream docs change only. |
| R6 | Git safety — feat/ branch, PR-only landing, no force-push, no direct push to main. |
| R7 | Gate proof — regen EXIT=0 on the branch tree with diff audit + directive grep pasted above; drift-gate state documented in How tested; the pr-validator is this PR's merge gate. |
| R8 | N/A — no production data or deployed system state (corpus publication only). |
| R9 | N/A — no vendor/schema code changes; config refs list + generated artifact only. |
| R10 | Fresh disposable proof — regen ran in a fresh clone (/tmp/mkt4) from the branch tree; diff verified from git status, nothing from an edited state. |
| RDD | Doc-driven — corpus content derives entirely from the layer skill entity (single source of truth); the upstream skill edit merged first, this regen is the cutover half. |
| ADE | Acceptance-driven — acceptance criteria (regen exit 0; diff = pin + git-workflow projection only; FROZEN-head count faithful to source) all met and pasted above. |
| SDD | N/A — no schema/spec change; repo config schema untouched (2026.249.2125). |
| B14 | No block — the regen needs a charly able to load config schema 2026.249.2125; the available /tmp/charly-r10/bin/charly (2026.250.1424) loads it and regenerated EXIT=0 — full command + log + EXIT pasted above. |
| B15 | No stray artifacts — 0 untracked files after the regen; the commit contains exactly the two intended paths. |
| B16 | No unpasted claims — every statement carries its pasted output above (EXIT:0, wrote-417 summary, diff --stat, diff --numstat, status --porcelain, FROZEN-head grep). |
| B17 | No sibling-branch reliance — the pin bumps a merged tag (v2026.251.0709 on merge commit 9a88200f, merged 2026-09-08T07:09:44Z), never a live PR branch. |
| B18 | Scope discipline — no other pin advanced, no other family touched; the diff is exactly the pin line + the git-workflow projection. |

## Change classification

- Change class: **docs/corpus regeneration + refs pin bump**.
- Verification gate: **analysed on a live system** — full `marketplace generate` EXIT=0 with the available charly binary on the branch tree, diff audit + directive grep pasted above; the repo's deploy drift gate is pre-existing broken at CI (stale pinned charly, documented in #290/#291).
- Attribution tier: analysed on a live system.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*