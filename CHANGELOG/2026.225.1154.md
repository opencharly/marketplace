# 2026.225.1154 — codify the docs CHANGELOG convention + resolve the git-workflow doc tensions

Regenerated by `charly marketplace generate` from the superproject's `candy/charly-internals`
manifest. The git-workflow and cutover-policy skills now name **`docs`** alongside the
superproject, `box/<distro>`, and `plugins` as no-CHANGELOG-exception repos: every `docs` landing
carries a `CHANGELOG/<YYYY.DDD.HHMM>.md` entry exactly like every other repo — proven by the 24/25
docs first-parent commits that carry one (the single exception `a252df9` is exactly the commit
that forced follow-up docs PR #46 to add its missing entry). The "Land docs-only" cutover-policy
step and its skills-skill mirror now land the `docs` commit (with its OWN CHANGELOG entry) as a
named leg, and the submodule-pointer-bump safety rule is re-anchored to the per-session worktree
flow (`git worktree add .claude/worktrees/<slug> -b feat/<slug> origin/main`, B1 step 0) instead
of the retired shared-tree `git switch -c feat/<slug>` authoring example. The concurrent-landings
note now states the org-wide branch-protection reality (`branch-protection.sh` applies
`strict: true` + the single `charly/pr-validator` context to EVERY active `main`-default repo — 14
today) instead of the stale "all three repos set `strict: true`" count, and B3's title is scoped to
the orchestrator's shared tree so it no longer reads as contradicting one-worktree-per-session.
Placeholder CalVer — the pr-validator finalizes it at merge.
