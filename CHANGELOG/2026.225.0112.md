# 2026.225.0112 — R3b fix: define Human + correct the worker-image narrative

Follow-on to the R3 batch (opencharly/charly PR #255 Phase 1b): the docs #44
cold-reader surfaced two further findings, fixed at the source `skill:`
entities and regenerated here.

- **Human defined on first use** — the resource model lists four kinds
  (Manager, Worker, Team, Human) but Human was never defined. Both skill pages
  now state it: a Human is a real person — a human-in-the-loop participant
  with an identity and permission tier — who can observe and intervene in the
  Rooms their permissions allow.
- **Worker-image narrative corrected** — "How it works" claimed a Worker
  container runs "the openclaw worker runtime plus the shared AgentTeams
  protocol libs"; the actual worker image runs the openclaw **gateway** plus
  the `agt` and `mc` tools and the shared protocol libs. The narrative now
  matches the Composition table and the worker image definition.

Regenerated by `charly marketplace generate` from the superproject's `skill:`
entities; regeneration is a no-op on a clean tree. Placeholder CalVer — the
pr-validator finalizes it at merge.
