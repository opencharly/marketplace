# 2026.229.1819 — the index row and the tag boundary say what the sections they point at say

**This is a REGENERATION, not an authored edit.** Both files carry
`GENERATED by `charly marketplace generate` — DO NOT EDIT`; their source is the `skill:` and agent
entities in `candy/charly-internals/charly.yml`, landing as `opencharly/charly#289`.

## What arrives

| Artifact | Correction |
|---|---|
| `internals/skills/git-workflow/SKILL.md` | the reference index called status-absence on a known head **"a false negative"** while the section it indexes says it **"proves nothing"** |
| `internals/agents/pr-validator.md` | the tag invariant placed `2026.215.1207` in **both** the untagged set and the tagged run |

**The two claims are not the same strength.** *A false negative* asserts the thing is real but
unseen; *proves nothing* asserts the observation is uninformative. The index was making the stronger
claim its own section had retracted — an index row is a summary, and a summary of a corrected claim
goes stale exactly when the claim is corrected.

**The tag boundary could not have been both.** `2026.215.1207` is the LATEST untagged entry, so the
tagged run opens above it. The sentence now names that entry rather than referring to it by pronoun:
the first fix said *"that entry is itself the latest untagged one"*, whose referent could bind to
either side of the boundary the sentence exists to place.

The corpus total and the run length are dropped rather than corrected. Both grow with every merge —
they were already wrong at this leg's own base, having gone stale when the previous leg merged and
was tagged — so re-deriving them would buy figures with the same expiry. The untagged count survives
because it is the figure that does not move: the corpus grew across this review while 66 held. No
reason is given for that beyond the measurement, deliberately — three successive rationales for it
were each refuted (an untagged entry "never becomes tagged", contradicted by a backfill this same
spec records; then "a regime that minted no tags", contradicted by the 70 of 136 entries at or
below the boundary that do carry tags). The measured half was right every time. Tagging before the
boundary was sporadic, not absent, which is what "interleave" means in that paragraph.

## Why this leg exists at all

Its two corrections were review findings against `charly#289`, raised **after** that cutover's
earlier projections had merged. The skill being landed states the rule that predicts this: the last
prose edit before the first projection merge is a one-way door, and afterwards a review-found defect
buys a full chain — source, projection, docs, gitlink. This is that chain, paid as stated.

Placeholder CalVer — the `pr-validator` finalizes it at merge and rewrites this H1 to match.
**Verify that rewrite by reading the committed object** (`git show <ref>:<path>`), never the working
tree.
