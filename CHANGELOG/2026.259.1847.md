# 2026.259.1847 — chore: bump the eval-omarchy ref to v2026.259.1834 (corpus byte-no-op)

chore: bump the eval-omarchy ref to v2026.259.1834 (corpus byte-no-op)

Advances the refs list's eval-omarchy pin v2026.259.1014 -> v2026.259.1834,
the merge of opencharly/eval-omarchy#79 (which advanced its plugin-pipeline pin
to the merged v2026.259.1825).

The regenerated corpus is a BYTE-NO-OP: #79 changed only the pipeline pin in
candy/eval-pr/charly.yml, not any skill:/hook: entity, so no generated skill
changes. Proven by the drift gate itself — after
`charly marketplace generate --root . --out .`, `git status --porcelain`
reports exactly one changed file (this refs line) and no untracked artifacts.

Verification:

  $ REFRESH_DRY_RUN=1 bash scripts/refresh-refs.sh
    eval-omarchy   v2026.259.1014 -> v2026.259.1834

  $ CHARLY_PLUGIN_ONLY=1 charly marketplace generate --root . --out .
  GEN_EXIT=0
  $ git status --porcelain
   M candy/charly-marketplace/charly.yml      # only the refs line — no corpus drift

  $ ls ~/.cache/charly/repos/github.com/opencharly/eval-omarchy@v2026.259.1834
  (the new tag was fetched and walked; the corpus it produced is identical)

Rulebook compliance: R1 (the ref advance is proven by resolving the new tag and
regenerating, not asserted); R3 (the refs list is the one generation input);
R5 (no stale artifact left alongside); the drift gate is the authority here and
is pasted above.

Change classification: pin/refs + regenerated corpus (byte-no-op). Verification
gate: the marketplace deploy workflow's drift gate, reproduced locally.
Attribution: fully tested and validated.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (fully tested and validated)*