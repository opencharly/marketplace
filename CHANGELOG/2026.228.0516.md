# PLACEHOLDER — regenerate the build and clean skills for the bounded tag cap

**This is a REGENERATION, not an authored edit.** Both files carry
`<!-- GENERATED by `charly marketplace generate` — DO NOT EDIT -->` and their source of truth is
`candy/charly-build/charly.yml` and `candy/charly-core/charly.yml` in the `charly` repo.

## Two claims were wider than the code

**The per-image tag cap was stated unconditionally.** *"at most that same number of tags of each"*
— while the undatable-row exemption runs unconditionally in the other direction: a row with
neither a datable label nor a datable tag *"can never be elected for removal however many tags its
image wears"*. The cap binds **datable rows only**, and the corrected sentence carries that
exception through the clause instead of leaving a later paragraph to qualify it. Same class as the
`EITHER`/`BOTH` comment the producing cutover already fixed — reappearing in the paragraph written
to explain that very guard.

**The post-build prune was described as enforcing `keep_images`, and it cannot.** `charly box
build` holds the build-activity lock across its own prune, so that prune ALWAYS observes a live
build, and the live-build path never removes an image's LAST tag. A build therefore trims surplus
tag rows from images wearing several and leaves every single-tagged image in place: the
**distinct-image** half of `keep_images` is enforced by `charly clean` alone. Nothing reports the
gap, because the summary counts what was removed, not what was selected and could not be.

This one was discovered rather than inherited — measured on a four-generation fixture whose rank-3
single-tagged image survived at `keep_images: 3`, and consistent with hosts carrying 4–5 distinct
images per group under that setting.

## Scope: exactly the two pages whose sources changed

`marketplace generate` also rewrites twelve other files whose projections are stale against
candies nobody regenerated. That drift is real, is not this change's, and is already filed as its
own cutover. Sweeping it in would carry twelve files of other people's changes past a gate that
cannot fail on them.

Both kept files are **additions-dominant** (14/4 and 11/1). A regeneration that is overwhelmingly
DELETIONS of coherent prose is the signature of a stale-source revert; that test is what separated
these two from the rest, and it is the same test that excluded three `vm` pages from the sibling
`docs` leg.
