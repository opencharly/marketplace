# 2026.223.0000 — Regenerate the image and layer skills from the charly-image candy docs

Regenerate `image/skills/image/SKILL.md` + `image/skills/layer/SKILL.md` from the `charly-image` candy's skill: entities (generated output — the committed state is byte-for-byte `charly marketplace generate`). Three doc additions, plus the round-1 validator's minor fix:

- the `extract:` install-file row (image skill) — `extract:` counts as an install file for Agent Driven Evaluation (ADE) validation (the HasExtract rule), the charly-native way to reuse a binary/static tree from an upstream image without its init/scripts
- the `download:` destination-created-before-extraction behavior (layer skill) — mirrors the sdk download fix (sdk#127, merged `2d01e8a`) and matches `copy:`'s auto-create-parents
- the "Charly-owned start scripts" section (layer skill) — the rootless `write:`-script pattern the agentteams candies use
- ADE expanded at first use in the image skill's new `extract` row (pr-validator 1b.1 item 12)

(placeholder — the pr-validator finalizes the CalVer at merge)
