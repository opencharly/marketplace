# 2026.229.2258 — a CI-gate claim the mechanism-keyed sweep missed, and a page that counted its own sections

**This is a REGENERATION, not an authored edit.** Both files are emitted by
`charly marketplace generate` from entities in `candy/charly-internals/charly.yml`, landing as
`opencharly/charly#289`.

## What arrives

| artifact | correction |
|---|---|
| `internals/skills/plugin/SKILL.md` | *"`TestBuiltinPluginSchemasSplice` is the CI gate that every builtin schema splices"* — `release-binary.yml` is the only workflow and it runs no `go test` |
| `internals/skills/git-workflow/references/evidence-and-freshness.md` | a whole-page section count, and a grouping that claimed five sections while naming four |

## A sweep inherits the shape of what it already found

`charly#289` retired *"`drift` is the CI gate"* after measuring that this repository has exactly one
workflow and it runs no tests. The identical claim about a different test survived, and so did a
third in `CLAUDE.md` — *"CI-enforced by `charly/import_purity_test.go`"* — which `AGENTS.md` states
without any enforcement claim, so the two rulebooks diverged on that sentence alone.

**The two survivors failed differently, and the difference is the useful part.** One was **reached**
by the sweep and cleared by a reader as a different subject. The other was **never reached**, because
no key derived from an already-found phrasing matches a differently-worded instance.

> **A sweep keyed to the phrasing of a site you already fixed can only rediscover sites shaped like
> it.** The class here is *asserts a CI gate that does not exist*, and a class has no closed set of
> wordings — so "nothing found" is never "none exists".

Keys chosen independently of every site this cutover touched — `enforced by`, `mechanically
(enforced|disabled)`, `blocks the merge`, `fails the build`, `branch protection`, `hook-enforced`,
`standing gate`, `automatically (run|check|enforc)` — return zero corpus-wide.

## A page that counts its own sections is a fixed point with a moving subject

The evidence page stated a total for itself. A merge added a section, and the sentence was false
without being touched.

> **A section count is a figure whose subject is the document stating it.** Removing it is the fix;
> restating it buys a figure with the same expiry.

The count is dropped rather than corrected. A neighbouring grouping keeps its number, because its
members are enumerated in the same sentence — **a count whose members sit beside it is
self-checking; a total with nothing near it to check against is not.**

## Proximity to a rule provides no protection

The rule that should have caught the surviving CI-gate claim — *a finding stated as an instance
protects only the sites it names* — lives in `charly`'s `CHANGELOG/2026.228.0609.md`, not on the
page carrying the defect. **The lesson and the defect were never in the same document**, so nothing
in the page's neighbourhood was going to surface it.

> **A rule recorded in a CHANGELOG protects the next cutover's prose only if someone carries it
> there.** Knowing a rule, and having it nearby, are both weaker than having a check that runs.

## The fixes were generating the findings

This leg opened as two sentence corrections and reached `+31/-8` on one page before landing at
`+2/-2`. Of thirteen review findings, ten were in text the leg's own fixes had added, and six of
those were created by the fix for the finding before them.

> **A fix that adds explanation creates a new surface with the same failure mode as the defect it
> corrects.** On a page whose *subject* is that failure mode, the recursion is not a risk — it is
> guaranteed.

**Every round was individually correct**: each finding real, each fix addressing what it named. A
pathological sequence composed entirely of correct steps is invisible from inside any step, so the
detector cannot be care — it has to be a trend:

> **When a change's diff grows every round while its defect count does not fall, the fixes are the
> source.**

The page therefore carries only its two corrections plus the arithmetic error they exposed. The
round-by-round account is in the pull request, permanently readable at `refs/pull/199/head`, and is
deliberately not repeated here.

Placeholder CalVer — the `pr-validator` finalizes it at merge and rewrites this H1 to match.
**Verify that rewrite by reading the committed object** (`git show <ref>:<path>`), never the working
tree.
