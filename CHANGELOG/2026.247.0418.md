# 2026.247.0418 — chore(corpus): advance layer-himalaya ref to v2026.246.1950

## Summary

The corpus still pinned layer-himalaya at v2026.243.0409 (pre-chacha20-fix). Advance to v2026.246.1950 (the `--locked` drop tag, layer-himalaya #4) to eliminate the "resolved to multiple versions" pin-drift warning in the openclaw bed evidence (distro-cachyos#61).

## How tested

```
$ charly -C . marketplace generate --root . --out .
(regeneration completed)
```

```
$ git diff --stat
 candy/charly-marketplace/charly.yml | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)
```

The drift gate (deploy.yml) regenerates the corpus and fails on mismatch — this commit carries the exact regenerated output.

## Rulebook compliance

- R1: RCA — the layer-himalaya pin-drift warning traced to this corpus's stale ref (v2026.243.0409); advanced it.
- R3: no duplication — the corpus is generated from the refs list; only the refs list is hand-edited.
- R4: no workaround — regeneration is the sanctioned path.
- R6: git safety — feat/ branch, no force-push.
- R7: gate — the drift gate validates the regenerated corpus.

## Change classification

- Change class: config (corpus regeneration)
- Verification gate: drift gate (deploy.yml) + R10 beds (check-openclaw-*-pod on distro-cachyos)
- Attribution tier: analysed on a live system

*Assisted-by: pi ollama-cloud/deepseek-v4-flash:0731 (analysed on a live system)*