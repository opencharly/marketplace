# 2026.227.0753 — add the helm skill; publish the agentteams + helm dispatcher rows

The Factory north-star's unit 4 (docs catch-up) plugins leg
(`PROGRAM/factory-north-star.md`). Units 2 and 3 landed capability — the helm
words (`step:helm-release`, `verb:helm`, the `helm_charts:` emission) and
`verb:agentteams` — with no Skill Dispatcher row and, for helm, no skill at
all.

The dispatcher is a CURATED fast path rather than an index: 60 of 307 skill
entities carry `triggers:`, and the rulebook documents the fallback for
everything else ("Anything not listed: read the index first"). So a missing row
does not make a capability unreachable — it makes it easy to miss at the first
tool call, which is where R0 routing actually happens. For surfaces as central
as the helm words and the entire agentteams family, that is a real routing gap.
This closes it.

- `kubernetes/skills/helm/SKILL.md` — NEW, generated from the `helm-skill:`
  entity added to `candy/charly-kubernetes/charly.yml`. Covers the
  `step:helm-release` install step (the `#HelmReleaseStep` field set, the
  three-arm KUBECONFIG precedence chain — an operator-set `KUBECONFIG`, else the
  k3s guest default `/etc/rancher/k3s/k3s.yaml`, else `$HOME/.kube/config`, the
  arm that carries a non-k3s cluster — and the `helm uninstall` ReverseOp
  the host records and replays), the four `verb:helm` methods
  (`release-exists`, `status`, `revision`, `values-hash`) with their
  method-exclusive modifiers, the `kubernetes:` substrate arm (`helm_charts:` →
  kustomize `helmCharts:` behind `--enable-helm`), and the two beds
  (`check-helm-vm`, `check-k8s-deploy`).

  The skill carries the authoring law the unit 2 cutover surfaced: a bed's own
  plan runs verify-only and `charly fleet add` lowers only CANDY plans' `run:`
  steps, so a mutating install must live in a candy's `run:` steps, never in the
  bed's plan — with the second-order trap that a `check:` step does not run
  during `fleet add`, so an install candy waits for node readiness itself.
  `candy/helm-chart` is the worked example.

- Dispatcher rows (generated into the superproject's `CLAUDE.md` + `AGENTS.md`
  between the `GENERATED SKILL DISPATCHER` markers, from `triggers:` on the
  skill entities — the table is generated, never hand-edited):
  `/charly-kubernetes:helm`, `/charly-agentteams:agentteams-cli` (the
  `charly agentteams` CLI + the `verb:agentteams` methods), and
  `/charly-agentteams:agentteams` (the box and its two beds). The agentteams
  family has shipped since its Unit 7 landing with zero dispatcher rows.

- `README.md` — the `charly-kubernetes` bucket row named only the `kube:` verb;
  it now names the helm words too.

## A revert this cutover absorbed, and had to undo

Regeneration carried a SECOND, unintended delta the first two rounds missed:
`marketplace.json`'s top-level description silently lost both "Kimi" and "the
Agent-in-the-Loop Factory" — reverting branding that `c251846`, the commit this
branch sits directly on, had landed one commit earlier.

Root cause: `c251846` landed the plugins half of the rebrand as GENERATED
output, but its superproject SOURCE half never landed (`candy/charly-hooks`'s
`marketplace:` block still held the pre-rebrand text; no remote branch carries
the fix). A generated tree ahead of its source silently reverts on the next
regeneration by anyone — and this PR was that next regeneration.

Fixed at the source in charly#274, restoring the exact text already merged and
published here, so source and generated output agree again. Notably this is the
same drift pair `c251846` closed, reopening in the opposite direction — the very
shape this cutover called a regression when it fixed the `charly-kubernetes`
row.

## Gate

Documentation-only change class at `documentation reviewed`: no bed.
`charly box validate` exit 0; `charly docs generate` exit 0 over 6 repo roots.
`charly marketplace generate` diffed line-by-line against `origin/main` rather
than eyeballed — every delta accounted for: the `charly-kubernetes` description
in `marketplace.json` + both `plugin.json`s (three fields in the codex manifest,
hence six changed lines), the README row, and the two new files. That
line-by-line diff is the practice the missed revert argues for; "no-op except
the intended delta" asserted without it is how the revert got through twice. Placeholder CalVer — the
pr-validator finalizes it at merge.
