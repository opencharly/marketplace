# 2026.227.1016 — helm skill: polish + two corrected mechanism cross-references

Regeneration of the `helm-skill:` entity after the source fixes in
opencharly/charly#274, following that PR's first validation round. The generated
page is a projection — every change here was made at the source, never on this
file.

- The `plan_grammar.go:42` citation now carries its path
  (`candy/plugin-check/plan_grammar.go:42`). Every other citation on the page is
  path-qualified; this one was not, on a page whose recent history is entirely
  about verifiable mechanism claims.
- "an `helm upgrade --install`" → "a". This sits in the frontmatter
  `description:`, which is published as the page subtitle.
- "Three things about that readiness step are deliberate" bolded only two of them.
- The `context: [runtime]` instruction named "the verb" as its nearest
  antecedent; it is the STEP you author.
- **The one that mattered.** The `fleet add` passage ("giving it
  `context: [deploy]` would not change that") and the `verb:helm` passage
  ("this is what makes it skip under `charly check box`") make emphatic X-not-Y
  claims about the same YAML step, ~100 lines apart, reaching opposite-sounding
  conclusions. Both are correct — they describe different walks — but a cold
  reader read them as a contradiction, and that confusion produced blocking
  findings in three separate validation rounds. Each passage now names its scope
  and cross-references the other.

Documentation-only change class. `charly box validate` exit 0;
`charly marketplace drift` clean (399 artifacts); `charly docs generate` exit 0
over 6 repo roots. Placeholder CalVer — the pr-validator finalizes it at merge.
