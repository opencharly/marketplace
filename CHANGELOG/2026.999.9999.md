# 2026.999.9999 — regenerate the worktree-session-lifecycle skill references

Regenerated from the superproject's `candy/charly-internals/charly.yml` manifest edits (cutover `worktree-session-lifecycle`) via `charly marketplace generate`. Every session that commits/branches/pushes now works in its OWN worktree under `.claude/worktrees/<slug>/`, branched off fresh `origin/main`; the main tree stays on `main`. Four skill reference files changed:

- `internals/skills/git-workflow/references/branch-and-pr-loop.md` — B1 gains **Step 0** (create the session worktree under `.claude/worktrees/<slug>/`, branched off fresh `origin/main`; the main tree stays on `main`), and Step 1's sync-before-start uses `git worktree add … -b feat/<slug> origin/main` instead of `git switch -c` in the shared main tree. B4 "Worktree hygiene" is expanded into the full per-session **create → work → land → remove** lifecycle.
- `internals/skills/git-workflow/references/multi-repo-coordination.md` — B7 states the one-worktree-per-session rule and that the main tree stays on `main`.
- `internals/skills/agents/references/hooks-and-lifecycle.md` — "Worktree lifecycle is orchestrator-owned" is scoped to the orchestrator+teammates model; **independent parallel sessions own their own worktree lifecycle**.
- `internals/skills/agents/references/orchestration-model.md` — the responsibility matrix gains a "Standalone sessions" paragraph with the same ownership clarification.

This is the `plugins` leg of a cross-repo docs cutover (plugins → docs → superproject). Regeneration is a no-op on a clean tree. Placeholder CalVer — the pr-validator finalizes it at merge.
