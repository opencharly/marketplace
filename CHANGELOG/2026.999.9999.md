# 2026.999.9999 — regenerate the agentteams-cli skill for the agentteams: check verb

The Unit 2 cutover (opencharly/charly#266) added the declarative
`agentteams:` check verb to `candy/plugin-agentteams`, but the generated
SKILL.md for the `agentteams-cli` skill was not regenerated — the verb's
method surface (`status` / `manager-running` / `worker-running` /
`worker-list`) was missing from the published skill.

Regenerated via `charly marketplace generate` from the candy's `skill:`
entity. One file changed:

- `agentteams/skills/agentteams-cli/SKILL.md` — the new "The `agentteams:`
  check verb" section documents the four methods, the host-based (mcp-pattern)
  probe mechanism, and the skip-under-`charly check box` behavior.

This is the `plugins` leg of the Unit 2 regen debt (charly#266). Regeneration
is a no-op on a clean tree. Placeholder CalVer — the pr-validator finalizes it
at merge.
