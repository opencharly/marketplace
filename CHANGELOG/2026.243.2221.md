# 2026.243.2221 — chore: bump the refs list to the tightened-description tags and regenerate the corpus

## Summary

Advance 12 `@github` pins in `candy/charly-marketplace/charly.yml` to the tags carrying the trigger-style skill-description tightening (each landed and validated in its owning repo: layer-charly-internals/-extra, layer-charly-build/-check, pod-charly-core/-automation, layer-charly-kubernetes/-local/-image/-pod, vm-charly-vm, plugin-wl), then regenerate the corpus with the CI-pinned charly (v2026.241.1243).

The regenerated SKILL.md files carry the tightened `description:` frontmatter (trigger-style index lines, same routing signal, ~45% fewer bytes). The corpus diff is description-focused. **No capability reduction**: every skill remains discoverable and loadable.

## How tested

- Regeneration: `/tmp/charly-pinned/bin/charly marketplace generate --root . --out .` — **EXIT=0**, 405 artifacts across 27 families, no errors.
- Idempotency: a re-run of the regeneration against the same refs list produced an identical diff (94 files, +196/−320) — measured against the pre-merge tree. After merging `origin/main` (which carried other repos' pin bumps from the daily refresh), the committed diff vs the PR base is **94 files, +105/−307** (per `gh pr view --json additions,deletions,changedFiles`). The corpus is a no-op regeneration of the merged refs list (the deploy drift gate re-verifies on CI).
- Diff review: the SKILL.md changes are the tightened description frontmatter; no unexpected large changes.

## Rulebook compliance

- **R3** — no duplication: the corpus is generated from the refs list; the refs bump is the only hand edit.
- **R5** — the refs list advances to merged tags only (never PR branches); the corpus is regenerated in the same change (the drift gate requires it).
- The owning repos validated their own description changes through their own pr-validators before tagging; this PR republishes what they landed.

## Change classification

- **Change class:** generated-corpus regeneration + refs bump (metadata-prose descriptions; zero behavior change)
- **Verification gate:** regeneration EXIT=0 + idempotency (no-op re-run) + diff review
- **Attribution tier:** `analysed on a live system` — the regeneration ran live with retained output; the drift gate re-verifies on CI.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*