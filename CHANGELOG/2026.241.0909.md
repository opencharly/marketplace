# 2026.241.0909 — fix: publish the skill corrections — advance the CI charly pin and four candy refs

## Summary of changes

Five merged PRs corrected skills that taught YAML the loader **rejects** and CLI flags that
**exit 80**. The corpus is generated from pinned refs, so none of those corrections reached a
single consumer — the skills people actually load still teach the broken forms. This PR is the
delivery step.

Concrete edits:

- **`.github/workflows/deploy.yml`** — the CI-time charly pin `v2026.241.0522` → `v2026.241.0848`
  (the tag on charly#442's merge, verified below to carry its candy-skill fixes).
- **`candy/charly-marketplace/charly.yml`** — four candy refs advanced to the tags carrying the
  standalone repos' corrections:

  | Candy repo | Now | Carries |
  |---|---|---|
  | `vm-charly-vm` | `v2026.241.0719` | inline `libvirt:` (deleted child-node form removed) |
  | `layer-charly-local` | `v2026.241.0732` | `ssh_arg` inline; `candy` / `add_candy` |
  | `pod-charly-core` | `v2026.241.0735` | five rejected flags removed |
  | `pod-charly-automation` | `v2026.241.0820` | three rejected flags removed |

- **23 regenerated `SKILL.md` files** across `automation`, `build`, `check`, `core`, `image`,
  `kubernetes`, `local` and `vm` — carried in the same commit as the pins that produce them, per
  this workflow's own contract ("a marketplace PR must bump the charly CI-time tag and carry the
  regenerated corpus").

## Change class

**cross-repo (consumer leg)** — a CI workflow pin, four candy refs, and the generated corpus they
produce. No Go source, no schema. Single repo here; the producer legs are already merged.

**Closes / relates to:** consumes `opencharly/charly#442` (via the pin) and `vm-charly-vm#2`,
`layer-charly-local#2`, `pod-charly-core#2`, `pod-charly-automation#3` (via the refs) — all
merged and tagged. **Paired leg:** `opencharly/charly#444` carries the SAME four ref values into
charly's copy; `task skills:refs` requires the two byte-equal, and they were diffed identical
before this commit. Producer→consumer order is already satisfied: every tag pinned here exists
and was verified to contain its fix.

**Breaking change + rollback:** none for consumers — the corpus changes are corrections to
documentation content; no skill, family, catalog entry or manifest is renamed or removed.
Rollback is a plain revert, which restores both the pins and the previous corpus in one step
(they move together, which is the point).

## How R10-tested — exact gate, commands, and pasted output

- **The gate for this change class:** this repo's own `marketplace` workflow, which runs on
  `pull_request` — it clones charly at the CI pin, builds the binary from that checkout,
  regenerates the whole corpus and **fails closed** on any drift. I reproduced that sequence
  locally, in its own order, before opening this PR.
- **Eval beds run:** `N/A (workflow pin + generated corpus)` — no `disposable: true` bed maps to
  it; the authority is the workflow's fail-closed drift gate, run locally and again on this PR.
- **Fresh rebuild:** the generating binary was **built from the pinned checkout itself**
  (`task build:binary` inside a worktree at `v2026.241.0848`), not from some other charly tree.
  When I first tried to drift with a binary built from a different commit, charly's freshness
  check REFUSED it — I obeyed that and rebuilt rather than setting
  `CHARLY_SKIP_FRESHNESS_CHECK`.
- **Did the CHANGED code path execute live?** **Yes** — generation and drift both ran against
  these exact pins and this exact refs list, and the emitted corpus was inspected for the
  corrections it is supposed to carry.
- **Concurrency:** `N/A` — pins and generated files; no shared state, lock or lifecycle.

*1. Fresh-rebuild gate output* (`charly version` + the gate run, at zero warnings):
```
CI's exact sequence, reproduced locally:

$ git worktree add --detach ../charly-at-pin2 v2026.241.0848     # charly at the NEW CI pin
$ cp candy/charly-marketplace/charly.yml ../charly-at-pin2/candy/charly-marketplace/charly.yml
$ cd ../charly-at-pin2 && task build:binary                      # binary FROM the pinned checkout
task: [build:binary] mv bin/.charly.next bin/charly

$ ./bin/charly marketplace generate --root ../charly-at-pin2 --out ../marketplace
$ ./bin/charly marketplace drift    --root ../charly-at-pin2 --out ../marketplace
marketplace drift: clean (405 artifact(s) on disk match their sources; regeneration is a no-op)

Zero warnings. Regeneration on the committed tree is a no-op, which is what the CI gate asserts.
```

*2. Each changed code path executing* (the lines proving the changed branch ran, not that it built):
```
(a) The corpus consumers load NOW carries the corrections — greps against the regenerated
    SKILL.md files in this commit, not against the candy sources:

  vm/skills/vm/SKILL.md              "CHILD NODE" survivors      -> 0     (dead form gone)
  local/skills/local-deploy/SKILL.md "ssh_args" survivors        -> 0     (wrong key gone)
  core/skills/logs/SKILL.md          "-n, --lines" survivors     -> 0     (rejected flag gone)
  build/skills/settings/SKILL.md     "CHARLY_BUILD_ENGINE"       -> 1     (real env var present)
  check/skills/wl-overlay/SKILL.md   "method: overlay-show"      -> 11    (all examples converted)
  check/skills/libvirt/SKILL.md      "LibvirtVerbInput"          -> 1     (correct def name)

(b) The CI pin's target actually carries charly#442's fixes — checked at the TAG, before pinning:

$ git log --oneline -1 v2026.241.0848
f7f56049 fix(skills): correct charly's own candy skills to the current schema and CLI (#442)
$ git show v2026.241.0848:candy/charly-check/charly.yml | grep -c LibvirtVerbInput
1
$ git show v2026.241.0848:candy/charly-build/charly.yml | grep -c CHARLY_BUILD_ENGINE
1

    An earlier run of this same check returned 0/0 and I nearly recorded the tag as not carrying
    the fix — it was a stale local tag ref; after `git fetch --tags` it resolves as above. The
    numbers here are the post-fetch ones.

(c) The two refs-list copies are byte-equal, which `task skills:refs` requires:

$ diff -q candy/charly-marketplace/charly.yml <charly>/candy/charly-marketplace/charly.yml
IDENTICAL

(d) Scope of the regeneration — exactly the 23 SKILL.md files whose sources changed, plus the two
    files I edited. No unrelated artifact moved:

$ git status --short          # after generate
 M .github/workflows/deploy.yml
 M candy/charly-marketplace/charly.yml
 M automation/skills/{enc,sidecar}/SKILL.md
 M build/skills/{generate,settings}/SKILL.md
 M check/skills/{libvirt,wl-overlay}/SKILL.md
 M core/skills/{cmd,deploy,logs,service,shell,stop}/SKILL.md
 M image/skills/{image,layer}/SKILL.md
 M kubernetes/skills/kubernetes/SKILL.md
 M local/skills/{local-deploy,local-spec}/SKILL.md
 M vm/skills/{arch-cloud-vm,cachyos-bootstrap-vm,debian-debootstrap-vm,ubuntu-debootstrap-vm,vm,vms-catalog}/SKILL.md
```

*3. Docs-only evidence* (the R5 grep self-test result + the cross-reference/markdown review):
```
N/A as a change CLASS — this is not docs-only: it moves a CI pin and four resolution keys, and
the `*.md` files in the diff are GENERATED artifacts carried under a fail-closed drift gate, not
hand-authored documentation. The gate that applies is block 1's regeneration no-op, which is
stronger than a markdown review: any hand edit to those files would show up as drift.
```

## Attribution tier

**fully tested and validated** — the binary was built from the pinned checkout, generation and
drift both ran in CI's own sequence on the final committed tree and return `clean` over 405
artifacts, every pinned tag was verified to exist AND to contain the correction it is pinned for,
and the emitted corpus was grepped to confirm the corrections actually reached the files
consumers load.

## Harness rulebook compliance — state HOW each is satisfied (or `N/A — <reason>`)

- **R0 skills:** `/charly-internals:git-workflow` loaded before the first git call and diffed
  against the current corpus commit before being followed. Branch off fresh `main` → PR → fresh
  validator; no self-merge, no force-push.
- **R1 RCA + zero warnings:** two of my own intermediate results were WRONG and were corrected
  rather than reported — a tag-content check that returned 0/0 against a stale local tag ref
  (re-run after `git fetch --tags`), and a `marketplace drift` invocation that failed with
  `unexpected argument marketplace` because I ran it from the marketplace clone rather than a
  charly project, where that plugin word is prescanned. Both were diagnosed, not retried blindly.
  Gate output is at zero warnings.
- **R2 no out-of-scope:** this is the delivery half of five merged PRs; without it the corrections
  exist in candy sources but reach nobody, which is exactly the "self-consistent with a stale
  pin" failure this corpus has hit before. Nothing is deferred.
- **R3 no duplication:** the pins exist once each; the generated corpus is produced by the single
  generator from the single refs list. Nothing is hand-edited into the emitted files.
- **R4 no workaround:** the corpus was REGENERATED by the tool, never hand-patched to look
  correct — those files carry a DO-NOT-EDIT header and a hand edit would surface as drift. The
  binary freshness refusal was obeyed by rebuilding, not bypassed with the documented escape
  hatch.
- **R5 hard cutover + grep:** `N/A — value advances plus their generated output; no identifier
  removed or renamed.` The sweep half is block 2a: the strings the corrections REMOVE now have
  zero survivors in the emitted corpus, which is the meaningful sweep for a generated tree.
- **R6 git safety:** `git status` + `git stash list` checked before branching; branch cut from
  freshly fetched `main`; no force-push, no history rewrite. Post-commit status clean and
  `git show --stat` lists exactly the 25 intended files. `git log origin/<branch>..HEAD` is empty
  before the body's claims are made, so the diff the validator reads is the diff described here.
- **R7 runtime gate:** proven by RUNNING generate + drift in CI's sequence and inspecting the
  emitted artifacts, not by reading the diff.
- **R8 artifact invariants:** the emitted artifacts were asserted after generation, not assumed —
  the corrections were grepped in the regenerated SKILL.md files (2a), the change set was
  confirmed to be exactly the expected files (2d), and re-running the generator is a no-op (1).
- **R9 binary == source + deps:** the generating binary was built FROM the pinned checkout, which
  is the strictest available form of this rule here — the binary and the source it generates from
  are the same commit. No new runtime OS dep.
- **R10 disposable + coverage:** no bed maps to a pin-and-regenerate change, so this branches to
  the fail-closed drift gate, run on the final committed tree and re-run by CI on this PR. The
  coverage that would fail without this change: with the old pins the generator emits the OLD
  skill text — block 2a shows the corrected strings present and the broken ones at zero.
- **RDD / ADE / SDD:** the two high-risk assumptions were both PROVEN before being relied on —
  "the pinned tags carry the fixes" (2b, at the tag) and "regeneration from these pins is
  reproducible and complete" (1 + 2d). ADE: no candy authored here. SDD: `N/A`, no `.cue` edited.
- **Hard cutover:** ONE atomic commit — pins and the corpus they produce move together, so no
  intermediate state publishes a corpus that disagrees with its own pins. No phase 2, no TODO.
- **Kernel/plugin boundary law:** `N/A — no core or sdk code; this repo carries the corpus.`
- **Disposable-only autonomy:** `N/A — nothing destroyed or rebuilt; no deploy target touched.`
- **Clean architecture + Go gates:** `N/A — no Go source in the diff.` The Go toolchain ran as
  part of building the binary from the pinned checkout and completed cleanly.
- **CHANGELOG:** `N/A` — `tag-on-merge` writes `CHANGELOG/<CalVer>.md` from this body at merge and
  mints the tag; an author-staged CalVer file is the deprecated flow and would orphan an entry.

---

*Assisted-by: Claude Code claude-opus-5[1m] (fully tested and validated)*