# 2026.247.1137 — feat(corpus): crabbox skills — CLI, coordinator, check verb, automation

## Summary

Corpus regeneration for the Crabbox wave (Phase 4): after the refs update,
`charly marketplace generate` emits the crabbox skill surface:

- **refs added** (candy/charly-marketplace/charly.yml): `layer-crabbox`
  v2026.247.0855, `pod-crabbox` v2026.247.1016,
  `plugin-crabbox/candy/plugin-crabbox` v2026.247.1021.
- **refs bumped**: `pod-charly-automation` v2026.247.1126 (carries the
  crabbox-deploy automation skill), `pod-charly-hooks` v2026.247.1133
  (registers the `crabbox` skill family in the marketplace entity).
- **New corpus**: `crabbox/` (CLI + coordinator skills),
  `check/skills/crabbox/` (the `crabbox:` check verb),
  `automation/skills/crabbox-deploy/`; generated plugin manifests updated
  (marketplace.json / plugin.json / profiles.json).

## How tested

```
$ charly -C . marketplace generate --root . --out .
GEN OK — changed: 4 generated manifests + the refs file; new: crabbox/,
check/skills/crabbox/, automation/skills/crabbox-deploy/

Regression check: regeneration on this tree is a NO-OP (drift gate clean) —
run twice, second run produces no diff.
```

## Rulebook compliance

| Rule | How this PR satisfies it |
|---|---|
| R0 — skills first | marketplace skill loaded before regeneration. |
| R1 — RCA every anomaly | Family-registration BLOCK RCA'd (the marketplace entity owns families); validate 0/0; no surviving warnings. |
| R2 — finish the whole cutover | Refs + corpus land in ONE PR (generated output is the corpus). |
| R3 — no duplication | Corpus is GENERATED from the candy skill: entities (refs are the one input — never hand-edited output). |
| R4 — no workarounds | Refs bumped to MERGED tags only; generation is the sanctioned mechanism. |
| R5 — delete legacy | N/A (new corpus dirs). |
| R6 — git safety | No force-push; branch-per-change PR. |
| R7 — prove behaviour | The generator ran live; the drift gate (regen no-op) proves reproducibility. |
| R8 — preserve emitted artifacts | Corpus regenerated from source entities; manifests validated by the generator. |
| R9 — binary equals source | Fresh worktree-built bin/charly used. |
| R10 — fresh disposable proof | N/A — corpus/generation change (no runtime path); the crabbox runtime surface is bed-proven (distro-cachyos #62). |
| RDD/ADE/SDD | Corpus mirrors the source skill entities; no spec changes (N/A SDD). |
| Concurrency | N/A. |
| Disposable-only gate | N/A — no targets created/destroyed. |
| Hooks doctrine | N/A standalone repo; merge gate = org pr-validator. |

## Change classification

- **Class:** generated corpus (skills + manifests).
- **Verification gate:** `charly marketplace generate` drift check (regen
  no-op) + the generated corpus validates.
- **Attribution:** fully tested and validated.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*