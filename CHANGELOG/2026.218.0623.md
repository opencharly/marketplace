# 2026.218.0623 — K-wave 2: the quadlet-path claim repoint

The plugins docs follow-up of the K-wave 2 residue-ledger program: deploy/SKILL.md
claimed the quadlet config-write "stays core, #86" — the quadlet write is fully
plugin-side (candy/plugin-deploy-pod + sdk/deploykit/quadlet_paths.go); the former
core quadlet path is DELETED.
