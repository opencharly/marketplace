# 2026.250.1446 — docs: MCP/systemd wave corpus — refs bump + regenerated skills

## Summary

The MCP/systemd wave's corpus regeneration: the source repos' skill entities were updated by their own doc PRs (layer-charly-build #10 — the charly-mcp-cmd skill; layer-charly-check #10 — the verb catalog + live-probe-verbs; layer-charly-image #8 — the layer skill's ServiceDeclaration). This PR bumps the marketplace's refs to those merged+tagged releases (v2026.250.1404 / .1402 / .1401) and carries the regenerated corpus (5 files):

1. **mcp-cmd skill**: the mcp: check verb's substrate-neutral endpoint resolution — the OCI label leg on container venues + the check-env `mcp_provide` fallback for VM/host venues (effectiveMCPProvides; a VM's declared forward threaded host-routable by plugin-check); `charly mcp serve` binds **127.0.0.1:18765 by default** (loopback only; the host is the security boundary) with `--listen 0.0.0.0:18765` the explicit opt-in; the generated packages ship non-autostarting system+user units + /etc/charly/charly.yml; child processes inherit the FULL environment (no CHARLY_* stripping).
2. **verb catalog + live-probe-verbs**: the addr: `HOST_PORT:<guest>` VM-venue runtime vars (a VM's network.port_forwards host ports threaded via the reverse channel) + the mcp: verb's VM/host venue resolution.
3. **layer skill**: a use_packaged service entry's `overrides:` (exec/env/after) materializes as a systemd drop-in (sdk/deploykit's packaged-service compile renders it; the executor writes + daemon-reloads before enable --now; the shipped unit untouched); the generated packages ship the non-autostarting units + the config.

## How tested

The refs bump is a config-bearing diff, so this is claimed as **analysed on a live system** (A3): the refs resolve (the three tags exist upstream — verified by git ls-remote), and the regeneration ran live on this machine with retained output:

```
$ charly marketplace generate --root <abs marketplace> --out <abs marketplace>
exit=0
$ git diff --stat
 check/skills/check/references/verb-catalog.md |  8 ++++++++
 image/skills/layer/SKILL.md                   | 20 ++++++++++++++++++++
 5 files changed, 62 insertions(+), 9 deletions(-)
```

All five changed files are the regenerated corpus entries whose sources merged upstream (the three doc PRs above, each validator-PASS + merged + tagged); the refs diff changes EXACTLY the three MCP/systemd layer pins (layer-charly-build v2026.250.1404, layer-charly-check v2026.250.1402, layer-charly-image v2026.250.1401) — the layer-charly-internals pin is unchanged at v2026.250.1222 (restored after a merge-conflict resolution briefly kept the pre-#288 value).

## Rulebook compliance

- R0: the marketplace generate skill + the docs pattern (the crabbox corpus wave).
- R1: the corpus documented the pre-wave behavior (OCI-label-only mcp:, no serve bind default); the source PRs corrected at the source + this PR ships the regenerated corpus.
- R3: the corpus is generated from the single source (the skill entities); no hand-edits here.
- R6: worktree, PR-only.

## Change classification

- **Change class:** docs (corpus regeneration + refs bump).
- **Verification gate:** the generation exit 0 + the diff is exactly the regenerated entries.
- **Attribution tier:** analysed on a live system — the generation ran live (exit 0, the corpus regenerated from the tagged sources); the changed path (the refs resolution + the regeneration) executed live with retained output above.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*