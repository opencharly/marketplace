# 2026.999.0001 (placeholder — pr-validator finalizes at merge)

The plugins (skills) leg of the K-wave seam-death terminus: the documentation commits keeping the
skills truthful through the five program waves and the terminus defect arc.

- **check-k8s + check**: stop teaching `cluster: "${DEPLOY_NAME}"` — for a VM-mode live check
  DEPLOY_NAME resolves the SHARED kind:vm entity, not the deploy, so sibling beds collide; the
  worked example now names the bed's own literal kind:k8s profile, with an explicit
  "do NOT write ${DEPLOY_NAME}" warning (#18).
- **agents + versa**: the stale `port:[auto]` sentinel corrected to absence-IS-auto (#19).
- **W3/W4 relocation sweeps**: file-attribution and identifier references corrected across the
  check/build/deploy skills as the subsystems moved (resolveVerbGraphics, RenderService /
  validateTextEgress, UnifiedDeployTarget.Test, check-bed dissolution references, and siblings).

- **The spec-repo relocation sweep (PR #131 F1-F4 round)**: the schema and generated param structs
  moved out of the sdk contract module into the `spec` submodule, and 31 skill/agent files still
  named their old homes. Every live claim is repointed and re-verified against the tree:
  `sdk/schema/*` → `spec/schema/*`, `sdk/spec/*` → `spec/spec/*` (including the bare `spec/<file>.go`
  spelling, now super-relative `spec/spec/<file>.go`), `sdk/schemaconcat` → `spec/schemaconcat`,
  `sdk/proto` → `spec/proto`, `sdk/protocol` → `spec/protocol`, `sdk/internal/{schemagen,wiregen}` →
  `spec/internal/…`, and the relocated kit files (`sdk/kit/{deploy_executor*,venue_descriptor,
  builder_run,charly_install}.go` → `spec/exec/…`, `sdk/kit/reexec_ssh.go` → `spec/hostenv/reexec.go`).
  The prose framing followed: the schema is owned by the **spec** repo, `task cue:gen` chains into
  `spec/`, and `charly/go.mod` requires `github.com/opencharly/spec`. Explicit past-tense tombstones
  for the deleted `sdk/spec/*_wire.go` files keep their historical paths.
- **ZERO-ALIASES reached in core**: the skills no longer describe `charly/*_aliases.go` as live
  residue — no such file exists, and `TestZeroAliases_NoAliasFilesInCharlyCore`
  (`charly/import_purity_test.go`) holds it that way. The former `charly/vmshared_aliases.go` /
  `charly/kit_aliases.go` claims now point at the owning plugin/kit files.
- **The arbiter bracket**: `charly/arbiter_bracket.go` (`arbiterBracketedStart`/`Stop`) is gone.
  `charly/host_build_arbiter_bracket.go` keeps only the `CHARLY_PREEMPT_LEASE` env read/write as two
  F10 host-builders; the bracket's ownership is `candy/plugin-bundle/deploy_target.go`'s
  `handleLifecycleSimple` → `runLifecycleBracket`. Corrected in install-plan, deploy-targets,
  vm-deploy-target, and the go source map.

Documentation-only; gated by the non-runtime standards. The claims changed here were each
verified against the live tree in their originating cutovers (per-commit bodies cite the beds);
every path asserted by the relocation sweep was existence-checked against the assembled
superproject before it was written.
