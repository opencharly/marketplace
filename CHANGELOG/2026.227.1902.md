# 2026.227.1902 — regenerate the check skill for the build-scope verdict resolver

Projection of the superproject's `candy/charly-check/charly.yml` `skill:` source
change that lands with `opencharly/charly`'s check-verb-correctness cutover.
Generated by `charly marketplace generate`, never hand-edited; the diff is
exactly the one artifact the drift gate names, and nothing else:

- `check/skills/check/references/beds-and-r10.md` — a new subsection, "Which
  artifact a build-scope verb evaluates".

The source change documents behaviour that changed in that cutover: `charly
check box`, `charly box feature run`, and `charly box labels` resolve their image
argument through `spec/container.ResolveBuiltImageRef`, which refuses a bare
short name whose election is not the newest local BUILD of that box rather than
silently certifying the older artifact. Full defect narrative + the measured
reproduction: `opencharly/charly` `CHANGELOG/<this CalVer>.md`.

A `skill:` source edit and its regeneration are one cutover across two repos;
landing either half alone leaves `main` inconsistent (the drift gate goes red for
everyone, or the next regeneration silently reverts).

CalVer 2026.227.1902, generated at merge by the pr-validator.
