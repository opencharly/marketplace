# 2026.254.0457 — Rebrand the plugin manifests to the wonky DevOps tagline

## Summary

Rebrands the three plugin-manifest descriptions to the current tagline **"The wonky DevOps tool for you and your agents."** (matching the README and site branding):

- `.claude-plugin/marketplace.json` (`description`)
- `kimi.plugin.json` (`description` + `interface.shortDescription`)
- `package.json` (`description`)

No corpus regeneration and no `skill:`/`hook:` entity changes — generated family dirs are untouched.

## How tested

- `git grep -n 'open infrastructure compiler'` at the repo root → exactly 1 hit, `CHANGELOG/2026.240.0810.md` (dated history of the earlier rebrand; intentionally untouched).
- `git status` lists only the three manifest files; diffstat = 3 files, 4 insertions(+), 4 deletions(-).
- All three files pass `python3 -m json.tool` (valid JSON).
- Branch `feat/docs-brand` @ `e3a1ca9fc3eddf118ecb86df23a6db19f8606918` verified upstream (ls-remote matches).

## Rulebook compliance

- Docs-only change class: manifest descriptions are metadata, not pins/scripts; no pins or scripts changed.
- Corpus and CHANGELOG history untouched.
- PR-only landing via the org validator; no direct-main push, no force-push.

## Change classification

- **Change class:** documentation (plugin metadata descriptions).
- **Verification gate:** claim-keyed grep + JSON validation + scoped diff.
- **Attribution tier:** documentation reviewed — docs-only change class; no pins/scripts changed.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (documentation reviewed)*