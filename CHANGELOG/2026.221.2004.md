# 2026.221.2004 — Align the service skill with the corrected removal semantics

Regenerate the service skill so its `charly config remove` rows match the corrected teardown semantics: `charly config remove` disables the quadlet service only (the quadlet file remains), completing the sweep the 2026.221.1839 entry began (that sweep regenerated the config/deploy/remove skills but missed the sibling service skill). (placeholder — the pr-validator finalizes the CalVer at merge)
