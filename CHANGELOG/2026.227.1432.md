# 2026.227.1432 — evidence page: guard the merge-tree probe, scope the execution rule to content

Regenerated from the superproject's candy `skill:` entities by `charly marketplace generate`. The authoring change lives in `opencharly/charly`; this repo carries the generated surface.

One page changes: **`internals/skills/git-workflow/references/evidence-and-freshness.md`**.

The page shipped a shell recipe that fed `git merge-tree --write-tree` straight into a command substitution. `merge-tree` exits 1 on conflict and prints the tree oid followed by the conflict stages, so on a conflicting pair the substitution captures multiple lines and `git commit-tree` rejects it — and the obvious repair, piping through `head -1`, is worse than the bug: it yields a real, checkoutable tree whose files contain `<<<<<<<` markers, which the probe then runs a gate against. A gate that passes on that tree has certified conflict markers.

Both call sites are now guarded on the **exit status** rather than the output shape — the probe recipe refuses and tells the reader to resolve the conflict, and the identity test in the submodule-revert section degrades to the numstat form instead of reporting a false `DIVERGED`. The one surviving `head -1` is in the diff-inspection recipe, where a conflicted tree is exactly what the line wants; it now says so, and says it is correct nowhere else on the page.

The frame is the actual defect. The page split its content into epistemic and mechanism sections and promised execution only for the mechanism half — which routed the one broken shell recipe, sitting in an epistemic section, to the reasoning-is-enough side. Patching the recipe alone would leave the next one exposed, so the rule is rescoped to **content**: every runnable command and every mechanism claim is executed before publication, wherever it sits. The three `merge-tree` behaviours the guard depends on are now pasted as terminal evidence rather than asserted.

Two readability findings from the same review land with it: a symptom-indexed entry table so a cold reader reaches actionable routing before the epistemology, and an explicit note that `PR_HEAD=$(git rev-parse HEAD)` assumes cwd is the PR checkout.

Documentation-only change class at `documentation reviewed`; regeneration is idempotent.

**Note for the validator on this repo's `marketplace drift` state.** `charly marketplace drift` exits 1 against `opencharly/charly`'s current `main`, naming eight artifacts — `build/skills/build`, `build/skills/migrate`, `internals/skills/agents/references/parallel-bed-testing`, `internals/skills/egress`, `internals/skills/go`, `tools/skills/charly`, `tools/skills/cue`, `tools/skills/docs-site`. None of them is this change. They are the R4a task-target sweep that landed here as `#174` while its authoring half has not yet landed in the superproject, so the source still projects the pre-sweep prose and any regeneration reverts `#174`. This branch therefore commits its one file and leaves the other eight at their `#174` content untouched. Verified by running `drift` against a pristine `main` with no local modifications: exit 1, the same eight paths. The superproject leg is in flight on `feat/r4a-residue-sweep-2` and owned elsewhere; it is not this cutover's to land.
