# 2026.247.0158 — chore(corpus): regenerate for openclaw 2.0 batch

## Summary

Regenerate the marketplace corpus for the openclaw 2.0 batch:
- **pod-openclaw** ref v2026.243.0411 → v2026.246.2119 (openclaw 2026.9.1)
- **layer-openclaw-full** ref v2026.243.0818 → v2026.246.1954 (2.0 gateway + gogcli fix)
- **layer-charly-openclaw** ref v2026.243.2110 → v2026.246.1626
- Regenerated corpus picks up the openclaw 2.0 skill prose (healthz endpoint) and the agents-skill source sync.

This eliminates the "resolved to multiple versions" pin-drift warnings in the distro-cachyos beds (the corpus previously pinned pod-openclaw at the pre-2.0 tag).

**agents SKILL.md removal:** the "Subagent model resolution (pi harness)" section was a manual edit to the GENERATED corpus (from the pi-subagents #1 work); the source (layer-charly-internals) does not carry it, so the regeneration deterministically removes it. This is the sanctioned cleanup of a hand-edit to a generated file.

## How tested

```
$ charly -C . marketplace generate --root . --out .
(regeneration completed; diff below)
```

```
$ git diff --stat
 candy/charly-marketplace/charly.yml     | 6 +++---
 internals/skills/agents/SKILL.md        | 7 -------
 openclaw/skills/openclaw-layer/SKILL.md | 2 +-
 openclaw/skills/openclaw/SKILL.md       | 4 ++--
 4 files changed, 6 insertions(+), 13 deletions(-)
```

The pin-drift elimination is evidenced by the `check-openclaw-desktop-pod` R10 bed (distro-cachyos, calver 2026.247.0138) — the image-build step now shows `warnings=10 allowlisted=1 cache=589/642` with the pin-drift warnings gone (the corpus resolves pod-openclaw at the single 2.0 tag):

```
charly check run check-openclaw-desktop-pod: [image-build] PASS after 2m42.153s [warnings=10 allowlisted=1 cache=589/642]
charly check run check-openclaw-desktop-pod: [check-live] PASS after 2m11.356s [warnings=4 allowlisted=1]
charly check run check-openclaw-desktop-pod: PASS (steps=13)
```

The drift gate (deploy.yml) regenerates the corpus and fails on mismatch — this commit carries the exact regenerated output.

## Rulebook compliance

- R1: RCA — the distro-cachyos bed's "resolved to multiple versions" warnings trace to the stale corpus refs; this PR advances them, evidenced by the bed's image-build warnings dropping.
- R3: no duplication — the corpus is generated from the refs list; only the refs list is hand-edited.
- R4: no workaround — regeneration is the sanctioned path.
- R6: git safety — feat/ branch, no force-push.
- R7: gate — the drift gate validates the regenerated corpus; the R10 bed confirms the pin-drift elimination.

## Change classification

- Change class: config (corpus regeneration)
- Verification gate: drift gate (deploy.yml) + R10 bed (check-openclaw-desktop-pod)
- Attribution tier: fully tested and validated

*Assisted-by: pi ollama-cloud/deepseek-v4-flash:0731 (fully tested and validated)*