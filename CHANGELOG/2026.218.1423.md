# 2026.218.1423 — git-workflow: amending a PR is legal, force-push stays forbidden

The git-workflow skill's non-negotiable invariant no longer lumps "amend" in with the forbidden
force-push: amending a `feat/` branch is a normal authoring action, legal until the first push,
while force-push on ANY branch stays forbidden (a pushed branch advances only by adding commits;
`main` only fast-forwards via the validator's squash-merge). Matches the CLAUDE.md landing-policy
wording.

CalVer finalized at merge by the fresh pr-validator.
