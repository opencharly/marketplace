# 2026.267.2207 — chore(marketplace): republish the git-workflow skill (INCONCLUSIVE class)

## Summary

Republish the `git-workflow` skill so its new **INCONCLUSIVE (verdict-less) class** section reaches consumers, via the marketplace's normal source→corpus projection.

Two mechanical changes:
1. `candy/charly-marketplace/charly.yml` — bump the `layer-charly-internals` pin `v2026.265.1741` → `v2026.267.2049` (the tag that carries the skill update, `opencharly/layer-charly-internals#19`).
2. `internals/skills/git-workflow/SKILL.md` — the regenerated corpus, now carrying the section.

That is the entire diff — a clean 2-file regeneration (pin + projected skill). The projection carries the source `content:` verbatim under the standard `GENERATED by charly marketplace generate` banner.

The republished section documents the class the gate emits when its review engine produces no `Verdict:` line at all: the check stays RED on purpose but it is NOT a finding about the diff; the two root causes (stale welded engine vs provider/endpoint) and the diagnostics that distinguish them; and the rule that it must never be treated as a review finding or merged around.

## How tested

**Regeneration is a clean 2-file diff** — no unrelated corpus drift (the whole point of the marketplace drift gate):

```
$ charly -C <checkout> marketplace generate --root <checkout> --out <checkout>
...
exit: 0

$ git status --short
 M candy/charly-marketplace/charly.yml
 M internals/skills/git-workflow/SKILL.md

$ git diff --stat
 candy/charly-marketplace/charly.yml    |  2 +-
 internals/skills/git-workflow/SKILL.md | 47 ++++++++++++++++++++++++++++++++++
 2 files changed, 48 insertions(+), 1 deletion(-)
```

**The projection carries the source section** (`+47`, zero removals beyond the pin line):

```
$ grep -c "INCONCLUSIVE (verdict-less)" internals/skills/git-workflow/SKILL.md
1
```

**The pinned tag exists and carries the source section** (the generation input is real, not a phantom pin):

```
$ git -C layer-charly-internals ls-remote --tags origin v2026.267.2049
78faa89f8176d83f52eb6d4c211bbfe35ac78775	refs/tags/v2026.267.2049
$ git -C layer-charly-internals show v2026.267.2049:charly.yml | grep -c "### The INCONCLUSIVE (verdict-less) class"
1
```

## Rulebook compliance

- **R0** — loaded `/charly-internals:marketplace`, `/charly-internals:git-workflow`.
- **R1** — the corpus is regenerated from the pinned source, not hand-edited; the pin points at a real merged tag.
- **R2** — the pin bump and its regenerated corpus land together (the drift gate's own requirement); nothing deferred.
- **R3** — one generator, one projection; no hand-editing a generated SKILL.md.
- **R4** — no workaround; the pin advances to the merged source tag.
- **R5** — no identifier removed; no stale pin left.
- **R6** — `fix/skill-validator-inconclusive` off `origin/main`; no force-push.
- **R7 / R7a** — regeneration is a clean 2-file diff and the projection carries the source section.
- **R8 / R9** — this IS the generated corpus artifact, regenerated from its source.
- **R10** — N/A (generated-docs/corpus regeneration; the gate is the clean-regeneration diff shown above, which is the marketplace's own drift contract).

## Change classification

- **Change class:** marketplace corpus regeneration + its refs-list pin bump.
- **Verification gate:** clean 2-file regeneration diff + the projected section present + the pinned tag carrying the source section.
- **Attribution tier:** `fully tested and validated`.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (fully tested and validated)*