# 2026.231.2146 — batch-2.1 charly-internals prose fixes

Batch 2.1 of the charly-internals prose sweep, projected from
`candy/charly-internals/charly.yml` by `charly marketplace generate`:

- `#294` — the CHANGELOG H1 byte-equality rule is scoped to the WHOLE line, and
  the final-status self-check reads the INDEX (`git show :CHANGELOG/<file>.md |
  head -1`) rather than the working-tree path, so a staged rename with an
  unstaged edit cannot pass a check whose only agreement layer is the working
  tree.
- `#295` — the evidence-and-freshness provenance clause names the content-scoping
  rule explicitly instead of pointing "two paragraphs up".
- `#302` — a symptom-row entry for "write a guard to enforce a claim" (the guard
  reports clean on the case it was meant to catch), plus a page-wide
  NONZERO-BEFORE detector paragraph at the end of the evidence skill.
- `#317` — B6a step 2 notes that superproject-facing drift is EXPECTED while a
  projection PR is in flight; its gate is the `plugins` tree, and the
  superproject catches up at step 5.
- `#339` — the libvirt-renderer / vm-spec / source-map tables mark
  `sdk/vmshared/libvirt_yaml.go` as the stub it is (the per-device structs moved
  to `#LibvirtDomain` in `spec/schema/vm.cue`) and correct the LOC figures for
  `libvirt_yaml_bridge.go` (~1800) and `libvirt_yaml_listen.go` (~24).

Regenerated from the candy source; never hand-edited.
