# 2026.244.1920 — chore: bump layer-charly-distros to v2026.244.1915 and regenerate the corpus (omarchy-cstream skill)

## Summary

Bumps the `layer-charly-distros` refs pin to v2026.244.1915 (the tag from layer-charly-distros#4, which added the `omarchy-cstream` skill) and regenerates the corpus. The regeneration emits the new `distros/skills/omarchy-cstream/SKILL.md` — the streamed Omarchy desktop (Hyprland + Quickshell over HTTPS).

This resolves the cross-reference `/charly-distros:omarchy-cstream` in the `omarchy` skill, which the docs generator's link gate rejects — the docs PR (opencharly/docs) cannot regenerate until this lands.

## How tested

- `charly marketplace generate --root /tmp/marketplace --out /tmp/marketplace` — regenerated the corpus
- `git status --porcelain` — exactly 2 changes: the pin bump + the new `distros/skills/omarchy-cstream/SKILL.md` (GENERATED banner, name=omarchy-cstream)
- The drift gate (deploy.yml) will regenerate and compare — a clean tree must be a no-op

## Rulebook compliance

- R3: no duplication — the skill entity lives in layer-charly-distros (the corpus owner); the corpus copy is generated
- R6: git safety — feat/ branch, no force-push, no direct main push
- PR-only landing per the org git-workflow; the refs bump + regenerated corpus ship in ONE PR (the drift gate requires it)

## Change classification

- Change class: docs/skill corpus (generated) + config (refs pin bump)
- Verification gate: regeneration ran live + layer manifest `charly box validate` PASS (layer-charly-distros#4) + the marketplace drift gate
- Attribution tier: analysed on a live system

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*