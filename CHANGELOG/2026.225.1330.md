# 2026.225.1330 — align the second port-allocation occurrence with the auto-allocation doctrine

Regenerated from the superproject's `candy/charly-internals/charly.yml` manifest edit (the `worktree-only-model` cutover's follow-up) via `charly marketplace generate`. The `parallel-bed-testing.md` guidance still told the author to assign disjoint host ports by hand while the same file's auto-allocation section (and the B3 doctrine in `multi-repo-coordination.md`) mandates port auto-allocation. One file changed:

- `internals/skills/agents/references/parallel-bed-testing.md` — the second port-allocation occurrence now states the doctrine: host-port disjointness is not statically guaranteed, so every bed uses port auto-allocation — never a hardcoded host port; manual port-picking is forbidden.

This is the `plugins` leg of the `worktree-only-model` cutover's port-contradiction follow-up (plugins → docs → superproject). Regeneration is a no-op on a clean tree. Placeholder CalVer — the pr-validator finalizes it at merge.
