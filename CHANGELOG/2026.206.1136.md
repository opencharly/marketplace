# 2026.206.1136 — plugins — R5 sweep: materialize is no longer a permanent in-core M-mechanism

CalVer finalized at merge by the fresh `pr-validator`.

## What changed

K1 unit 1 (the paired charly/sdk code cutover, already landed) moved the
kind-decode MATERIALIZE fold/not-found DISPATCH POLICY out of charly core and
into `candy/plugin-loader`'s `Materializer` seam (`MaterializeNode`
implementing `spec.Materializer`) — the ACTUAL registry resolve + provider
dispatch stays host-side (clause M, unchanged), but the not-found POLICY that
used to live in-core is now a plugin capability. This is the plugins leg of
the K1 keystone's 3-repo landing (task #24): `sdk` (`sdk#103`, merged as
`v0.2026206.1046`) → **this leg** → `charly` (`charly#189`, in flight — the
loader is enveloping out via the same keystone, `LoadUnified`/`ApplyDiscover`
relocating to `sdk/loaderkit`).

Two surfaces described materialize as one of FOUR permanent in-core
M-mechanisms — a documentation-divergence R1 incident once the code moved —
so this cutover sweeps both:

- **`internals/skills/plugin/SKILL.md`** — the E/M/B/D/R kernel/plugin
  boundary-law definition, the decision procedure, the defines-vs-calls
  test's own canonical illustration (previously cited `materialize.go` as the
  "where the mechanism is DEFINED" example — now corrected to
  `provider_kind_invoke.go`, plus documents `materialize.go`'s own former
  self-misclassification as the test's worked example), the practical audit
  framing, the "whole subsystems" paragraph, the target-kernel job list, the
  target-kernel LOC table, and the K1-proper migration-ledger row (previously
  described the move as NOT YET DONE — now reflects landed state).
- **`internals/agents/pr-validator.md`** — the fresh validator's OWN
  architecture-gate placement test (2 occurrences) — so a future PR moving
  kind-decode dispatch is not incorrectly held to a stale 4-mechanism bar.

Post-sweep the in-core M set is `{plugin loading, prescan-dispatch, wire
broker}` — three, not four.

Documentation-only change, zero behavior change. R5 grep self-test clean
(`git grep -i materialize` across every rule/skill surface in this repo now
describes materialize as a moved plugin capability, never permanent in-core
M — CHANGELOG/ historical entries excepted, R5).

Assisted-by: Claude Code Anthropic Claude Sonnet 5 (documentation reviewed)
