# 2026.231.0243 — project the libvirt-renderer row charly#315 left behind

`charly#315` (`c8ba494f`) added a size column to the `spec/schema/vm.cue` row in
`candy/charly-internals/charly.yml`, and its projection never landed here. `task skills:drift`
red on `internals/skills/libvirt-renderer/SKILL.md` the moment a superproject branch merged
that source in.

This is `charly#318`'s class — a candy skill edit landing without its projection — and the
direction was measured before acting, because the opposite case looks identical at the gate:
regenerating this page ADDS `| ~720 |`, so the SOURCE is ahead and regenerating is the fix. Had
it REMOVED text, the projection would have been ahead of its source and regenerating would have
destroyed landed content.

Regenerated by `charly marketplace generate`; never hand-edited.
