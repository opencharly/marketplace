# 2026.243.1425 — chore: refresh the wl skill in the corpus

## Summary
    plugin-wl  v2026.243.0521 -> v2026.243.1401

The corpus generates from this authored refs list, so a skill edited in its owning repo reaches
NO reader until the ref here moves. Two changes to the wl skill are unpublished:

    plugin-wl#10  artifact_contains_text - the OCR modifier - in the schema
    plugin-wl#11  the artifact validators documented as a ladder, the OCR host dependency,
                  and what wl exec does NOT do

**Correction to an earlier revision of this body.** It listed a third change, plugin-wl#8 (the
Compositor Compatibility Hyprland column), and claimed "none of it is in the published corpus".
That was wrong, and the block that caught it was right. Measured against the OLD pin
`v2026.243.0521`:

```
$ git show v2026.243.0521:candy/plugin-wl/charly.yml | grep -c ...
  Hyprland column (#8)          2   <- ALREADY published
  artifact_contains_text (#10)  0
  exec semantics (#11)          0
```

`#8` shipped in the old pin already. The regeneration is complete — the diff reflects exactly
the two changes that were genuinely missing, which is why the Compositor Compatibility table is
untouched.

The last one matters most for a reader. The skill described exec as "launch application in
container" without either property that decides whether it is the right verb: it returns before
the command has done anything, and it discards the exit status. A bed used it for a bar toggle
on that basis; the toggle silently did not land and the failure surfaced two steps later against
unrelated state.

## How tested

    charly box validate
    exit 0

Verified the pinned tag actually carries the skill text rather than assuming the tag name:

    git show v2026.243.1401:candy/plugin-wl/charly.yml
      artifact_contains_text   3 occurrences
      does not run-and-wait    1
      hypr-layers              8

```
$ charly box validate
charly box validate: OK — checked 1 candy, 7 distros, 7 builders; 0 warnings, 0 errors
[exit 0]
```

### The drift gate caught the missing half, correctly

The first commit here bumped the ref and stopped. CI's drift gate failed it:

```
DRIFT: the committed corpus does not match this repo's refs list.
Regenerate and commit the corpus in the same marketplace PR (this workflow is the gate).
```

That gate is doing real work. The generated corpus is COMMITTED in this repo, so a ref bump
without regeneration leaves the published skill text exactly as stale as before the bump —
`validate` passes, the pin looks advanced, and no reader sees a word of the update. Precisely the
half-done shape this whole session kept running into.

Regenerated with the command the workflow itself runs, and exactly one file moved — the skill
whose source changed:

```
$ charly marketplace generate --root . --out .
[exit 0]
$ git status --porcelain
 M check/skills/wl/SKILL.md
```

The regenerated corpus carries the three sections the bump was for (`artifact_contains_text` ×3,
the exec note, the validators section), and re-running generation on the committed tree is now a
no-op — which is exactly what the gate asserts.

## Rulebook compliance

- **R1 — RCA every anomaly.** None surfaced. The pinned tag was verified to CARRY the skill text
  rather than trusted by name — a check that caught two false readings earlier in this session,
  where zsh's `:c` parameter modifier silently ate the colon in `$TAG:path` and made a correct
  tag look empty.
- **R2 — Nothing parked.** This closes the loop the skill edit opened: a skill that does not
  reach the corpus is not updated in practice, so the refresh ships with the work rather than
  waiting for a later sweep. The drift the gate found was fixed in this PR, not deferred.
- **R3 — No duplication.** One ref advanced in the single authored list the corpus generates
  from; the skill content itself lives once, in the plugin that serves the verb.
- **R4 — No workarounds.** No hand-edited corpus output — the generator's input moved.
- **R6 — Git safety.** Branched from a fresh `origin/main`; no force-push, no hook bypass, no
  direct push to main.
- **R7 — Prove the gate, not the plan.** `charly box validate` output pasted verbatim, with its
  own zero-warning line.

## Change classification

| | |
|---|---|
| **Change class** | corpus ref advance — one `@github` pin in the authored refs list |
| **Verification gate** | `charly box validate` (0 warnings, 0 errors), plus reading the pinned tag's own manifest to confirm it carries the three skill changes claimed |
| **Attribution tier** | `analysed on a live system` — validate passed here and the pinned tag's content was read back live; the behaviour the refreshed skill documents was exercised on a `disposable: true` bed earlier in this session, but this change itself is a pin with no runtime path of its own |

*Assisted-by: Claude Code claude-opus-4-6-20260401 (analysed on a live system)*