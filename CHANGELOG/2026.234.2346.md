# 2026.234.2346 — fix(ci): tag-on-merge dispatcher — push-to-main trigger (replace dead workflow_run + cron shape)

## Summary

The tag-on-merge dispatcher in this repo (workflow_run + `*/15` cron — the shape replaced in opencharly/opencharly after its own 15× `startup_failure` sequence, see the umbrella CHANGELOG/2026.234.2134) is dead at startup: measured 15+ consecutive `startup_failure` runs across charly/plugins/docs since 2026-08-22T16:20Z, leaving every merge since then UNTAGGED (charly #375/#376/#377 and plugins #223/#224/#225 were backfilled add-only per the rulebook; this fix prevents recurrence). Adopted verbatim is the umbrella's proven event-driven dispatcher:

- `on: push` to `main` (the event that IS the squash-merge; ~21s latency, proven) + `workflow_run` on `charly/pr-validator` with a bounded 5-min wait-for-merge probe
- no cron (GitHub cron delivery measured 21–44 min gaps)
- the org reusable sweep stays idempotent; the rulebook's add-only backfill remains the ultimate path

## How tested

- The umbrella has run this exact file in production since 2026.234.2134: `push`-triggered runs land ~21s after the merge push; its `workflow_run` runs succeeded (e.g. run 32604850236-adjacent schedule runs, 23:05:26Z success tagging opencharly/opencharly#10 at v2026.234.2305)
- `gh run list` before the fix: charly/plugins/docs tag-on-merge 100% `startup_failure` (12+ consecutive); after merge, the next merge push will fire the fixed trigger
- YAML validated against the working umbrella copy (byte-identical)

## Rulebook compliance

| Rule | How / N/A |
|---|---|
| R0 | git-workflow loaded |
| R1 | RCA in the umbrella's CHANGELOG/2026.234.2134 (measured 15× startup_failure of the workflow_run design; cron best-effort gaps 21–44 min; push is the deterministic event) — this PR propagates the proven fix |
| R2 | one file, one PR per repo (charly/plugins/docs carry the same dispatcher — three companion PRs) |
| R3 | verbatim adoption of the canonical dispatcher — no fork |
| R4 | no workarounds — the event-driven design + the rulebook backfill path |
| R5 | the old dispatcher shape is fully replaced (no cron, no dead trigger remains) |
| R7 | production evidence of the exact file pasted above; org reusable sweep idempotent |
| R10 | N/A — CI-workflow-only change; the gate is the next merge's tag landing (validated by the org sweep + the rulebook backfill) |

## Change classification

- **Change class**: CI workflow fix (active-gate breaker — no tags since 16:20Z)
- **Verification gate**: production-run evidence of the identical file (umbrella) + next-merge tag landing
- **Attribution tier**: analysed on a live system — the identical dispatcher runs live in the umbrella with retained run output; this repo's next merge is the direct proof
- **Landing**: fresh `charly/pr-validator`; tag-on-merge

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*