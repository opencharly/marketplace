# 2026.265.1604 — chore(refs): pin distro-arch as a corpus source

## Summary of changes

Add `@github.com/opencharly/distro-arch:v2026.265.1557` to the refs list
(`candy/charly-marketplace/charly.yml`). This is the corpus-source half of the
skill-ownership wave: `opencharly/distro-arch` now owns the seven arch-family
`skill:` entities (moved there by `opencharly/distro-arch#39`, tag
`v2026.265.1557`), so the generator must be able to resolve those entities from a
pinned source.

The pin is deliberately inert until the companion deletion PR lands: the arch
skills are still ALSO defined in the pinned `layer-charly-distros`, so the
generated corpus is unchanged by this pin (proven below). The companion
`layer-charly-distros` PR removes the duplicates, at which point this pin is the
single source.

## How tested

Regeneration is a no-op on this tree. Generated the full corpus from this refs
list and compared byte-for-byte with the committed corpus:

```
$ charly marketplace generate --root . --out /tmp/opencode/skills-migration/mkt-pin
# generator log confirms the new pin resolved:
#   Downloading github.com/opencharly/distro-arch:v2026.265.1557...
# 427 generated artifacts

# full-tree hash comparison (generated vs committed), excluding hand-authored files:
generated: 427   committed: 758
only in generated (NEW artifacts): 0
content DIFFERS: 0
```

The 331 files present only in the committed tree are all hand-authored (the
documented set: `README.md`, `CLAUDE.md`, `LICENSE`, `.gitignore`, `charly.yml`,
`kimi-user-config.toml`, `scripts/*`, the per-family `README.md` files, and
`CHANGELOG/*`), which carry no DO-NOT-EDIT banner and sit outside the generated
paths.

## Wave / landing order

1. `opencharly/distro-arch#39` — MERGED, tagged `v2026.265.1557` (owns the seven entities).
2. **this PR** — pins the source; regeneration is a no-op.
3. `opencharly/layer-charly-distros` — drops the seven now-duplicated entities (the activation step).

## Rulebook compliance

- **R1** — regeneration divergence RCA'd as a no-op (0 differing, 0 new artifacts).
- **R2** — the wave is scheduled as one coherent cutover (steps 1–3).
- **R3** — no duplication introduced; the pin is the corpus-source path.
- **R4** — no sleeps/retries/workarounds; the real generator is the gate.
- **R5** — the companion deletion (step 3) removes the duplicate entities.
- **R6** — dedicated checkout, PR-only, no force-push.
- **R7** — `charly marketplace generate` ran on this refs list; no-op proven.
- **R8/R9** — no labels/plans/schemas; no binary built here.
- **R10** — config change class (refs list); the generator (its only consumer)
  ran live with retained output; regeneration is byte-identical.

## Change classification

Config (refs-list pin) → `analysed on a live system`. Proof: real generator run,
0 new artifacts, 0 content differences against the committed corpus.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (analysed on a live system)*
