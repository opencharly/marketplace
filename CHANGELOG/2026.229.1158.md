# 2026.229.1158 — regenerated: six findings codified into their owning skills

Placeholder CalVer (the pr-validator finalizes it at merge). Chosen after `2026.229.0947`,
with no filename or tag collision.

Generated output; nothing here was hand-edited. Three candy sources gained content and these
are their projections. See the superproject entry for this CalVer for what changed and why.

The three skills:

- **skills** — "How to Update" no longer opens by telling the reader to edit
  `plugins/<plugin>/skills/<name>/SKILL.md`, which is a generated file carrying a
  DO-NOT-EDIT banner; that instruction manufactured the very error it should prevent. Plus a
  new section on regenerating only the projections of the sources you edited, because a
  projection merges before the source that pins it and a wholesale regeneration reverts
  landed prose (measured: ~500 lines across 21 + 41 files).
- **check / authoring-gotchas** — gotcha 10 extended to `vm:` beds, to the fact that
  `sudo` does not cover a shell redirect, and to where `sudo` comes from per substrate;
  new gotcha 13 (a step is SIGKILLed at the 90s never-hang floor unless it declares
  `timeout:`) and new gotcha 14 (a check must be able to fail for the reason it names —
  four measured ways one passed while measuring nothing).
- **build / generate** — an unrenderable package install is a hard error rather than a
  dropped step, with the two authoring consequences that produced the original defect.

## Scope note

Only projections whose full regenerated output is revert-free are included. A wholesale
regeneration also wanted to delete landed prose whose candy source is still in flight — see
the superproject entry; `vm`'s projection is deferred for exactly that reason, so the
firmware-matrix correction lives in the candy source until a tree with every source
regenerates it.
