# 2026.260.0021 — chore: bump the eval-omarchy ref to v2026.260.0013 (carries the corpus regen)

chore: bump the eval-omarchy ref to v2026.260.0013 (carries the corpus regen)

Advances the refs list's eval-omarchy pin v2026.259.1834 -> v2026.260.0013, the
merge of opencharly/eval-omarchy#80.

NOT a byte-no-op this time: #80 changed a `skill:` entity's content — the entry
omarchy-eval skill's applicability map gained the row for the new vision
judgement — so the regenerated corpus carries exactly that one line:

  distros/skills/omarchy-eval/SKILL.md
  + | "the change works as a user would see it" | graded — `screen-vision-judged`
    asks a vision model whether the captured live-VM screenshot shows a rendered
    desktop (a panel + wallpaper), not merely that a non-uniform PNG exists; a
    visual PR cannot pass on a broken render | `@gate.eval_steps` entry
    `screen-vision-judged` |

(That is why the ref and the corpus land together: advancing the ref without the
regen would trip the deploy workflow's fail-closed drift gate.)

Verification:

  $ CHARLY_PLUGIN_ONLY=1 charly marketplace generate --root . --out .
  GEN_EXIT=0
  $ git status --porcelain
   M candy/charly-marketplace/charly.yml
   M distros/skills/omarchy-eval/SKILL.md
  (exactly the ref line + the one skill line; no untracked artifacts)

Rulebook compliance: R1 (the drift is proven by regenerating and diffing, not
asserted); R3 (the refs list is the one generation input); R5 (no stale corpus
left); the deploy workflow's drift gate is the authority and is reproduced above.

Change classification: pin/refs + regenerated corpus. Verification gate: the
marketplace deploy workflow's drift gate, reproduced locally. Attribution: fully
tested and validated.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (fully tested and validated)*