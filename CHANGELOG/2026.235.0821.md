# 2026.235.0821 — chore(corpus): docs de-submodule cutover — the docs/docs-site skills teach the standalone flow

## Summary

Regenerated skill corpus for the docs de-submodule cutover (the source `skill:` entities were edited in the paired charly PR's candy configs; this repo carries the projection, regenerated by `charly marketplace generate`):

- `build/skills/docs` + `tools/skills/docs-site` now teach the standalone flow: the opencharly/docs repo owns generation, the content-drift gate, the Astro build and the publish in its own `deploy.yml` workflow, running `charly docs generate` against the charly commit its `.gitmodules` pins; regeneration is a docs PR that bumps that pin. All `docs/`-submodule / `task docs:sync` / `task docs:drift` references are gone.
- `git-workflow` B-references: the docs leg of a multi-repo landing is now a docs PR (charly-pin bump + regenerated pages) gated by the docs repo's deploy workflow; the docs-site `DOCS_REF` re-pin task now compares against the docs repo's live main head (charly carries no docs gitlink anymore).
- `skills` corpus: the regen step ends in "a docs PR carrying the regenerated pages".
- `pr-validator` lessons: the historical `docs:drift` evidence examples reworded to the new docs drift gate.
- `parallel-bed-testing.md`: the sdk de-submodule debt liquidation — the entity already carried the proxy-resolved sdk/spec text; this regen projects it (its drift-baseline entry is deleted in the charly cutover PR).

## How tested

**Regeneration — executed on the charly cutover worktree with the candies' edited entities (pasted):**

```
$ bin/charly marketplace generate --root .
  plugins/build/skills/docs/SKILL.md
  plugins/internals/agents/pr-validator.md
  plugins/internals/skills/agents/references/parallel-bed-testing.md
  plugins/internals/skills/git-workflow/references/branch-and-pr-loop.md
  plugins/internals/skills/git-workflow/references/multi-repo-coordination.md
  plugins/internals/skills/skills/SKILL.md
  plugins/tools/skills/docs-site/SKILL.md
```

**The gate that goes red on a stale corpus — `charly marketplace drift` (pasted):**

```
$ bin/charly marketplace drift --root .
skills: no drift.
```

(the paired charly PR commits the regenerated surfaces; the drift gate is the no-op check that this projection equals the entity sources)

## Rulebook compliance

| Rule | How / N/A |
|---|---|
| R0 | git-workflow loaded before branching/PR |
| R1 | the `docs:drift`/`docs:sync` identifiers swept corpus-wide; the parallel-bed-testing debt (sdk cutover leftover) liquidated by the same regen |
| R3 | the corpus remains a projection of the candy entities — regenerated, never hand-edited (all 7 files carry the DO-NOT-EDIT banner) |
| R4 | no workarounds — the standalone docs flow is the mechanism |
| R5 | every stale `docs:sync`/`docs:drift`/docs-submodule reference in the corpus updated in this landing |
| R7 | regeneration + drift outputs pasted above |
| R10 | the load-bearing gate is `charly marketplace drift` on the regenerated tree (a no-op proves the projection matches the entities); the docs content staleness gate is the docs repo's deploy workflow — this corpus PR itself is not that gate's input |

## Change classification

- **Change class**: docs-corpus regeneration (`.md` only — A3 documentation class)
- **Verification gate**: `charly marketplace drift` no-op on the regenerated tree
- **Attribution tier**: documentation reviewed — whole-diff markdown; generation ran live with retained output (`marketplace generate` + `marketplace drift`); no runtime path changed
- **Landing**: fresh `charly/pr-validator`; tag-on-merge

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (documentation reviewed)*