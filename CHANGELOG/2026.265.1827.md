# 2026.265.1827 — docs(corpus): regenerate for the org-ruleset skill updates

## Summary

Regenerate the marketplace corpus for the org-ruleset skill updates.

- **`candy/charly-marketplace/charly.yml`** — the refs list pins `layer-charly-internals` at `v2026.265.1741` and `layer-charly-internals-extra` at `v2026.265.1721` (the merged skill-update tags). This is a **pin change**, which is why the attribution tier below is `fully tested and validated`, not `documentation reviewed` (the rulebook forbids `documentation reviewed` when pins change).
- **Regenerated projections (4 files):** `internals/skills/git-workflow/SKILL.md` (+ its `branch-and-pr-loop.md` / `validator-and-calver.md` references) and `internals/agents/pr-validator.md` now describe the **ONE organization ruleset** (carrying both the `workflows` rule naming `org-wide-pr-validator-required.yml` AND the branch rules) instead of the retired per-repo dispatcher stub / `branch-protection.sh`. This round also sweeps the residual POISON-remedy per-repo-dedupe text.

Regeneration touched ONLY the refs bump + the two skills' projections: 5 files, zero untracked output.

## How tested

`charly marketplace generate` against the bumped refs, a second run to prove the no-op, and the projection audit:

```console
$ charly -C . marketplace generate --root . --out .   # exit 0
$ git status --porcelain
 M candy/charly-marketplace/charly.yml
 M internals/agents/pr-validator.md
 M internals/skills/git-workflow/SKILL.md
 M internals/skills/git-workflow/references/branch-and-pr-loop.md
 M internals/skills/git-workflow/references/validator-and-calver.md

# second run — the drift gate's no-op condition (post-commit regeneration is clean):
$ charly -C . marketplace generate --root . --out .
$ git status --porcelain
(empty — the committed corpus is a fixed point of the generator)

# projection audit — the ONE net-deletion is the intended POISON-remedy condensation:
$ git diff --numstat origin/main...HEAD
2	2	candy/charly-marketplace/charly.yml
14	10	internals/agents/pr-validator.md
19	13	internals/skills/git-workflow/SKILL.md
3	3	internals/skills/git-workflow/references/branch-and-pr-loop.md
15	17	internals/skills/git-workflow/references/validator-and-calver.md   # net -2

# untracked regenerated output:
$ git status --porcelain | grep '^??'
(no output)

# stale-claim sweep of the regenerated corpus:
$ grep -rnE "branch-protection\.sh|org-wide-pr-validator-dispatcher|per-repo branch RULESET|14 today|is the required check|which repos carry it" internals/skills/git-workflow/ internals/agents/pr-validator.md
$ echo $?
1
```

The `validator-and-calver.md` **net −2 lines is intended**: it deletes the obsolete per-repo-dedupe POISON remedy (`Add the per-repo concurrency dedupe to that repo's dispatcher`, the `417 of 420 dispatchers did NOT` measurement, and the `Add the dedupe to the org template` step) and replaces it with the two-line statement that the dedupe now lives in the ONE org required workflow.

## Rulebook compliance

- **R0 skills:** `/charly-internals:marketplace` + `/charly-internals:skills` loaded.
- **R1:** the corpus is a projection; the stale claims were swept at the source and re-derived here.
- **R2 complete:** refs bump + regenerated corpus ship together (the drift gate requires it).
- **R3 no duplication:** the corpus is generated, never hand-edited.
- **R4 no workaround:** no hand-edit of a generated file; the source is the fix.
- **R5:** the claim-keyed sweep is executed above.
- **R6 git safety:** branch + PR; no force-push.
- **R7 gate:** the regeneration + no-op + audit outputs pasted above.
- **R8:** `N/A — no generated artifact outside this repo's own corpus`.
- **R9:** `N/A — no binary or image`.
- **R10:** `N/A — no runtime bed; the change is the pinned-ref corpus regeneration + the deploy drift gate proves the committed corpus reproduces` (same class as the merged `chore: refresh the refs list` landings).
- **Architecture gate:** `N/A — no source tree, package, or capability moved`.
- **Change class:** generated-corpus regeneration from a pin change.
- **Attribution tier:** `fully tested and validated` — the regeneration and its no-op are pasted above; this matches the convention of the merged refs-refresh landings (#322/#300).

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (fully tested and validated)*