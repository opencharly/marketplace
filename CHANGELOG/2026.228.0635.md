# 2026.228.0635 — the validation rules, regenerated from their source at last

**This is a REGENERATION, not an authored edit.** All three files carry
`<!-- GENERATED by `charly marketplace generate` — DO NOT EDIT -->` and their source of truth is
`candy/charly-internals/charly.yml` in the `charly` repo.

## `#178`'s prose arrives the right way round

`internals/skills/git-workflow/references/evidence-and-freshness.md` (`+317/-33`) and
`multi-repo-coordination.md` (`+48/-2`) are exactly the changes `plugins#178` authored — **directly
into these generated artifacts.** The next `marketplace generate` would have reverted every line
while its diff looked entirely clean. The prose now lives in the candy and arrives here as output.

The re-homing was verified by **byte-identity in both directions**: regenerating from the corrected
candy reproduces `#178`'s two emitted files exactly, and the insertion/deletion counts above are
`#178`'s own. Reading the prose confirms it is good; only byte-identity confirms it is *the same
prose*.

`plugins#178` is closed in favour of the source-side change, with a comment saying why.

## Three new validator rules

`internals/agents/pr-validator.md` (`+103/-0`):

- **Phase 1 begins by finding generated files** — `git grep -lEi "DO[- ]NOT[- ]EDIT"` over the
  change set, before any content is read. The tolerance is measured, not stylistic: the two trees
  use OPPOSITE separators (`plugins` 335 spaced / 3 hyphenated; `docs` 2 spaced / 897 hyphenated),
  so a matcher for either form alone reports a clean tree for the other. Banner position varies
  too — 8, 7 and 10 observed — so `head -3` misses all three.
- **Read the ref, not the checkout**, including its exit-code costume: a submodule ancestry check
  run from the superproject exits **128**, which is falsy under a bare `if`, so "objects absent"
  reads identically to "not an ancestor". Capture `rc` and classify it.
- **Phase 3 verifies the landed CHANGELOG's CalVer equals the version it MINTED**, which is the only
  check that catches a SKIPPED rename rather than a botched one.

## Scope

**The files this leg carries are exactly those whose candy source this cutover changed.** That is
the invariant; it does not need a count, and a count is what keeps going wrong.

Successive revisions of this entry claimed **twelve**, then **three**, then **zero** other drifting
files — three different numbers for the same question, each correct when taken and each falsified by
the next `update-branch`. Every one of them measured *how far behind this branch was*, not anything
about this repository. **A number that changes when you rebase was never describing the repo**, and
publishing it as though it were is the mistake this entry has now made three times. The invariant
above is stable under every one of those moves.
