# 2026.260.2238 — feat(corpus): publish the jetkvm skill

## Summary of changes

Publishes the **`jetkvm` skill** into the marketplace corpus. The skill entity landed in `layer-charly-check` (tag `v2026.260.1416`), but the refs list still pinned the older `v2026.250.1955`, so the corpus never carried it.

- `candy/charly-marketplace/charly.yml`: bumps the `layer-charly-check` pin to `v2026.260.1416` (the merge tag carrying the skill entity). Only that one pin moves.
- `check/skills/jetkvm/SKILL.md` (NEW, generated): the skill for the `jetkvm:` IP-KVM check/control verb served out-of-process by `candy/plugin-jetkvm` — the authoring shape, the full method catalog split read-only / mutating / never-autonomous, the `allow_control` device-safety gate, credential resolution, the `host`/`insecure` connection contract, and the measured `getVideoState.ready` caveat.
- `DISPATCHER.md` (generated): the R0 dispatcher row routing JetKVM/IP-KVM triggers to `/charly-check:jetkvm`, plus the `check` skill's probe-verb list now naming `jetkvm`.
- `.well-known/skills/index.json` (generated): the new skill's catalog entry.

All three generated files come from `charly marketplace generate` — none is hand-edited.

**Closes / relates to:** follows `opencharly/layer-charly-check#13` (the skill entity). Complements `opencharly/plugin-jetkvm`.

**Breaking change + rollback:** `none` — a corpus addition plus one pin bump. Revert the merge to roll back.

## Change class

Marketplace corpus regeneration — the repo's own deploy workflow is the gate.

## How tested

Regeneration is the gate, and it is reproducible: a second `charly marketplace generate` over the committed tree leaves it unchanged, so the deploy workflow's drift check (`git diff --exit-code` plus a `git status --porcelain` that also catches NEW untracked artifacts) passes on this commit.

```console
$ charly -C . marketplace generate --root . --out .
(regenerates; exit 0)

$ # second run — must be a no-op, which is exactly what the drift gate asserts
$ charly -C . marketplace generate --root . --out .
$ diff -r <saved jetkvm skill> check/skills/jetkvm
(no output)                          # reproducible
$ git status --porcelain
 M .well-known/skills/index.json
 M DISPATCHER.md
 M candy/charly-marketplace/charly.yml
?? check/skills/jetkvm/               # == exactly this PR's change set
```

The generated skill's front-matter and body match the source entity in `layer-charly-check` (`check/skills/jetkvm/SKILL.md` carries the `GENERATED … DO NOT EDIT` banner and the entity's `name`/`description`/`content` verbatim):

```console
$ head -1 check/skills/jetkvm/SKILL.md
---
$ sed -n '2,3p' check/skills/jetkvm/SKILL.md
name: jetkvm
description: |-
  The `jetkvm:` check/control verb (declarative, dispatched to the out-of-process plugin candy/plugin-jetkvm) — drive a JetKVM IP-KVM without a browser over its own WebRTC control plane: screenshots, keyboard/mouse input, ATX/DC power, virtual media, USB gadget, and device configuration. Use before any JetKVM device interaction from a candy/box plan.
```

## Change classification

- **Change class:** generated marketplace corpus (a projection from the `layer-charly-check` skill entity).
- **Verification gate:** `charly marketplace generate` reproducibility — the repo's own deploy-workflow drift gate.
- **Attribution tier:** `fully tested and validated` — the corpus was regenerated from the pinned source and a second run is a verified no-op, so the committed corpus matches the refs list exactly.

---

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (fully tested and validated)*