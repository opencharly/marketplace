# 2026.227.1302 — git-workflow: the evidence-discipline reference

Adds `internals/skills/git-workflow/references/evidence-and-freshness.md` and its
Reference Index row, plus an amendment to `references/branch-and-pr-loop.md`
pointing its superseded per-path forward-gitlink check at the whole-tree
comparison in the new page. Generated from the `git-workflow-skill:` entity in
`candy/charly-internals/charly.yml`; never hand-edited.

**Why it exists.** Every item in it was a blocking finding on a real landing during
the Factory unit-4 cutover (charly#274 plus plugins#168-#171), most of them
more than once. Sixteen validation rounds produced roughly a dozen blocking
findings, and the majority were not defects in the code — they were false or
stale CLAIMS about the code, in PR bodies, pasted gate output, and CHANGELOG
prose. Nothing in CI covers that surface, so it was costing a round each time it
recurred.

What the page carries:

- **A structural split, stated up front.** Five sections are EPISTEMIC — ways a
  claim goes wrong, all one question failing against a different noun (*what
  exactly did I measure, and is it the thing my claim is about?*). Three are
  MECHANISM — how charly and git actually behave. The split is load-bearing: an
  epistemic claim is checked by reasoning, a mechanism claim only by running the
  thing. This page's own first revision shipped a false mechanism claim while its
  epistemic sections were sound, which is the argument for the distinction.
- **Provenance over plausibility.** A paste and its label are two separate
  claims, and only the first is usually checked. The motivating case was a
  truthful drift result attached to the wrong tree, which would have shipped a
  revert recipe inside an immutable CHANGELOG entry.
- **Three independent freshness surfaces** — the head moves, the body moves
  (no SHA, no notification, no status reset), and pasted gate output goes stale
  in place while the prose around it stays accurate.
- **Positive and negative claims decay differently.** A found defect stays true;
  "still unfixed" is falsified by any edit, so it needs a read time.
- **After a fix, sweep for the claims the fix INVALIDATED**, not just the one it
  corrected — R1's claim-keyed sweep turned inward on the document being edited.
- **The merged tree's gate, not the head's**, for any PR that goes `BEHIND`;
  and measure the tree a change PRODUCES when deciding what it can do.
- **A `skill:` source edit and its regeneration are one cutover across two
  repos**, with the two failure directions tabulated.
- **Submodule pointers reverted by a non-conflicting merge**, and the whole-tree
  detection that catches them.
- **Status-absence on a known head MAY be a false negative** for a passing PR —
  stated as may, not is: observed on two of three landings, while the
  pr-validator spec requires a status on the pre-finalization head.

Documentation-only change class. Placeholder CalVer — the pr-validator finalizes
it at merge.

## Authorship provenance

Accounted for BY SHA, because the property I first used to tell the sessions
apart — the author email — does not discriminate: `atrawog@users.noreply.github.com`
appears on commits from both sessions, including this one's.

Three commits follow `f348276`:

| Commit | Diff | Trailer | What it is |
|---|---|---|---|
| `dea6195` | the round-3 recipe fixes | `Claude Code <deepseek-v4-flash:0731-cloud>` (angle brackets) | another session committing THIS session's uncommitted working tree |
| `68a3bd7` | **empty — message only** | `Claude Code deepseek-v4-flash:0731-cloud` | that session correcting its own trailer form, and asserting it authored the round-3 content |
| `9423602` | the first version of this section | `Claude Code claude-opus-5[1m]` | this session recording provenance |

**What follows is this session's TESTIMONY, and is marked as such** — it is not
repo-verifiable, and neither was the claim it supersedes. A reader can check the
diffs, the trailers and the timestamps; nobody can check from the repository which
runtime typed which edit. Recording an unverifiable account is still better than
leaving two contradictory ones, but it is weaker evidence than anything else on this
page and should not be read as more.

**On that basis `68a3bd7`'s authorship assertion is SUPERSEDED and this entry is the
account that stands.** It states the round-3 fixes were authored by the
`deepseek-v4-flash:0731-cloud` runtime. They were not. Every one of those edits —
the `submodule update --init --recursive` line, the `git fetch`, the `PR_HEAD`
assignment, the `MERGE`/`SIDE_A`/`SIDE_B` rewrite, the `head -1` discovery, and
the five prose corrections — was made by Claude Code / `claude-opus-5[1m]` in its
own working tree, and executed and verified there before any of it was committed.
What the other session did was create commit objects from that uncommitted tree,
and then correct the trailer form on its own commit. **Its trailer-FORM correction
was right and is kept; only its authorship claim is superseded.**

**Repo-verifiable facts, for a reader who discounts the testimony entirely:** the
author email changes exactly ONCE, at `dea6195`, and never
changes back: every commit before it carries `atrawog@gmail.com`, every commit
from it onward carries `atrawog@users.noreply.github.com`. That marks a boundary
in TIME and cannot attribute any individual commit to a session.

(Phrased without a count deliberately. An earlier revision said "five commits
before it, four after" — true when written and false the moment it landed, since
the commit asserting it was the fifth. A durable record cannot contain a number
that the act of recording it changes, and after the squash there is no branch
left to count.) `68a3bd7` has an empty diff. `68a3bd7` and
`9423602` are **7 seconds apart** (12:31:56Z and 12:32:03Z), asserting incompatible
accounts of the same content; if those are two sessions, both were pushing to this
branch inside the same ten seconds.

Mixed authorship is a fine thing to record, and this is what it looks like:
content from one runtime, two commit objects from another. The squash carries
`claude-opus-5[1m]` because that is who wrote the page.

**Three hazards worth outliving this PR, and they have distinct triggers.** A session committing another session's
uncommitted working tree can catch it mid-edit and push an incoherent state; it
did not here, but that was timing rather than design.

Second, linked worktrees SHARE `.git/modules/<sub>`, so a submodule HEAD moved in
one worktree moves for every worktree of that repo. Two distinct triggers were
observed, and they should not be merged into one: `git submodule deinit -f --all`
inside a linked worktree clobbers the shared module config (a removal WITHOUT the
deinit left the main checkout clean), and separately a shell cwd reset silently
redirects subsequent commands into a different tree, where an ordinary checkout
does the same damage. No git command warns in either case.

Third — and this one is about the REPAIR rather than the damage — a restore
overshoots as easily as it undershoots. Reverting the main checkout after the
cwd-reset incident also discarded a `go.work.sum` modification that predated this
session entirely. Regenerable by a build, so harmless here, but only a
session-start snapshot distinguishes "restored" from "over-restored", and nobody
takes one by default.
