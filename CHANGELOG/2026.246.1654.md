# 2026.246.1654 — chore: OpenClaw 2.0 skill + refs refresh

## Summary

- **refs:** `pod-charly-automation` `v2026.243.2149` → `v2026.246.1643` (the
  tag carrying the OpenClaw 2.0 updates to the `openclaw-deploy` skill).
- **corpus:** regenerated — `automation/skills/openclaw-deploy/SKILL.md` now
  carries the 2.0 content: the `openai-codex/*` → `openai/*` model-route
  unification, the `/startupz` container probe, and the operator-facing 2.0
  migration notes (OpenProse retirement, dead chrome-extension-relay config,
  `openclaw doctor --fix` migrations).

## How tested

```
$ charly marketplace generate --out=.
exit 0
```

The regeneration is deterministic (a second run produces no diff). The diff is
confined to the refs list and the regenerated skill file.

**The new ref's content is validated by its producer PR:** the
`pod-charly-automation` tag `v2026.246.1643` was created by
opencharly/pod-charly-automation#11, whose `charly box validate` (the repo's CI
gate, run with the repo's pinned charly v2026.238.1242) passed with exit 0 — the
skill content this corpus now carries is the validated content of that tag. The
corpus regeneration consumed that tag live (the Downloading line in the generate
output).

## Rulebook compliance

- **R0 (skills)** - `/charly-internals:marketplace` for the corpus model;
  `/charly-internals:git-workflow` for PR-only landing and tag-on-merge.
- **R1 (RCA)** - the ref advance tracks the pod-charly-automation tag; the corpus
  is regenerated from the source, not hand-edited.
- **R2 (no deferral)** - the corpus ships in the same batch as the skill update.
- **R3 (no duplication)** - the corpus is generated output; the skill source is the
  single source of truth.
- **R4 (no workarounds)** - N/A.
- **R5 (hard cutover)** - the stale skill content is gone.
- **R6 (git safety)** - feature branch, no force-push, no hook bypass.
- **R7 / R10 (prove the gate)** - the regeneration ran on the committed tree; the
  new ref's content is validated by its producer PR's CI gate.
- **R8 (generation)** - the corpus is generated by `charly marketplace generate`.
- **R9 (artifact integrity)** - N/A: no image is built here.
- **ADE (candy description/plan)** - N/A.
- **SDD (CUE source)** - N/A.

## Change classification

- **Change class:** refs refresh + regenerated corpus. Config/corpus change (the
  refs list is config; the skill file is generated output).
- **Verification gate:** `charly marketplace generate` exit 0 + the producer PR's
  `charly box validate` (opencharly/pod-charly-automation#11, exit 0)
- **Attribution tier:** `analysed on a live system` - the regeneration ran on a
  real checkout with the exit code recorded; the new ref's content is validated by
  its producer PR's CI gate

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*