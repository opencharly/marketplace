# 2026.230.9999 — project two corrected symbol attributions from charly#297

The projection half of `opencharly/charly#297`. Two generated artifacts, regenerated from that
PR's corrected candy sources:

- `build/skills/generate/SKILL.md` — the cache-mount sentence said `charly tasks.go` picks the
  right set based on task context. `charly/tasks.go` is 64 lines and one function and does no such
  thing; `TaskCacheMounts` is `sdk/deploykit/tasks_emit.go:203`. The same page's "Build-time plugin execution" paragraph is
  restructured by the same source change.
- `internals/skills/install-plan/references/step-kinds.md` — a line attributed `EmitTasks` to
  `tasks.go`. The symbol is real, the file is not: `func (g *Generator) EmitTasks` is
  `sdk/deploykit/tasks_render.go:27`, with its `case "plugin":` arm at `:136`. `charly/tasks.go`
  has zero of either, and `charly/import_purity_test.go` forbids `charly/` importing the sdk at
  all — so that attribution was not merely stale but architecturally impossible.

## Why only two artifacts

A full regeneration from `charly#297`'s tree rewrites **ten**. Eight are not this cutover's and
were reverted by explicit pathspec:

```
DESTRUCTIVE — landed projections whose source PR (charly#289) is still open
  internals/agents/pr-validator.md                      +9   -341
  internals/.../references/evidence-and-freshness.md    +72  -327
  internals/.../references/multi-repo-coordination.md   +2    -48

OTHER PENDING — someone else's source edits, not ours
  internals/skills/cutover-policy/SKILL.md · internals/skills/git-workflow/SKILL.md
  .../validator-and-calver.md · internals/skills/plugin/SKILL.md
  ollama/skills/ollama-layer/SKILL.md
```

Emitting the first three from a tree lacking `charly#289`'s source deletes 710 lines of landed
prose — silently, exit 0, no conflict.

**The two kept were selected by testing each artifact's regenerated diff for one of this
cutover's corrected strings**, not by removal size. A size heuristic would have swept in
`ollama-layer/SKILL.md` (+10/−14), which belongs to the ollama chain. That is the difference
between a proxy and a discriminator.

Related: `charly#318` (no gate runs this check anywhere automatic), `charly#317` (the rule
forbidding a tag on a red-gate tree is unsatisfiable for this class by construction).
