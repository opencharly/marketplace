# 2026.230.9999 — /charly-build:load answers the questions a cold reader actually asks

<!-- PLACEHOLDER CalVer: the pr-validator regenerates this stamp at merge. -->

The `plugins#189` validator performed the cold-reader role itself after its sub-agents
never returned — and said so, rather than letting a missing reader read as a clean one.
Its findings against the freshly-landed `/charly-build:load` page were real: the page
named a default socket without stating it, described composing a candy without showing
it, named `check-boxload-pod` with no indication of where that lives, and claimed a
verified transfer without showing what one looks like.

Fixed at the CANDY SOURCE (`candy/charly-build/charly.yml` in the superproject) and
regenerated — never hand-edited here. The page now states the default socket path
(`/run/user/1000/podman/podman.sock`), carries a composable YAML snippet, locates the bed
as a `disposable: true` deploy in the project's own `charly.yml`, and shows the real
output that distinguishes a transfer from an idempotency skip:

```
box load: streaming …:latest into venue storage (save | load)...
box load: …:latest is now in venue storage (verified intact)
```
```
box load: venue already has …:latest (verified intact) — skipping transfer
```

That distinction is the one a reader most needs and the one a bare exit code hides.

## Also: three claims re-projected after `charly#315` fixed them at source

Landed here after the first draft of this entry was written, which is why that draft
described only the load page — half the change would otherwise have entered history
undescribed:

- `internals/skills/vm-deploy-target/SKILL.md` named `podmanCmd(rootless)`, a helper
  `charly#315` DELETES. The replacement is `deploykit.ImageVenue.PodmanCmd`, the single
  field the load, the integrity probe and the tag all read, so they cannot disagree about
  which store they address. A reader following the old name finds nothing.
- `vm/skills/vm/SKILL.md` described `charly vm cp-box` as `podman save | scp | podman
  load`. It has never used `scp`; it streams over ssh with no intermediate tarball, which
  is the point — the guest `/tmp` tmpfs cannot hold a multi-GB image.
- That same row was **unreadable regardless of its wording**: the pipe was unescaped, and
  backticks do not protect pipes in GFM tables. The row yielded 4 cells against a
  3-column header, rendering a dangling `` `podman save `` and dropping everything after
  the pipe. So the correction above was invisible on the published page until the pipe
  was escaped. Verified by rendering through GitHub's `/markdown` endpoint in `gfm` mode,
  with the correctly-escaped row two lines above as the control: 3 header cells, 6 data
  cells, and the corrected text present in the output.

Scope note: a full regeneration touches **26** files here, because several other legs'
sources are mid-flight in both directions — some ahead in `charly` main, some behind. An
earlier draft said 35; that figure was measured before this branch caught up to
`plugins` main, and 9 of those windows CLOSED when their owning legs landed. A drift count
is a property of the base, not of this PR, so it is re-measured at every catch-up.

**Three** files are this leg's and are staged — `build/skills/load/SKILL.md`,
`internals/skills/vm-deploy-target/SKILL.md`, `vm/skills/vm/SKILL.md`. An earlier draft
said "only that file is staged", true when written and false once the re-projections
landed. Every other path the regeneration touches is left exactly as `plugins` main has
it.
