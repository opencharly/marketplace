# 2026.235.1142 — feat: standalone marketplace — charly submodule, deploy-driven generation, Pages catalog mirror

## Summary

The **marketplace de-submodule cutover's marketplace leg** (charly#382 landed the generator + harness side): this repo — renamed from `plugins` — is now the **standalone marketplace**. It pins charly in its own `.gitmodules`; the `deploy.yml` workflow is the SOLE owner of generation — recursive checkout, build charly from the pin, `charly marketplace generate --root ./charly --out .`, a **fail-closed drift gate**, catalog sanity checks, and a **GitHub Pages** catalog mirror (the kimi marketplace JSON included, since Kimi Code consumes a hosted URL).

- **Corpus regenerated at the new generator shape**: per-family manifests carry **no version field** (commit-SHA versioning per the Claude Code marketplace docs — updates flow with every corpus commit), repository metadata points here, and the new root catalogs ship: `.agents/plugins/marketplace.json` (Codex/AGENTS, `INSTALLED_BY_DEFAULT`), `kimi.plugin.json` (the whole marketplace is one Kimi plugin), `package.json` (the whole marketplace is one pi package).
- The generated `setup` launcher ships at the root (executable, generator-emitted): its content is
```
#!/usr/bin/env bash
# GENERATED by charly marketplace generate — DO NOT EDIT.
# Regenerate this marketplace from the charly checkout its .gitmodules pins:
#   ./setup            regenerate (charly marketplace generate --root ./charly --out .)
#   ./setup --check   fail-closed drift gate (regeneration must be a no-op)
set -euo pipefail
cd "$(dirname "$0")"
for arg in "$@"; do
    if [ "$arg" = "--check" ] || [ "$arg" = "--dry-run" ]; then
        exec charly marketplace drift --root ./charly --out .
    fi
done
exec charly marketplace generate --root ./charly --out .
```
  (a launcher-cwd refinement — the `charly` prescan resolves from the charly checkout — routes charly-side with the next pin bump; the README keeps the charly-binary command as the local contract)
- README/CLAUDE reworked for the marketplace role with per-harness install instructions (Claude Code `/plugin marketplace add opencharly/marketplace`, Codex by GitHub source, Kimi `/plugins marketplace <url>` or GitHub-URL install, pi `git:` package).
- `kimi-user-config.toml` guards no longer probe a `plugins/` dir.
- `.gitmodules` pins charly at the cutover main `fa254fd5` (charly#382); the deploy workflow checks it out recursively.

## How tested

**Regeneration at the pinned charlyly is a no-op on the committed corpus (pasted):**

```
$ cd /tmp/mp-marketplace/charly && /tmp/mp-marketplace/bin/charly marketplace generate --root /tmp/mp-marketplace/charly --out /tmp/mp-marketplace
marketplace generate: wrote 406 artifact(s) across 26 family(ies)
$ git -C /tmp/mp-marketplace status --porcelain | wc -l
2
$ git -C /tmp/mp-marketplace status --porcelain | awk '{print $1}' | sort | uniq -c
      1 A      # the charly submodule gitlink
      1 M      # .gitignore
(no corpus files changed — the committed corpus is a no-op regeneration at the pinned charlyly)
```

2. **Catalog shape (pasted):**

```
$ python3 -c 'import json; m=json.load(open(".claude-plugin/marketplace.json")); print(m["name"], len(m["plugins"]))'
charly-plugins 26
$ python3 -c 'import json; m=json.load(open(".agents/plugins/marketplace.json")); print(all(p["policy"]["installation"]=="INSTALLED_BY_DEFAULT" for p in m["plugins"]))'
True
$ python3 -c 'import json; m=json.load(open("kimi.plugin.json")); print(len(m["skills"]), "family skills dirs")'
26 family skill dirs
$ python3 -c 'import json; m=json.load(open("package.json")); print(m["keywords"], m["pi"])'
['pi-package'] {'skills': ['./*/skills']}
```

3. **The PR's own `marketplace` deploy check is the live gate — GREEN at head 067c8c3** (run 32635911891): the pipeline ran end-to-end — recursive checkout at the pin → build → generate (406 artifacts/26 families) → **drift gate: "corpus: committed tree matches the pinned charlyly (regeneration is a no-op)"** → catalog sanity → Pages assemble + upload → Pages deploy. The Pages environment's branch policy (which silently rejected PR deployments) was reset, and the rerun's deploy job succeeded (3 steps).

## Rulebook compliance

| Rule | How / N/A |
|---|---|
| R0 | git-workflow + docs skills loaded before editing |
| R1 | the validator surfaced a real defect: the drift gate's `git diff --exit-code` did not catch UNTRACKED generated files (a generator emitting a NEW artifact would recreate it as untracked and the gate would pass stale) — RCA'd + fixed in-tree: the gate now runs `git diff --exit-code` **plus** `git status --porcelain` (commit 067c8c3) |
| R2 | one surfaced issue routes to a named batch: the stale `git -C /abs/path/plugins …` example in the GENERATED `internals/skills/agents/references/hooks-and-lifecycle.md` cannot be edited in-tree (the drift gate would fail) — the fix is charly-side (candy/charly-internals' skill source) and is picked up by the successor charly PR that bumps MARKETPLACE_REF (same for the `./setup` launcher cwd, which is generator-emitted) |
| R3 | the generator (charly candy/plugin-marketplace) remains the single canonical corpus owner; this repo commits only its projection |
| R4 | no workarounds — the drift gate is the policy's own operation |
| R5 | the old `plugins/`-era references reworked (README/CLAUDE/kimi config); the corpus regenerated at the cutover shape |
| R6 | git status checked; no force-push; feat-branch + PR landing |
| R7 | the regeneration no-op proof above + the PR's deploy check (the workflow is the gate) |
| R8 | the emitted artifacts verified live (the generate run above + the catalog sanity pasted) |
| R9 | N/A — the charly binary is built by the deploy workflow from the pinned checkout (no binary in this repo) |
| R10 | the fresh-checkout regeneration at the pinned charly is the R10 for this repo (the deploy does the same on every run) |

## Change classification

- **Change class**: cutover landing (standalone marketplace repo)
- **Verification gate**: the `marketplace` deploy workflow (build → generate → drift gate) on this PR + the regeneration no-op proof pasted above
- **Attribution tier**: analysed on a live system — the regeneration + catalog sanity ran live with retained output; the deploy gate's full run is the PR's own CI
- **Landing**: fresh `charly/pr-validator`; tag-on-merge

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*