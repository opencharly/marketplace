# 2026.227.1715 — evidence page: the fixture could not be rerun, and the table undercounted itself

Regenerated from the superproject's candy `skill:` entities by `charly marketplace generate`. Follow-up to `#175` (`ab1f93d9`), landing two defects that review found in the page that PR shipped. One page changes: **`internals/skills/git-workflow/references/evidence-and-freshness.md`**.

## The fixture did not run

`#175` added a fixture to the evidence block, introduced as "the fixture, so you can rerun this", to satisfy the rule that everything runnable on the page is executed before publication. It opened `git init -q repo`. Git's out-of-the-box default branch is still `master`, so line 4's `git switch -q main` died for any reader without `init.defaultBranch` configured:

```
$ git init -q repo && cd repo && git symbolic-ref --short HEAD
master
$ git switch -q main
fatal: invalid reference: main            # exit 128, on git 2.55.0 with a clean HOME
```

It is now `git init -q -b main repo`, verified verbatim against a git with no `init.defaultBranch` anywhere: exit 0, branches `main side-a side-b`, and `merge-tree --write-tree side-a side-b` exiting 1 as the block claims.

The failure is worth naming precisely, because the page's own rule did not prevent it: the fixture WAS executed, but in a shell whose git had `init.defaultBranch=main` — the author's environment rather than the reader's. Executing a command proves it runs where you ran it. The published form still has to be run as published.

## The table undercounted itself, by one row that is its own best evidence

`#175` added a table of correct fixes that silently falsified the prose describing them, and asserted "zero for three". It was four. The missing row: that PR's own round-2 refactor made the numstat reuse `$RESOLVED` instead of invoking `merge-tree` a second time, which left the R5 sweep pasted in its PR body still counting six `merge-tree --write-tree` sites. The page has five.

The row is added and the count corrected to zero for four, with the note that this instance is the sharpest of them — **the sweep missed a miscount in the sweep's own output, in the change that adds the section about missed sweeps.**

Fresh census at this head, five sites: three in the evidence paste, one guarded probe recipe, one guarded identity test. The surviving `head -1` operates on `$RESOLVED`, not on a `merge-tree` call, which is why the sixth disappeared.

Documentation-only change class at `documentation reviewed`; regeneration is idempotent.
