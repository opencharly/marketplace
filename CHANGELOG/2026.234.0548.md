# 2026.234.0548 — spec de-submodule: regenerated skill pages
<!-- auto-merge-finalized: 2026.234.0548 -->

Regeneration from the charly superproject's de-submodule cutover
(opencharly/charly#370): the spec contract module is vendored into the main
repo as plain tracked files (no more `spec/` git submodule, no
`git submodule update --init spec`), and schema cutovers land in the same
superproject PR instead of a spec-repo-first cross-repo sequence.

Updated (generated from `candy/charly-internals` + `candy/charly-build`):
- `internals/skills/go/references/source-map.md` — spec module vendored;
  pluginsgen guards a missing `spec/go.mod` with a tree-corruption error.
- `internals/skills/go/references/schema-change-recipe.md` — schema-version
  bump is a same-repo step (bump + `task cue:gen` + migrations entry in ONE
  superproject PR); out-of-tree consumers adopt via a spec-subtree sync to
  opencharly/spec + spec Go-module tag.
- `internals/skills/cutover-policy/SKILL.md` — `#SchemaVersion` bump is
  in-repo (no spec repo landed + tagged first).
- `build/skills/migrate/SKILL.md` — "Adding a future cutover" rewritten from
  the cross-repo spec-first sequence to the same-repo sequence.
- `internals/skills/egress/SKILL.md` — the ingress `task cue:gen` step runs
  in the vendored spec module.

Documentation-only change class: no behavior change; every page is a
byte-perfect regeneration from the candy sources (`charly marketplace
generate` is a no-op on this tree once the superproject sources land).