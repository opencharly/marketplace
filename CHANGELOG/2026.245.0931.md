# 2026.245.0931 — feat: project the omarchy layer skills (omarchy-base, omarchy-shell)

## Summary

Project the two EXISTING omarchy layer skills into the corpus — they were authored in their owning repos but never generated, because the marketplace refs list (candy/charly-marketplace/charly.yml) did not pin them:

- Add `@github.com/opencharly/layer-omarchy-base:v2026.242.0701` and `@github.com/opencharly/layer-omarchy-shell:v2026.243.1941` to the refs list (both repos carry `skill:` entities with `family: distros`).
- Regenerate the corpus: new `distros/skills/omarchy-base/SKILL.md` + `distros/skills/omarchy-shell/SKILL.md`. Nothing else changed — regeneration on the rest of the tree is a no-op (drift-clean).

This closes the omarchy coverage gap found in the umbrella harness deep-dive (opencharly/opencharly#110): every layer candy used by the omarchy box family now has a marketplace skill, and the omarchy/omarchy-cstream/omarchy-eval/omarchy-vm family skills will be loadable together with the layer skills.

## How tested

- Regenerated with the checkout-built charly binary (the deploy's own approach):

```
$ charly marketplace generate --root . --out .
exit=0
$ git status --short
 M candy/charly-marketplace/charly.yml
?? distros/skills/omarchy-base/
?? distros/skills/omarchy-shell/
```

- New skills verified: the emitted SKILL.md frontmatter carries `name` + `description` + the GENERATED banner (the GENERATED projection does not emit a `family:` field — the `family: distros` lives on the source `skill:` entities in layer-omarchy-base / layer-omarchy-shell, which is what routes them to the `distros/` family dir).
- Drift gate contract: regeneration is a no-op for the unchanged tree — the diff is exactly the refs pins + the two projected skills.

## Rulebook compliance

- Corpus is GENERATED: only the refs list was hand-edited; the two SKILL.md files carry the DO-NOT-EDIT banner and were emitted by `charly marketplace generate`.
- PR-only landing, feat/ branch from origin/main, no force-push.
- The owning layer repos authored these skill entities and tagged them; this PR only pins those tags (policy: pin merged refs only).

## Change classification

- Change class: marketplace corpus (refs pin + regenerated projection)
- Verification gate: full (local generation + drift-clean diff; org-wide pr-validator on this PR)
- Attribution tier: fully tested and validated

*Assisted-by: pi ollama-cloud/deepseek-v4-flash:0731 (fully tested and validated)*