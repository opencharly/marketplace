# 2026.268.1704 — chore(refs): advance the five swept skill repos + regenerate the corpus

## Summary

Advance the marketplace refs list to the merged Taskfile→`charly task` sweep tags,
then regenerate the entire corpus so the projections carry the rewired skill
prose.

Refs bumps (all merged + tagged):

| Repo | Old pin | New pin |
|---|---|---|
| layer-charly-internals | v2026.267.2049 | v2026.268.1524 |
| layer-charly-internals-extra | v2026.265.1721 | v2026.268.1547 |
| layer-charly-build | v2026.261.2027 | v2026.268.1449 |
| layer-charly-check | v2026.263.0038 | v2026.268.1545 |
| layer-charly-image | v2026.250.1401 | v2026.268.1334 |

Because the corpus is regenerated at these refs, the projections also advance
past work already merged in the source repos but not yet projected. The full
content accounted for:

- **The Taskfile→`charly task` sweep** (the reason for this PR): `task <name>` →
  `charly task <name>`, `task build:binary` → `scripts/bootstrap-charly.sh`
  (R9's bare form), the per-plugin params-generation correction, and the removed
  `taskfiles/`/`Taskfile` references. One deliberate historical mention survives
  in `git-workflow`'s `multi-repo-coordination.md`: "or the old `task cue:gen` it
  replaced" — an intentional call-out of the retired name in a footgun example,
  not a live command.
- **`check/skills/jetkvm/SKILL.md` (+121/−15)** and the `DISPATCHER.md` jetkvm
  routing row: the jetkvm skill's `ocr` method + `artifact_contains_text`, the
  `install` console-installer driver + `kind: jetkvm` device entity, `wake-host`,
  and the `charly-jetkvm auth --heal` repair path — landed in layer-charly-check
  PR #21 (tag v2026.268.1545) and projected now.
- **`check/skills/check/references/beds-and-r10.md` (+41/−38)**: the bed-home
  relocation (beds moved to their artifact owners) plus the `kind:check-roster`
  native-roster wording — landed in layer-charly-check PRs #20/#23 (same tag).
- **`internals/skills/agents/references/{agent-roster,parallel-bed-testing}.md`**:
  the `/verify-beds` → native `kind:check-roster` retirement wording, projected
  from the same bumped tags.

## How tested

`charly marketplace generate` (the deploy workflow's exact command) completed
cleanly against the bumped refs:

```
$ charly -C . marketplace generate --root . --out .
(exit 0)
```

The refs list pins are the merged tags:

```
$ grep -E 'layer-charly-(internals|build|check|image)' candy/charly-marketplace/charly.yml
            - '@github.com/opencharly/layer-charly-internals:v2026.268.1524'
            - '@github.com/opencharly/layer-charly-internals-extra:v2026.268.1547'
            - '@github.com/opencharly/layer-charly-build:v2026.268.1449'
            - '@github.com/opencharly/layer-charly-check:v2026.268.1545'
            - '@github.com/opencharly/layer-charly-image:v2026.268.1334'
```

A second regeneration is a no-op (the corpus matches the pins):

```
$ charly -C . marketplace generate --root . --out .
$ git status --short
(empty)
```

**Deletion audit** (a wholesale regen can silently revert landed prose). Every
net-deletion file was read; all changes trace to the bumped tag sources — the
task-sweep rewrite plus the already-merged check-roster / jetkvm work:

```
$ git diff --numstat origin/main...HEAD | awk '$2>$1'
13	21	internals/skills/agents/references/agent-roster.md
20	23	internals/skills/agents/references/parallel-bed-testing.md
13	15	internals/skills/go/SKILL.md
5	7	internals/skills/go/references/schema-change-recipe.md
```

Verified the new tag sources carry both halves:
`git -C layer-charly-internals show v2026.268.1524:charly.yml` contains
`charly task sync` ×6 and `kind:check-roster` ×3; and the check tag
`v2026.268.1545` contains the jetkvm `ocr` content the previous pin lacked.

## Rulebook compliance

- **R5 (delete legacy completely):** the refs advance to tags whose sources are
  swept; the projections carry the `charly task <name>` form. The one surviving
  `task cue:gen` mention is the deliberate historical call-out named above.
- **The deploy drift gate is satisfied:** the regenerated corpus ships WITH the
  refs bump in the same commit.
- **Live or skip:** the generator ran against the REAL repo cache / network.

## Change classification

- **Change class:** generated-corpus regeneration + refs-list pin bump
  (config + generated artifacts).
- **Verification gate:** `charly marketplace generate` end to end (exit 0),
  second run no-op, deletion audit read in full.
- **Attribution tier:** `fully tested and validated`.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (fully tested and validated)*