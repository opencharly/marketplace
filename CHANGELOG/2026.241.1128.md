# 2026.241.1128 — fix: publish Phases A and B — advance the CI charly pin, drop the setup launcher

## Summary of changes

Publishes Phases A and B of the approved cutover into the corpus.

Advances the CI-time charly pin `v2026.241.0848` → `v2026.241.1121`, which pins
`plugin-marketplace v2026.241.1105` and therefore carries:

- **`plugin-marketplace#5`** — `marketplace.json`'s `description`/`version` move to **top level**
  per the [spec](https://code.claude.com/docs/en/plugin-marketplaces). Nested under `metadata`,
  the published catalog had neither as far as Claude Code is concerned — and it validated anyway,
  because `metadata` is free-form.
- **`plugin-marketplace#6`** — the generator generates the marketplace and nothing else: no
  `.claude/hooks`, no `.claude/settings.json` merge, no R0 dispatcher splice, no `setup` launcher.

Concrete edits:

| File | Change |
|---|---|
| `.github/workflows/deploy.yml` | CI charly pin → `v2026.241.1121` |
| `.claude-plugin/marketplace.json` | the regenerated catalog — **the only corpus file that changes** |
| `setup` | **deleted** — it existed only to run generation locally, the workflow this cutover removes, and nothing emits it now |
| `README.md` | Regeneration section corrected (three stale claims) |

**All 303 `SKILL.md` are byte-identical.** This changes the catalog shape, not skill content.

The README's Regeneration section carried three stale claims: that CI checks out a charly
**submodule** (removed — a declared submodule dragged the whole charly repo into consumer fetch
trees), that generation runs as `--root ./charly` from that submodule, and that `./setup` is the
local path.

## Change class

**cross-repo (consumer leg)** — a CI pin, the generated catalog it produces, a deleted generated
artifact, and README prose. No skill source, no refs-list change.

**Closes / relates to:** consumes `opencharly/plugin-marketplace#5`/`#6` and `opencharly/charly#449`
(all merged). Sibling in flight: `opencharly/charly#450` (charly stops generating) — independent;
CI supplies its own refs-list copy either way.

**Breaking change + rollback:** `./setup` no longer exists. It could not have worked for a
consumer anyway without a charly checkout beside the repo, and generation is CI-owned. Rollback is
a plain revert, which restores the pin, the catalog and the launcher together.

## How R10-tested — exact gate, commands, and pasted output

- **The gate for this change class:** this repo's `marketplace` workflow, which runs on
  `pull_request` — it clones charly at the CI pin, builds the binary from that checkout,
  regenerates and **fails closed** on any diff. I reproduced that exact sequence locally first.
- **Eval beds run:** `N/A (CI pin + generated artifacts)` — no `disposable: true` bed maps to it;
  the authority is the fail-closed drift gate.
- **Fresh rebuild:** the binary was built **from the pinned checkout itself** (a worktree at
  `v2026.241.1121`), not from some other charly tree — the strictest form available here.
- **Did the CHANGED code path execute live?** **Yes** — generation and drift both ran at the new
  pin, against this repo's refs list, in CI's own layering order.
- **Concurrency:** `N/A`.

*1. Fresh-rebuild gate output* (zero warnings):
```
CI's sequence, reproduced locally:

$ git worktree add --detach ../charly-pin v2026.241.1121     # charly at the NEW pin
$ mkdir -p ../charly-pin/candy/charly-marketplace
$ cp candy/charly-marketplace/charly.yml ../charly-pin/candy/charly-marketplace/charly.yml
$ cd ../charly-pin && task build:binary                      # binary FROM the pinned checkout

$ ./bin/charly marketplace drift --root ../charly-pin --out ../marketplace
marketplace drift: clean (397 artifact(s) on disk match their sources; regeneration is a no-op)
```

**397, down from 405** — exactly the 8 artifacts the generator no longer emits: `setup` (1),
`.claude/hooks/*` (4), `.claude/settings.json` (1), `CLAUDE.md`, `AGENTS.md`.

*2. Each changed code path executing:*
```
(a) The new generator's corpus change is exactly ONE file:

$ charly marketplace generate --root <charly@new pin> --out .
$ git status --porcelain
 M .claude-plugin/marketplace.json

$ git status --porcelain | grep -c SKILL.md
0                       # 303 SKILL.md byte-identical

(b) The catalog diff is pm#5's effect, and nothing else:

-  "metadata": {
-    "description": "Claude Code, Codex, and Kimi plugins for OpenCharly, …",
-    "version": "3.1.0"
-  },
+  "description": "Claude Code, Codex, and Kimi plugins for OpenCharly, …",
+  "version": "3.1.0",

(c) The result validates against the official gate:

$ claude plugin validate .
✔ Validation passed with warnings     # the 26 expected "no version" advisories, per the spec's
                                      # recommendation to omit version for active development

(d) pm#6 confirmed live — the generator wrote NOTHING into the charly checkout:

$ git -C <charly> status --short -- .claude CLAUDE.md AGENTS.md
                        # empty
```

*3. Docs-only evidence:*
```
N/A — not docs-only: a CI pin, a generated catalog and a deleted generated artifact.
```

## Attribution tier

**fully tested and validated** — the binary was built from the pinned checkout, generation and
drift ran in CI's own sequence and return `clean` at 397 artifacts, the emitted catalog was
inspected and validated with `claude plugin validate`, and the skill corpus was confirmed
byte-identical.

## Harness rulebook compliance

- **R0 skills:** `/charly-internals:git-workflow` loaded before the first git call and diffed
  against the current corpus commit before being followed.
- **R1 RCA + zero warnings:** the pin target was verified to carry both changes before being
  pinned (`v2026.241.1121` → `plugin-marketplace:v2026.241.1105`), rather than inferred from merge
  order. Drift output at zero warnings.
- **R2 no out-of-scope:** the orphaned `setup` and the three stale README claims the cutover
  creates are fixed **here**, not filed — an earlier PR in this series was correctly blocked for
  parking exactly this kind of residue.
- **R3 no duplication:** one pin, one generated catalog, one owner (`deploy.yml`).
- **R4 no workaround:** the catalog was REGENERATED by the tool, never hand-edited — it carries a
  DO-NOT-EDIT contract and a hand edit would surface as drift.
- **R5 hard cutover + grep:** `setup` is deleted rather than left unowned, and the instruction to
  use it is gone. The sweep returns exactly one line, and it is the correction itself, not a
  survival:

  ```
  $ grep -n "\./setup" README.md CLAUDE.md
  README.md:55:`./setup` launcher it used to emit is gone with the local-generation workflow it existed for.
  ```

  No remaining text tells a reader to run it.
- **R6 git safety:** `git status` + `git stash list` checked before branching; branch cut from
  freshly fetched `main`; no force-push; post-commit status clean; `git show --stat` lists exactly
  the four intended files.
- **R7 runtime gate:** proven by running generate + drift at the new pin, not by reading the diff.
- **R8 artifact invariants:** asserted after generation — one file changed, zero SKILL.md touched,
  `claude plugin validate` clean, drift a no-op.
- **R9 binary == source + deps:** the binary was built from the pinned checkout, so binary and
  source are the same commit.
- **R10 disposable + coverage:** no bed maps to a pin-and-regenerate change, so this branches to
  the fail-closed drift gate, run locally and re-run by CI on this PR. Coverage that would fail
  without the change: with the old pin the catalog keeps `metadata.description` and the corpus
  keeps `setup`.
- **RDD / ADE / SDD:** the two load-bearing assumptions — that the pin carries both changes, and
  that regeneration is reproducible and complete — were PROVEN before relying on them.
- **Hard cutover:** ONE atomic commit; pin, catalog, deletion and docs move together.
- **Kernel/plugin boundary law / Disposable-only autonomy:** `N/A`.
- **Clean architecture + Go gates:** `N/A — no Go source in this repo.`
- **CHANGELOG:** `N/A` — `tag-on-merge` writes it from this body at merge.

---

*Assisted-by: Claude Code claude-opus-5[1m] (fully tested and validated)*