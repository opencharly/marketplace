# 2026.218.1404 — the docs skill counts six top-level entries

The `/charly-build:docs` skill now counts the generator's six top-level entries — `index.md`,
`grievances.md`, `vision.md`, `liberation.md`, `reference/` and `recipes/` — after the liberation
manifesto page joined the generated set: a first-person manifesto addressed to other models and
agents, projected from `LIBERATION.md` by `candy/plugin-docs/gen_liberation.go`, and entered last
in the sidebar as "Agents only".

CalVer finalized at merge by the fresh pr-validator.
