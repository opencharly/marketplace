# 2026.261.1142 — feat(corpus): publish the narrowed key wording and the charly-jetkvm env flow

## Summary of changes

Regenerates the marketplace corpus from the current `layer-charly-check` release.

- `candy/charly-marketplace/charly.yml`: bumps the `layer-charly-check` pin **`v2026.261.0617` → `v2026.261.1057`**. (0617 was the last projected pin; the intermediate 0934 was never projected, so this bump spans both #15 and #16.)
- Regenerated corpus: `check/skills/jetkvm/SKILL.md` gains the current `jetkvm` "Connection details" prose from the source entity — device-address resolution (`host:` → `JETKVM_HOST` → venue), input semantics (`move`/`mouse` pure, `click`/`drag` press, `left` default), the **absolute HID `[0,32767]` pointer coordinates** (not pixels), and the **narrowed** key-resolver wording ("common USB HID … names outside that set fail with `unknown key`"). The diff against the previously-projected corpus is **purely additive** (29 insertions, 0 deletions): the base at pin 0617 carried none of this text, so nothing in it is a "replacement" — the outdated claim never reached the corpus, and the regenerated text is the corrected one.

**Closes / relates to:** `opencharly/layer-charly-check#15`, `#16` (source entities) and `opencharly/plugin-jetkvm#5`, `opencharly/charly-jetkvm#1` (code).

**Breaking change + rollback:** `none` — a pin bump plus its generated projection. Revert the merge to roll back.

## Change class

`corpus` — a marketplace refs pin bump plus its generated projection (`candy/charly-marketplace/charly.yml` is a manifest/config file; `check/skills/jetkvm/SKILL.md` is generated markdown).

## How tested

The marketplace's own gate is `deploy.yml`'s **drift gate**: the committed corpus must equal a regeneration from this repo's refs list. Reproduced with the pinned binary — the FIRST run produced the corpus, and a SECOND run was a no-op:

```console
$ # on the committed tree, regeneration must be a NO-OP (the drift gate)
$ /tmp/charly-marketplace marketplace generate --root "$PWD" --out "$PWD"
$ echo $?
0
$ git status --porcelain
                       # (empty — the committed corpus matches generation exactly)
$ git --no-pager diff HEAD~1 HEAD --numstat
1	1	candy/charly-marketplace/charly.yml
29	0	check/skills/jetkvm/SKILL.md
```

The committed tree regenerating with an empty `git status` is the drift gate's contract. The generated content matches the source entity:

```console
$ grep -c "common USB HID" check/skills/jetkvm/SKILL.md
1
$ grep -c "charly-jetkvm env" check/skills/jetkvm/SKILL.md
2
$ grep -c "arbitrary names" check/skills/jetkvm/SKILL.md
0
$ grep -c armadillo check/skills/jetkvm/SKILL.md
0
```

The generated file keeps its `DO-NOT-EDIT` header; the source is the `layer-charly-check` entity, so this PR contains no hand edit.

## Change classification

- **Change class:** `corpus` (pin bump + generated projection).
- **Verification gate:** the drift gate reproduced (generator ran against the pinned tag; second run a no-op) as pasted above.
- **Attribution tier:** `fully tested and validated` — the corpus was regenerated from the pinned source and a second run is a verified no-op, so the committed corpus matches the refs list exactly.

## Harness rulebook compliance

- **R0 skills:** `/charly-build:docs`, `/charly-internals:git-workflow`.
- **R1 RCA + zero warnings:** the pin lagged the merged source edits (source and projection are one cross-repo cutover; the corpus pins the source's merge tag); fixed by the pin bump + regeneration.
- **R2 no out-of-scope:** the single pin and its generated file are the whole change.
- **R3 no duplication:** the content is projected from the source entity, never hand-written here.
- **R4 no workaround:** the pin moves to the merged tag; the real generator produced the corpus.
- **R5 hard cutover + grep:** the greps above; the second generation is a no-op.
- **R6 git safety:** fresh branch off current `origin/main`; no force update, no hooks bypass, no direct push to `main`.
- **R7 runtime gate:** the generator ran twice; the second run is the no-op the drift gate asserts.
- **R8 artifact invariants:** the generated file keeps its `DO-NOT-EDIT` header and the corpus stays reproducible.
- **R9 binary == source + deps:** generation used the charly binary built from the tag the workflow pins.
- **R10 disposable + coverage:** `N/A — a corpus repo`; the drift gate is the shipped coverage and was exercised.
- **RDD / ADE / SDD:** `N/A — corpus projection only`.
- **CHANGELOG:** written by `tag-on-merge` from this body.

---
*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (fully tested and validated)*