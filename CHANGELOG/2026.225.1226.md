# 2026.225.1226 — sweep the tagged-repo enumerations to name docs

The docs CHANGELOG-convention cutover (plugins#156) landed the convention statements but left
every repo-set enumeration omitting `docs` — the claim-class residue the docs#47 validator's R5
finding surfaced. `docs` carries no `charly.yml` and is tagged at merge (v2026.225.1058), so a
fresh validator reading any pre-edit list could conclude `docs` is a tag-exemption — the exact
leak the cutover closes. This follow-up sweeps the claim class across every surface that names
the participant repos:

- **Tagged-repo enumerations** — "Every repo (superproject, `box/<distro>`, `plugins`, `pkg/*`)
  is tagged on its own merged HEAD" (cutover-policy), "Every repo is tagged at merge. The
  superproject, every `box/<distro>`, `plugins`, and `pkg/*` all mint…" (git-workflow
  invariant), "Every repo (…) mints `v$VER`" (validator-and-calver), and the B7 tag-step
  sentences ("`plugins` and `pkg/*` ARE tagged…", "tag EVERY repo (`plugins` and `pkg/*`
  included…)", "a `plugins` / `pkg/*` tag fires NO workflow") — all gain `docs`.
- **Repo-set enumerations** — the PR-gate audit scope, B2 branch-correlation, B4 sync/prune,
  the validator working-directory rule, the W1 no-`.claude/` list, and the signpost `CLAUDE.md`
  inventory (8 → 9, `docs/` added).

Regenerated from the superproject's `candy/charly-internals/charly.yml` manifest by `charly
marketplace generate` (never hand-edited); regeneration is a no-op on the committed tree and
`charly box validate` passes at zero warnings. The `docs` leg (docs#47) carries the same sweep;
the superproject leg bumps both gitlinks after both merge. Placeholder CalVer — the pr-validator
finalizes it at merge.
