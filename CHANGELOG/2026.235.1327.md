# 2026.235.1327 — feat: bump the charly pin to the ref-advance main (45cdc36); carry the regenerated corpus

## Summary

The marketplace cutover's **pin-advance + corpus regen** (charly#383 merged the ref bump + the launcher fix + the internals examples): this repo's charly submodule advances from `fa254fd5` to `45cdc36` (the ref-advance main), and the corpus is regenerated from it — the four deltas the charly PR's sources cause:

- `internals/agents/pr-validator.md`, `internals/skills/agents/references/hooks-and-lifecycle.md`, `internals/skills/git-workflow/references/validator-and-calver.md` — the marketplace-path examples (the `git -C /abs/path/marketplace …` form; the charly-side source fix projects here).
- `setup` — the launcher's cwd/flag contract (`cd ./charly && charly marketplace generate --root . --out ..`).

## How tested

**Regeneration at the pinned charly is a no-op on the committed corpus (pasted):**

```
$ git -C charly checkout 45cdc36bf71a3b790f91a7f8ce0a546b6b16c1df && git -C charly submodule update --init
$ cd charly && /tmp/mp-marketplace/bin/charly marketplace generate --root /tmp/mp-marketplace/charly --out /tmp/mp-marketplace
marketplace generate: wrote 406 artifact(s) across 26 family(ies)
$ git status --porcelain | awk '{print $1}' | sort | uniq -c
      5 M     # charly gitlink + the 4 regenerated corpus files (the only deltas)
$ git status --porcelain
 M charly
 M internals/agents/pr-validator.md
 M internals/skills/agents/references/hooks-and-lifecycle.md
 M internals/skills/git-workflow/references/validator-and-calver.md
 M setup
```

2. **The PR's own `marketplace` deploy check is the live gate** — the pipeline (recursive checkout at the pin → build → generate → fail-closed drift gate incl. untracked files → catalog sanity → Pages) runs on this PR.

## Rulebook compliance

| Rule | How / N/A |
|---|---|
| R0 | git-workflow + docs skills loaded before editing |
| R1 | N/A — no surfaced failure |
| R2 | N/A — no surfaced issue parked |
| R3 | the generator (charly candy/plugin-marketplace) remains the single canonical corpus owner; this repo commits only its projection |
| R4 | no workarounds — the drift gate is the policy's own operation |
| R5 | the old launcher contract + the `plugins`-path examples regenerated away at the new pin |
| R6 | git status checked; no force-push; feat-branch + PR landing |
| R7 | the regeneration no-op proof pasted above + the PR's deploy check (the workflow is the gate) |
| R8 | the emitted artifacts verified live (the generate run above) |
| R9 | N/A — the charly binary is built by the deploy workflow from the pinned checkout |
| R10 | the fresh-checkout regeneration at the pin IS the R10 for this repo (the deploy does the same on every run) |

## Change classification

- **Change class**: submodule pin advance + regenerated corpus
- **Verification gate**: the `marketplace` deploy workflow on this PR (build → generate → drift gate) + the regeneration no-op proof pasted above
- **Attribution tier**: analysed on a live system — the regeneration ran live with retained output; the deploy gate's full run is the PR's own CI
- **Landing**: fresh `charly/pr-validator`; tag-on-merge

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*