# 2026.269.1750 — docs(corpus): regenerate the git-workflow skill for layer-charly-internals v2026.269.1446

## Summary

Regenerate the marketplace corpus for the corrected `git-workflow` skill:
adopt `layer-charly-internals` `v2026.269.1726` and regenerate in one commit, so
the corpus teaches the **measured** body-fix mechanism.

### Why

`marketplace/internals/skills/git-workflow/**` is GENERATED from the
`layer-charly-internals` candy. The authored correction —
opencharly/layer-charly-internals#25, MERGED, tag `v2026.269.1726` — replaced the
disproven `edited`-trigger claim with the **measured** mechanism: a body-only
BLOCK is cleared by the `rerun` label (a plain per-repo `rerun-listener` that
re-runs THIS head's failed validator run — same `GITHUB_SHA`, SAME check run in
place, no duplicate, clears POISON), or manually `gh run rerun <run-id>`. Never
an empty commit.

The earlier commit on this branch (`32fd642`) adopted `v2026.269.1446`, whose
skill still carried the false `edited` claim; the final commit corrects it.

### What changed

- `candy/charly-marketplace/charly.yml` — `layer-charly-internals` →
  `v2026.269.1726`.
- `internals/skills/git-workflow/SKILL.md` +
  `references/validator-and-calver.md` — regenerated.

## How tested

Built charly from THIS repo's CI pin (`v2026.266.1654`); a second generation run
proves the committed corpus matches the refs list (the repo's drift gate):

```
$ /tmp/charly-mp-bin marketplace generate --root . --out .
marketplace generate: wrote 457 artifact(s) across 38 family(ies)
$ /tmp/charly-mp-bin marketplace generate --root . --out .
$ git status --porcelain | wc -l
0                       # second regen is a no-op
```

The generated skill carries the corrected mechanism and drops the false claim:

```
$ grep -c "rerun. label\|rerun-listener\|gh run rerun" internals/skills/git-workflow/SKILL.md
6
$ grep -c "ready_for_review, edited\|edited. trigger re-runs" internals/skills/git-workflow/SKILL.md
0
```

## Rulebook compliance

- **R0** — the projected skill was reviewed in its owning repo.
- **R1** — N/A here; the consumer tracks an already-RCA'd fix.
- **R2** — no deferral: pin bump + regenerated corpus land together.
- **R3** — one source (the pinned refs list); no hand-editing.
- **R4** — the generator is the canonical path.
- **R5** — the superseded `edited` claim is removed from the corpus (grep: 0).
- **R6** — feat/ branch, PR-only, no force-push (append-only).
- **R7/R8** — the emitted artifact is validated by regeneration-as-no-op.
- **R9** — N/A (no binary).
- **R10** — the fresh-rebuild gate IS the second `marketplace generate` no-op.

## Change classification

- **Change class:** generated-corpus regeneration + its refs-list pin bump.
- **Verification gate:** `charly marketplace generate` end-to-end + a no-op
  second run + the corrected section present and the false claim absent.
- **Attribution tier:** `fully tested and validated`.

Depends on: **opencharly/layer-charly-internals#25** (MERGED, `v2026.269.1726`).

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (fully tested and validated)*