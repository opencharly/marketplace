# 2026.245.1509 — chore(marketplace): bump layer-charly-check v2026.245.1444 + regen

## Summary

Bumps the `layer-charly-check` refs pin `v2026.243.2146` → `v2026.245.1444` (the tag carrying the recording-lane skill-entity updates) and regenerates the corpus: the `check/skills/record/SKILL.md` and `check/skills/spice/SKILL.md` diffs reflect the merged sources (`record: run` + `record_env` for record; the `record` method for spice).

## How tested

```
$ charly marketplace generate --root . --out .    # on the bumped refs list
$ git status --porcelain
 M candy/charly-marketplace/charly.yml
 M check/skills/record/SKILL.md
 M check/skills/spice/SKILL.md        # the only diffs — generated corpus matches the new ref content
```

The deploy drift gate (regen + porcelain-empty) is the repo workflow's own gate.

## Rulebook compliance

- R1: corpus change via PR to the owning repo; regen is the sanctioned verb. R3: no hand-edits — the SKILL.md diffs are GENERATED from the layer's entities. No core/pins beyond the ref bump.

## Change classification

- **Change class:** config change (refs pin bump + generated corpus)
- **Verification gate:** regen produces exactly the expected diffs; deploy drift gate is the repo workflow
- **Attribution tier:** analysed on a live system (the regeneration ran live on this host, producing the exact three-file diff)

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*