# 2026.250.1233 — chore: refs to the config: verb skill tags + regenerated corpus

## Summary

Docs wave for the **`config:` plan-step verb** (spec#117-120/sdk#237/plugin-box#11/plugin-egress#3 — charly config-file generation): bumps the refs pins to the owning repos' new tags and ships the **regenerated corpus** in the same PR (drift-gate compliant).

- refs: `layer-charly-image v2026.250.1221` (#7 — the verb catalog config: row), `layer-charly-build v2026.250.1221` (#9 — config: rules + the content-substitution rule), `layer-charly-internals v2026.250.1222` (#10 — the crabbox-yaml egress kind).
- project `version:` 2026.240.1943 → 2026.249.2125 (the current floor; the previous schema blocked regeneration with the current binary).
- Regenerated corpus (the three SKILL.md copies): `image/skills/layer/SKILL.md` (+1: config: verb row), `build/skills/validate/SKILL.md` (+3: config in the exactly-one list + rules + content-substitution rule), `internals/skills/egress/SKILL.md` (+1: crabbox-yaml kind).

## How tested

```
$ charly marketplace generate --root <marketplace> --out <marketplace>   # in the marketplace checkout
(regenerated corpus — git diff shows exactly the three doc additions: 5 files, +9/-5 — no unintended drift)
```
The regeneration diff matches the owner-source changes one-to-one; the marketplace deploy drift gate re-verifies on the next run.

## Rulebook compliance

- R3: the corpus is generated from the owner sources — no hand-edited SKILL.md; the drift gate enforces the invariant.
- R7: the regeneration command + the exact diff pasted above.
- Policy B / pin discipline: refs point at the MERGED tags (cross-verified: tag-sha == merge-commit-sha for all three).

## Change classification

- **Class:** refs/schema bump + regenerated docs corpus. **Verification gate:** the regeneration ran live and produced the exact expected diff; the marketplace deploy drift gate re-verifies. **Attribution:** analysed on a live system (generation executed; the deploy gate is the standing verifier).

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*