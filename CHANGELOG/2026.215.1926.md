# 2026.215.1926

## docs(skills): #55 import-purity stale-claim sweeps (re-applied to the modernized layout)

Two claim-classes swept, re-authored against the context-engineering split (218e43e):

- **go/references/source-map.md** — credential_plugin.go's row no longer names the
  `setDefaultCredentialStoreForTest`/`resetDefaultCredentialStoreForTest` test-override seam
  (DELETED in the #55 decoupling: zero callers remained; ResolveCredential's env-precedence
  path is covered by charly/credential_resolve_test.go) and no longer attributes the
  keyring-wait caller to enc.go (it lives in candy/plugin-enc since C16a).
- **plugin/references/boundary-law.md + agents/references/orchestration-model.md** — the P16
  gate (a) description updated: the per-file allowlist (~4k floor) is RETIRED for the
  import-surface assertion (charly/import_purity_test.go — every charly/ file imports only
  spec/* + the proto/plugin-api contract + vetted third-party); gate (b) now states charly/
  at ZERO github.com/opencharly/sdk references (prod, test, go.mod — the #55 terminus).
