# 2026.216.2340 — the encrypted-volume boot gate is the backend name, and why

The `enc` skill's boot section was a table headed **Boot Behavior: Backend-Gated**, which taught
autostart as something a deploy earned by picking the right credential store — a row per backend,
with the backend name as the gate.

**That table was right about the gate**, and earlier revisions of this entry claimed the opposite.
The rewrite replaced it with a capability — *"can the passphrase be obtained with no human
present?"* — which is false: `ConfigFileStore.Get` reads a plaintext file with no prompt and no
keyring, so it satisfies that test more easily than Secret Service does, and a rule phrased that way
would grant autostart to the cleartext case and deny it to the encrypted one.

The section, now headed **Boot Behavior: Which Backend, and Why**, leads with a two-row table keyed
on `secret_backend` and supplies the part the old table never did: **why**. sdk#121 ruled that
`config` stays supported with a cleartext warning — shipped as `warnCleartextStorage` in
charly#213 — and rejected the capability form because "it would grant autostart to a deploy whose
passphrase sits in cleartext". A passphrase in a plaintext file must not be auto-mounted, unattended,
at every boot; Secret Service qualifies because the key is protected until an operator unlocks it,
not because it is easier to obtain.

`keyring`, `auto` and unset carry `WantedBy=default.target` with `TimeoutStartSec=0`, so
`ExecStartPre` blocks in the mount until the keyring opens rather than timing out. `auto` is the
default, so that is the out-of-the-box arrangement with nothing to configure.

`config` gets no `WantedBy=` target. That outcome stays documented, because it is a supported
configuration an operator will hit. The table's `at boot` column states the outcome — the unit does
not start and needs an explicit `charly start <image>`.

Backends are named deliberately, because the gate is the name. Nothing is described as removed,
unsupported, or deprecated.

`usesWaitingBackend` (`ResolveEncPassphraseForMountWithResolver`) tests the same set and is cited as
such — same members, different question: whether the resolver waits rather than fails fast. Task #27
carries the duplication.

## The sweep found a second, larger problem in the same file

Sweeping for other statements of the removed model turned up something else: the `enc` skill's
source map described a file that does not exist. `charly/enc.go` was cited eight times, and every
symbol attributed to it — `encMount`, `encUnmount`, `encPasswd`, `ensureEncryptedMounts`,
`encPlanFor`, `encStatus`, `encryptedPlainDir`, `isEncryptedMounted`, `cipherPopulatedPlainEmpty`,
`loadEncryptedVolume`, `verifyBindMounts`, `encExecViaPlugin`, `awaitKeyringUnlockViaPlugin` — is
gone from `charly/`. The skill also quoted an error message that appears nowhere in the codebase,
and named a test file (`charly/enc_resolve_mount_test.go`) that does not exist.

Every claim is re-derived from the live tree and re-pointed at where the code now lives:

- `sdk/deploykit/enc_probe.go` — the probe and plan surface (`EncPlanFor`, `LoadEncryptedVolume`,
  `EncStatus`, `VerifyBindMounts`, `CipherPopulatedPlainEmpty`, `FuseAllowOtherEnabled`,
  `AskPassword`).
- `sdk/deploykit/enc_passphrase.go` — the resolution orchestration (`ResolveEncPassphraseForMount`
  and its `usesWaitingBackend` split, `EncNotStoredError`, `RetryUnavailable`, and the exported
  `EncMountDeadline` / `EncMountPollPeriod` that the skill had as lowercase in-core variables).
- `candy/plugin-pod/enc_cmd.go` — the `charly config status|mount|unmount|passwd` leaf bodies,
  including the all-mounted fast path and the injected keyring-unlock waiter.
- `candy/plugin-enc/enc.go` — the gocryptfs shelling, unchanged and already correctly described.

The quoted error is now the real `EncNotStoredError` text, the test reference is
`sdk/deploykit/enc_passphrase_test.go`, and its sub-case count is re-derived from the function
rather than carried over.

One internal contradiction fell out of this. The systemd source-classification list said `locked`
retries to a deadline, while "Event-driven keyring waiting" further down the same page said the wait is
unbounded. The code takes the unbounded branch whenever an unlock waiter is wired, which the
systemd path always does; the list now says so, and names the degraded no-waiter case rather than
implying it is the only one.

## Sibling skills carrying the same dead paths — NOT fixed here, and why

An earlier revision of this entry claimed `/charly-core:charly-config`, `/charly-core:deploy` and
`/charly-core:start` were fixed alongside, "since a corrected page beside three uncorrected ones is
the failure mode where the history gets fixed and the guidance does not."

**They were dropped from this change and the claim outlived the cut.** This branch was cut before
plugins#129 reverted three skills for documenting unlanded code; it carried that content forward,
and removing it took the sibling edits with it. So the sentence describing the failure mode was
itself shipping the failure mode.

`charly/enc.go` therefore survives at `core/skills/charly-config/SKILL.md:603` and
`core/skills/deploy/SKILL.md:847`. Both are file-level citations of a package the plugin migration
emptied, which is exactly the scope of **task #28** — measured, not estimated: 28 of 42 distinct
`charly/*.go` files cited across the 9 core skills that cite any do not exist.

**And the same is true of the file this change DOES touch.** Fixing the `credential_plugin.go` row
does not make `source-map.md` correct: 45 of its 75 `charly/*.go` rows still name files that do not
exist, including `deploy.go` and `provides.go` adjacent to the `enc.go` row deleted here. Only the
row this branch edits is repaired, on the principle that a dead citation beside an added paragraph
is this branch's to carry. The remaining 45 are the second population task #28 records, so nothing
knowingly left behind is left without an exit.

Fixed here: `/charly-internals:go`'s source map, because this change edits that file anyway and a
dead citation three lines from an added paragraph is this branch's own to carry.

The `charly/` file table in `/charly-internals:go` loses its `enc.go` row — a table of files in a
package should not list one the package no longer has — and gains a short pointer to the four
places the surface actually lives.
