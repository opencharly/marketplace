# 2026.265.1816 — chore(refs): pin the five remaining distro-* families as corpus sources

## Summary

Add five refs-list pins for the distro image families that now own their OS/base
skills — the corpus-source half of the skill-ownership wave (same wave as the
already-landed `distro-arch:v2026.265.1557` pin):

- `@github.com/opencharly/distro-fedora:v2026.265.1656`
- `@github.com/opencharly/distro-cachyos:v2026.265.1729`
- `@github.com/opencharly/distro-omarchy:v2026.265.1655`
- `@github.com/opencharly/distro-debian:v2026.265.1656`
- `@github.com/opencharly/distro-ubuntu:v2026.265.1657`

The pins are inert on today's tree (the skills are still also defined in the
pinned `layer-charly-*` aggregators), so regeneration is a no-op — proven below.
The DECISIVE proof is that the skills still emit when the aggregator copies are
removed, i.e. the state after the companion deletion PRs.

Files changed (`git diff --stat origin/main`):

```
 CHANGELOG/2026.265.1735.md          | 86 +++++++++++++++++++++++++++++++++++++
 candy/charly-marketplace/charly.yml |  7 +++
 2 files changed, 93 insertions(+)
```

## How tested

**1. Regeneration is a no-op on today's tree:**

```
$ charly marketplace generate --root . --out /tmp/opencode/skills-migration/mkt-wave
# generator log confirms all five new pins resolved:
#   Downloading github.com/opencharly/distro-fedora:v2026.265.1656...
#   Downloading github.com/opencharly/distro-cachyos:v2026.265.1729...
#   Downloading github.com/opencharly/distro-omarchy:v2026.265.1655...
#   Downloading github.com/opencharly/distro-debian:v2026.265.1656...
#   Downloading github.com/opencharly/distro-ubuntu:v2026.265.1657...
# full-tree hash comparison (generated vs committed), excluding hand-authored files:
generated: 427   only-new: 0   content-differs: 0
```

**2. DECISIVE — the skills survive with the aggregators' copies REMOVED.** A
scratch closure was built whose refs list drops the nine `layer-charly-*` /
`pod-charly-versa` / `vm-charly-vm` sources, redirecting those repos (git
`insteadOf`) to locally-patched copies with all 37 wave skills deleted — the
exact post-deletion state. The generator run over it:

```
$ # redir/<agg>/charly.yml = the aggregator with the moved skills removed, e.g.:
$ grep -c 'name: cachyos$'      redir/layer-charly-distros/charly.yml   -> 0
$ grep -c 'name: versa$'        redir/pod-charly-versa/charly.yml       -> 0
$ grep -c 'name: omarchy-vm$'   redir/vm-charly-vm/charly.yml           -> 0

$ charly marketplace generate --root proj-closure --out closure-out3
EXIT=0

$ python3 -c '<compare each wave skill in closure-out3 vs the committed corpus>'
wave skills present in closure-WITHOUT-aggregators: 37/37
missing: []
content-differs: []
```

So after step 3's deletions land, every wave skill is still emitted — and only
from its new distro-* owner.

**3. Deterministic dedup (R3).** While both sources exist, the generator resolves
duplicate skill names deterministically (highest source tag / local wins,
`candy/plugin-marketplace` `resolve.go:preferEntity`), so the dual-source window
is not order-dependent. The no-op in (1) is that dedup resolving to the
(byte-identical) copies.

## Wave / landing order

1. Producer PRs — MERGED + tagged: `distro-fedora#53`, `distro-cachyos#91`,
   `distro-omarchy#55`, `distro-debian#23`, `distro-ubuntu#23`.
2. **this PR** — pins the five sources; regeneration is a no-op.
3. Aggregator deletions — `layer-charly-distros#11`, `layer-charly-coder#8`,
   `layer-charly-infrastructure#7`, `layer-charly-jupyter#5`,
   `layer-charly-local#4`, `layer-charly-openclaw#8`, `layer-charly-selkies#4`,
   `pod-charly-versa#2`, `vm-charly-vm#6`.

## Rulebook compliance

- **R0** — `/charly-internals:marketplace` + `/charly-internals:skills` loaded.
- **R1** — regeneration divergence RCA'd as a no-op (0 differing, 0 new).
- **R2** — one coherent wave (steps 1–3).
- **R3** — no NEW duplication: the closure dedups deterministically; the
  dual-source window is pre-existing and closed by the named deletions.
- **R4** — no sleeps/retries/workarounds; the real generator is the gate.
- **R5** — the companion deletion PRs remove the duplicate entities.
- **R6** — dedicated checkout, PR-only, no force-push.
- **R7** — `charly marketplace generate` ran on this refs list, plus the
  aggregators-removed closure run (the check that FAILS if the pins supply
  nothing: the 37 skills would be missing).
- **R8/R9** — no labels/plans/schemas; no binary built here.
- **R10** — config class (refs list); the generator ran live with retained output.

## Change classification

Config (refs-list pins) → `analysed on a live system`. Proof: real generator run
(no-op on today's tree) + the aggregators-removed closure run (37/37 skills
persist from the new pins).

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (analysed on a live system)*