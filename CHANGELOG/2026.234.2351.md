# 2026.234.2351 — docs: regenerate validator-and-calver flow bullet + the missed assembled-pair/key-deps sweep

## Summary

Regenerated corpus for two waves:

1. **Flow bullet** — the charly-internals write-access entity now describes the tag-on-merge flow (native auto-merge squash; `tag-on-merge` finalizes the CalVer, writes `CHANGELOG/<CalVer>.md` from the merged PR body, tags the merged HEAD) — regenerated into `internals/skills/git-workflow/references/validator-and-calver.md`. Surfaced by the docs-leg validator on the regenerated docs page (opencharly/docs#93) as an internal landing-flow contradiction.
2. **Missed sweep completion** — the last two sdk-replace claims (B2 "assembled-pair" adaptation + the go skill's key-deps line) were committed to plugins#225 after its auto-merge had already squashed (push 23:31Z vs merge 23:27Z), so plugins main still carried the old text. This regeneration completes the sdk de-submodule corpus sweep in `multi-repo-coordination.md` + `go/SKILL.md`.

## How tested

**Regeneration — executed (charly worktree at the charly#379 head, plugins submodule at plugins main):**

```
$ ./bin/charly marketplace generate --root <charly-worktree>
exit=0
$ git -C plugins status --short
 M internals/skills/git-workflow/references/multi-repo-coordination.md
 M internals/skills/git-workflow/references/validator-and-calver.md
 M internals/skills/go/SKILL.md
(parallel-bed-testing.md regenerates from pre-registered drift — reverted)
```

**Content checks (executed):**

```
$ grep -c "gh pr merge --squash --delete-branch" internals/skills/git-workflow/references/validator-and-calver.md
0
$ grep -c "tag-on-merge" internals/skills/git-workflow/references/validator-and-calver.md
3
$ grep -c "gitlink bump" internals/skills/git-workflow/references/multi-repo-coordination.md
0
```

## Rulebook compliance

| Rule | How / N/A |
|---|---|
| R0 | git-workflow + skills loaded |
| R1 | the missed-sweep race RCA'd (push-after-merge 23:31Z vs 23:27Z) — completed here, not deferred |
| R2 | both corpus fixes in one PR |
| R3 | regeneration only; entities in charly#379 are the source |
| R4 | N/A — no runtime surface |
| R5 | the last sdk-replace claims + the stale flow bullet removed; greps above |
| R7 | regen + grep outputs pasted |
| R10 | docs-only class — non-runtime standards |

## Change classification

- **Change class**: documentation-only (3 generated skill pages)
- **Verification gate**: non-runtime standards; regeneration no-op verified
- **Attribution tier**: documentation reviewed
- **Landing**: fresh `charly/pr-validator`; tag-on-merge

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (documentation reviewed)*