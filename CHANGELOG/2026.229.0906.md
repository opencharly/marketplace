# 2026.229.0906 — the drift transcript names its tree, and the message it quotes stops overclaiming

**This is a REGENERATION, not an authored edit.**
`internals/skills/git-workflow/references/evidence-and-freshness.md` carries the
`GENERATED … DO NOT EDIT` banner; its source is the `skill:` entity in
`candy/charly-internals/charly.yml` in `charly`, landing as `opencharly/charly#289`.

## What moved

One file, `+70/-10`: the `A skill: source edit and its regeneration are one cutover across two
repos` block.

A `pr-validator` found the page publishing a figure —
`marketplace drift: clean (400 artifact(s) match the committed tree)` — labelled only *"Both runs
perturb a clean tree"*, while the same page rules that **a figure is publishable only with the tree
identified: which commit, which submodule shas, which working-tree state.** The count was
re-derived independently and is correct; the defect was provenance. Worse, that line had been seen
printing `exit 0` beside a `git status --short` calling the tree dirty, so *"a clean tree"* was
ambiguous between git-clean and drift-clean — the ambiguity the rule exists to kill, inside the
example the rule ships with.

The block is now a four-run TRANSCRIPT taken in one session at a named tree (superproject commit,
both submodule shas, `git status --porcelain` empty including untracked files, elisions stated). It
publishes the disagreement rather than hiding it: runs 3 and 4 print the identical `clean` line
from opposite git states, because `drift` compares on-disk artifacts with on-disk sources and never
invokes git. It also records ` m plugins` (modified content in the submodule) versus ` M plugins`
(a moved gitlink) as the discriminator for whose half of a two-repo cutover is still outstanding.

The quoted success line changes with it — `clean (400 artifact(s) on disk match their sources;
regeneration is a no-op)` — because the superproject cutover corrects the message itself: `drift`
never had a mechanism to establish anything about commit state, so it no longer claims one.

## The review round the block earned

A `pr-validator` held the first version on a defect of exactly the class the block exists to fix:
runs 3 and 4 **asserted** their state in `#` comments where the block's own banner promises a paste.
Run 3 claimed a regeneration whose `charly marketplace generate` appeared nowhere on the page, and
run 4 asserted `git status --porcelain` empty without running it — twenty lines after the same block
establishes that drift-clean and git-clean are *independent claims*.

The transcript is now one continuous six-run session in which **every state it asserts is a state it
runs**, reverts included, taken in a worktree checked out at the cited commit with nothing
uncommitted in it. Three additions came out of the same round, each measured rather than reasoned:

- **`(stale)` marks the ORPHAN and reads backwards** — the genuinely out-of-date artifact gets no
  suffix; the file projected by nothing gets it. Both directions print the same "are stale"
  headline, so the suffix is the only discriminator, and it names the rarer case.
- **The ` m` / ` M` submodule discriminator holds only under `--short`.** `--porcelain` collapses
  both states to ` M`, so the script-safe spelling is the one that loses the distinction.
- **`charly version` identifies the COMMIT a build was made at, never the bytes compiled.** The
  stamp comes from HEAD's timestamp by design, so a dirty-tree build reports its HEAD's CalVer — as
  the block's own first draft did.

## Landing order

This projection merges FIRST and the superproject PR then pins it: a pin needs a merged target, and
`docs/main` must be a fixed point over landed inputs. Regeneration on this tree is a no-op
(`charly marketplace drift` clean at 400 artifacts).

Placeholder CalVer — the `pr-validator` finalizes it at merge.
