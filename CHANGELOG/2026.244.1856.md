# 2026.244.1856 — chore: bump layer-charly-distros to v2026.244.1850 and regenerate the corpus (omarchy-eval skill)

## Summary

Bumps the `layer-charly-distros` refs pin to v2026.244.1850 (the tag from layer-charly-distros#3, which added the `omarchy-eval` skill) and regenerates the corpus. The regeneration emits the new `distros/skills/omarchy-eval/SKILL.md` — the omarchy PR evaluation procedure (the charly-driven pipeline for evaluating omacom/omarchy PRs on disposable beds).

This closes the S8 gap: the omarchy-eval skill was authored in distro-omarchy (distro-omarchy#21) but distro-omarchy is not a refs-list repo, so the skill never reached the corpus. Projected into the distros family owner (layer-charly-distros#3) and now emitted here.

## How tested

- `charly marketplace generate --root /tmp/marketplace --out /tmp/marketplace` — regenerated the corpus live; `git status --porcelain` shows exactly 2 changes: the pin bump + the new `distros/skills/omarchy-eval/SKILL.md` (105 lines, GENERATED banner, name=omarchy-eval)
- The bumped layer's manifest was validated live: layer-charly-distros#3's build job ran `charly box validate` at the pinned charly — PASS (0 warnings, 0 errors)
- The marketplace drift gate (deploy.yml) regenerates the ENTIRE corpus from this PR's refs list and fails if the committed corpus does not match — the gate is the full verification for this change class

## Rulebook compliance

- R3: no duplication — the skill entity lives in layer-charly-distros (the corpus owner); the corpus copy is generated
- R6: git safety — feat/ branch, no force-push, no direct main push
- PR-only landing per the org git-workflow; the refs bump + regenerated corpus ship in ONE PR (the drift gate requires it)

## Change classification

- Change class: docs/skill corpus (generated) + config (refs pin bump)
- Verification gate: regeneration ran live + layer manifest `charly box validate` PASS (layer-charly-distros#3) + the marketplace drift gate
- Attribution tier: analysed on a live system — the regeneration and the layer manifest validation ran live with retained output; the drift gate is the full gate for this class

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*