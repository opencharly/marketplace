# 2026.245.0949 — chore: add plugin-herdr + pod-herdr pins and regenerate (herdr + herdr-box skills)

## Summary

Add the two new herdr repos to the refs list and regenerate the corpus:

- `plugin-herdr/candy/plugin-herdr:v2026.245.0926` — the `herdr` skill
  (automation family): the compiled-in-capable `charly herdr` CLI + `herdr:`
  check verb, external placement.
- `pod-herdr:v2026.245.0944` — the `herdr-box` skill (automation family):
  the herdr stack box + check-herdr-pod R10 bed.

Regenerated corpus: `automation/skills/herdr/SKILL.md` (new),
`automation/skills/herdr-box/SKILL.md` (new).

## How tested

Pasted output:

```
$ charly marketplace generate --root . --out .     # exit 0
 M candy/charly-marketplace/charly.yml
?? automation/skills/herdr-box/
?? automation/skills/herdr/
```

```
$ git status --porcelain      # after a SECOND regeneration (drift-gate contract)
 M candy/charly-marketplace/charly.yml
?? automation/skills/herdr-box/
?? automation/skills/herdr/
$ git diff --stat
 candy/charly-marketplace/charly.yml | 2 ++
 1 file changed, 2 insertions(+)
```

The two generated SKILL.md files (new) carry the `herdr` / `herdr-box`
entities verbatim from the pinned repos.

## Rulebook compliance

- Refs bump ships its regenerated corpus in the SAME PR (drift gate) — verified
  by the second-run porcelain above.
- **R1** — the `herdr-box` family mismatch (not in the families map) was
  surfaced by the generator and fixed upstream (pod-herdr, family → automation)
  before this bump.
- **Pin discipline** — both pins are merged tags.

## Change classification

- Change class: chore (corpus refresh + regen)
- Verification gate: regeneration diff + drift-gate contract (pasted)
- Attribution tier: `analysed on a live system`

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*