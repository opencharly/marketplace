# 2026.206.1338 — plugins — R5 sweep: syntheticVmBox relocated to candy/plugin-bundle

CalVer finalized at merge by the fresh `pr-validator`.

## What changed

Core-min wave 3, K4 unit B (the charly-side box/candy compile-selection move):
`charly`'s host-core `syntheticVmBox` (and its sibling `syntheticHostBox`) were
deleted — the equivalent synthetic-box construction now lives plugin-side in
`candy/plugin-bundle/candy_select.go` (`syntheticVmBoxFromEnvelope` /
`buildVmSyntheticBox`), reading the resolved-project envelope's `distro:` tag
map instead of a live `*buildkit.DistroConfig`/`LoadUnified`.

- **`build/skills/build/SKILL.md`** — the `distro.<name>.version:` package-surface
  paragraph named `syntheticVmBox` directly as the consumer of the version-tag
  cascade; updated to name the current consumer (`candy/plugin-bundle/candy_select.go`'s
  `syntheticVmBoxFromEnvelope`/`buildVmSyntheticBox`) instead of the deleted
  charly-core function — an R1 documentation-divergence catch made during the
  charly-side R5 grep sweep for this cutover.

Docs-only leg (no code in this repo). See the `charly` superproject's own
CHANGELOG entry for this cutover's full narrative and R10 proof; the `sdk`
repo's matching leg carries the wire-contract extension
(`#DeployCompileRequest.candy_ref`/`vm_entity`/`box_ref`) this relocation rides on.

Assisted-by: Claude Code Anthropic Claude Sonnet 5 (documentation reviewed)
