# 2026.241.2106 — chore(refs): advance layer-charly-check to v2026.241.2052 + regenerate the corpus

## Summary

Advances the `layer-charly-check` ref in the refs list (`candy/charly-marketplace/charly.yml`) from `v2026.241.1216` to `v2026.241.2052` (the tag minted after layer-charly-check#2 fixed the stale beds-and-r10 roster) and regenerates the corpus so the drift gate stays green.

The regenerated `check/skills/check/references/beds-and-r10.md` now reflects the live roster: the main repo owns 33 beds (not 3), `check-local` is `check-local-vm`, and the box submodules own 52 distro beds.

## How tested

- `charly marketplace generate --root . --out .` (worktree binary) → exit 0, regenerated the corpus.
- `git diff --stat` → 2 files: the refs list (1 line) + the regenerated beds-and-r10.md (43 insertions, 19 deletions).
- The regenerated corpus matches the source entity: `grep 'check-local-vm' check/skills/check/references/beds-and-r10.md` → present; `grep 'check-local` | local' ` → absent.
- The deploy.yml drift gate (the SOLE OWNER of generation) will verify the committed corpus matches the pinned charly at CI time.

## Rulebook compliance

- R1: the stale roster was RCA'd (the reference claimed 3 main-repo beds; the live tree owns 33) and fixed in the owning source repo (layer-charly-check#2), then the corpus regenerated here.
- The refs list is the ONE input to generation (per deploy.yml); the corpus is generated output, never hand-edited.

## Change classification

- **Change class:** refs advance + regenerated corpus
- **Verification gate:** analysed on a live system (generation run live; the drift gate verifies at CI)
- **Attribution tier:** analysed on a live system

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*