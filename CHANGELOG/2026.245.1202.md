# 2026.245.1202 — chore(refs): bump layer-charly-distros to v2026.245.1157 + regenerate

## Summary of changes

Bump the corpus refs for `layer-charly-distros` from v2026.244.1915 to **v2026.245.1157** (its omarchy-eval skill entity was synced to opencharly/distro-omarchy#25) and regenerate the corpus with `charly marketplace generate`.

- `candy/charly-marketplace/charly.yml`: refs bump (the ONE generation input).
- `distros/skills/omarchy-eval/SKILL.md`: regenerated projection (template route + EXTERNAL/NON-AUTHORITATIVE disclaimer, channel lanes stable/rc/edge/dev, apply contract, snapshot lane, update-channel machinery; dangling-§ refs gone).

**Drift gate:** regeneration on the bumped refs reproduces exactly these two changed paths — the corpus is clean (`git status --porcelain` after regen shows only the refs bump + the omarchy-eval projection).

## Change class

**candy/box-config** — refs bump + regenerated corpus projection; no hand-edited content.

**Closes / relates to:** opencharly/layer-charly-distros#5 (skill source sync)

**Breaking change + rollback:** none — pure refs bump; revert restores the old pin (corpus regenerates identically).

## How R10-tested — exact gate, commands, and pasted output

- **The gate for this change class:** regeneration is the gate — the committed corpus must match what the pinned charly generates (deploy drift gate uses `git status --porcelain`).

```
$ charly/bin/charly marketplace generate --root . --out . > /tmp/marketplace-regen.log 2>&1; echo exit=$?
exit=0

$ git status --porcelain
 M candy/charly-marketplace/charly.yml
 M distros/skills/omarchy-eval/SKILL.md
# both committed with this PR — a clean re-run is a NO-OP (drift gate green)

$ grep -c 'Update-channel machinery' distros/skills/omarchy-eval/SKILL.md
1
$ grep -c 'PR-EVAL-TEMPLATE' distros/skills/omarchy-eval/SKILL.md
2
```

- **Fresh rebuild / changed code path executing:** N/A — corpus regeneration tooling, no runtime surface.
- **Concurrency:** N/A.

**Evidence blocks:**

*1. Fresh-rebuild gate output*: the regen command above (exit 0).

*2. Each changed code path executing*: N/A — generated projections, no code path.

*3. Docs-only evidence*: regen output + porcelain listing above; projection diff is text.

## Attribution tier

**analysed on a live system** — the regeneration ran live (exit 0, diff verified) and the drift-gate contract (regen reproduces the commit) is checked; not a code path but a live generator run.

## Harness rulebook compliance — state HOW each is satisfied (or `N/A — <reason>`)

- **R0 skills:** marketplace skill loaded (refs list = the one input; bump + regen ship together; drift gate contract).
- **R1 RCA + zero warnings:** regen exit 0, no warnings; the earlier dangling-§ content was fixed at the source (distro-omarchy#25 / layer-charly-distros#5).
- **R2 no out-of-scope:** refs bump + its projection only.
- **R3 no duplication:** single refs list, regenerated corpus.
- **R4 no workaround:** the canonical generator, not manual SKILL.md edits.
- **R5 hard cutover + grep:** N/A — no identifiers removed; projection regenerated wholly.
- **R6 git safety:** status clean before branch/commit; no destructive ops.
- **R7 runtime gate:** N/A — no runtime code path (projection regen).
- **R8 artifact invariants:** the regenerated corpus IS the artifact; drift gate verified (clean re-run = no diff).
- **R9 binary == source + deps:** the regen used the checkout-built charly (charly/bin/charly); no package/dep changes here.
- **R10 disposable + coverage:** N/A — no disposable target.
- **RDD / ADE / SDD:** N/A.
- **Hard cutover:** one atomic commit (bump + corpus).
- **Kernel/plugin boundary law:** N/A.
- **Disposable-only autonomy:** N/A.
- **Clean architecture + Go gates:** N/A — no Go.
- **CHANGELOG:** N/A — tag-on-merge writes it from this PR body.

---

*Assisted-by: pi ollama-cloud/deepseek-v4-flash:0731 (analysed on a live system)*