# 2026.261.2035 — feat(corpus): publish the docs skill's corrected corpus note

## Summary of changes

Regenerates the marketplace corpus for the corrected `docs` skill entity.

- `candy/charly-marketplace/charly.yml`: bumps `layer-charly-build` `v2026.261.1930` → **`v2026.261.2027`**.
- Regenerated `build/skills/docs/SKILL.md`: the stale "the PUBLISHED marketplace corpus is not internally consistent at its current HEAD … the marketplace regeneration has not landed the card" paragraph is replaced by the state-independent reason the bare-clone tests use fixtures.

**Closes / relates to:** `opencharly/layer-charly-build#13` (the source entity); surfaced by `opencharly/docs#127`.

**Breaking change + rollback:** `none` — a pin bump plus its generated projection. Revert the merge to roll back.

## Change class

`corpus` — a marketplace refs pin bump and the regenerated corpus it produces.

## How tested

The marketplace's own gate is `deploy.yml`'s drift gate (regeneration from the refs list must be a no-op on a clean tree). Reproduced with the pinned binary:

```console
$ /tmp/charly-marketplace marketplace generate --root "$PWD" --out "$PWD"     # run 1
$ /tmp/charly-marketplace marketplace generate --root "$PWD" --out "$PWD"     # run 2
$ git status --porcelain
 M build/skills/docs/SKILL.md
 M candy/charly-marketplace/charly.yml
```

The second run added nothing — the two files are exactly the pin plus its projection. The stale claim is gone:

```console
$ grep -c 'has not landed the card\|not internally consistent' build/skills/docs/SKILL.md
0
$ git diff --stat
 build/skills/docs/SKILL.md          | 12 ++++++------
 candy/charly-marketplace/charly.yml |  2 +-
```

The generated file keeps its `DO-NOT-EDIT` header; the source is the `layer-charly-build` entity, so this PR contains no hand edit.

## Change classification

- **Change class:** corpus/pin bump.
- **Verification gate:** the drift gate reproduced locally (regeneration is a no-op after the first run).
- **Attribution tier:** `fully tested and validated`.

## Harness rulebook compliance

- **R0 skills:** `/charly-build:docs`, `/charly-internals:git-workflow`.
- **R1 RCA + zero warnings:** the corpus lagged the merged source edit; fixed by the pin bump + regeneration.
- **R2 no out-of-scope:** the one pin and its generated file are the whole change.
- **R3 no duplication:** projected from the source entity, never hand-written here.
- **R4 no workaround:** the pin moves to the merged tag; the real generator produced the corpus.
- **R5 hard cutover + grep:** the greps above; the second generation is a no-op.
- **R6 git safety:** fresh branch off current `origin/main`; no force push, no hooks bypass, no direct push to `main`.
- **R7 runtime gate:** the generator ran twice; the second run is the no-op the drift gate asserts.
- **R8 artifact invariants:** the generated file keeps its `DO-NOT-EDIT` header and the corpus stays reproducible.
- **R9 binary == source + deps:** generation used the charly binary built from the tag the workflow pins.
- **R10 disposable + coverage:** `N/A — a corpus repo`; the drift gate is the shipped coverage.
- **RDD / ADE / SDD:** `N/A — corpus projection only`.
- **CHANGELOG:** written by `tag-on-merge` from this body.

---
*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (fully tested and validated)*