# 2026.213.1652 — #55 R1 follow-up: correct the `buildengine-prep` behavioral claim

Docs-only follow-up to 2026.213.1638 (#116, the R1 doc-divergence sweep across
plugins skills). That first sweep renamed the deleted `build-prep` host-builder
seam → `buildengine-prep` and corrected the deleted-seam *identifier* citations,
but left the DELETED fat seam's *behavioral* description attached to the new
`buildengine-prep` name — the R5 grep was keyed to the deleted seam NAMES, not
the behavioral CLAIM class, so the sibling survived the rename. Zero behavior
change — the Documentation-only change class.

## What was stale, and what's actually true now

- **`buildengine-prep` does NOT return the envelope + drive-model**
  (`build/skills/build`, `build/skills/generate`, `internals/skills/plugin`):
  the prose still claimed `Executor.HostBuild("buildengine-prep", …)` "→ the
  envelope + a serializable drive-model" and that `build:generate` calls it
  "with `GenerateOnly`". Neither is true. `hostBuildPrep`
  (`charly/host_build_buildengine.go`) only populates the render-seam-floor
  `renderGenCache` and returns an EMPTY `map[string]string{}` (no envelope, no
  drive-model); the resolved-project envelope + drive-model come from the
  plugin-side `resolveBuildEngine` reply (`spec.BuildResolveReply`,
  `candy/plugin-build/resolve.go`) — the plugin-side replacement for the
  DELETED host `build-prep` fat seam (`hostBuildBuildResolve` /
  `build_resolve_host.go`); and `generateOnly` is a PLUGIN-SIDE parameter on
  `spec.BuildResolveRequest` (NOT a `HostBuild("buildengine-prep")` argument —
  that host leg takes `spec.ResolvedProjectRequest`, which has no `GenerateOnly`
  field). The host keeps ONLY the thin `buildengine-*` shards + `render-seam`
  (the loader + resolve + envelope projection + drive-model moved plugin-side —
  K3 build-engine migration). The host-builder-kind enumeration in
  `internals/skills/plugin` is corrected to describe the thin shards
  `resolveBuildEngine` reaches, not the former fat seam.

## Verification

- Non-runtime standards only (Documentation-only change class): adversarial
  consistency review against the live tree (`charly/host_build_buildengine.go`
  `hostBuildPrep` returns an empty map; `candy/plugin-build/resolve.go`
  `resolveBuildEngine` returns `spec.BuildResolveReply`; `spec/spec/cue_types_gen.go`
  `ResolvedProjectRequest` has no `GenerateOnly` while `BuildResolveRequest` has
  it); R5 claim-keyed grep self-test — `git grep 'serializable drive-model'`
  and `git grep 'HostBuild("buildengine-prep") with'` return only this
  CHANGELOG's historical context + the now-accurate `resolveBuildEngine`-reply
  attribution (the false "buildengine-prep returns it" framing is gone);
  cross-reference validation (every corrected passage names the correct owning
  files); markdown integrity (no broken tables/links introduced).

Assisted-by: Claude Code glm-5.2:cloud (documentation reviewed)