# 2026.250.2011 — feat(corpus): skill-discipline wave — bump layer-charly-internals/-extra/check pins + regenerated skills

## Summary

Skill-discipline wave (Stage 2 of 2): bump the three skill-discipline layer pins in `candy/charly-marketplace/charly.yml` and carry the regenerated corpus, after the owning-layer PRs (tags below) updated their `skill:` entities. The skill edit and its regeneration are ONE cutover; the refs list pins the release tags, so the corpus is regenerated here after all three tags landed.

- **Pin bumps** (`candy/charly-marketplace/charly.yml` lines 22/23/25):
  - `@github.com/opencharly/layer-charly-internals:v2026.250.1222` → `v2026.250.1956` (PR #11 — "every delegated worker MUST read the relevant skill before its first tool call")
  - `@github.com/opencharly/layer-charly-internals-extra:v2026.246.0516` → `v2026.250.2006` (PR #4 — "R0x. Gate-before-push + no re-run against a root-caused defect + R10 never deferred")
  - `@github.com/opencharly/layer-charly-check:v2026.250.1402` → `v2026.250.1955` (PR #11 — "check live is VERIFY-ONLY, never an R10 gate")
- **Corpus projections** (regenerated with `charly marketplace generate`, binary from the local charly checkout):
  - `internals/skills/agents/SKILL.md` — +14 lines (delegated-worker-must-read-skill rule).
  - `internals/skills/strict-policy/SKILL.md` — +8 lines (gate-before-push / no re-run against root-caused defect / R10 never deferred).
  - `check/skills/check/SKILL.md` — +22 lines (MODE DISCIPLINE — `check live` is VERIFY-ONLY, never an R10 gate).
- Only these four files changed — regen on the rest of the corpus is a byte-identical no-op.

## How tested

Pasted outputs (branch `feat/skill-discipline-wave-2026-09-07`):

```text
$ git diff --cached --stat
 candy/charly-marketplace/charly.yml     |  6 +++---
 check/skills/check/SKILL.md             | 22 ++++++++++++++++++++++
 internals/skills/agents/SKILL.md        | 14 ++++++++++++++
 internals/skills/strict-policy/SKILL.md |  8 ++++++++
 4 files changed, 47 insertions(+), 3 deletions(-)

$ git diff --cached --check   →  no output (clean)

$ charly marketplace generate --root <this repo> --out <this repo>   (exact recipe; local charly checkout binary)
EXIT=0 — git status after regen shows ONLY the four paths above (M charly.yml + 3 M SKILL.md); no other drift.

# Directive greps in the GENERATED corpus (the three landings carry through):
$ grep -c "delegated worker MUST read the relevant skill" internals/skills/agents/SKILL.md
1
$ grep -c "R0x. Gate-before-push" internals/skills/strict-policy/SKILL.md
1
$ grep -cF "check live" check/skills/check/SKILL.md
12   ("MODE DISCIPLINE — `check live` is VERIFY-ONLY, never an R10 gate" present)
```

Generator reproducibility note (as documented in #290): the regen ran with a charly binary from the local charly checkout (schema 2026.249.2125 — equal to this repo's config schema). The CI-time charly pin in `.github/workflows/deploy.yml` / `refresh.yml` is `v2026.241.1243` whose max schema is 2026.240.1943, so the CI regenerate step errors before any comparison (`config schema ... is newer than this charly supports`) — the pre-existing repo-wide drift-gate breakage documented in #290, unrelated to this change. The pr-validator is the merge gate for this PR.

## Rulebook compliance

| Rule | How |
|---|---|
| R1 | Diff-scope RCA: the regenerated-tree audit shows exactly the four expected changes (three pins + three skill projections); nothing else moved (git status had no other files), proving the sources-driven generator reproduced the corpus byte-identically on the untouched families. |
| R2 | No conflict — one corpus per skill: each projection updated in place in its owning family directory (no overwrite of other skills). |
| R3 | No duplication — projections are generated from the layer `skill:` entities (single source); the regen is the ONLY path that writes corpus files (DO-NOT-EDIT headers). |
| R4 | No workarounds — no sleeps/retries/manual corpus edits; standard `charly marketplace generate` run. |
| R4a | Pin bumps are the org flow (refresh.yml does exactly this daily) — not hand-pins outside published tags; all three tags are tag-on-merge outputs of the merged owning-layer PRs. |
| R5 | Old content replaced with the change — the regenerated projections carry the new directives and supersede the previous skill text in the same PR. |
| R6 | Git safety — feat/ branch, PR-only landing, no force-push, no direct push to main. |
| R7 | Gate proof — regen EXIT=0 on the final tree; the deploy drift gate is currently broken repo-wide on main (stale CI charly pin, pre-existing, documented in How tested); the pr-validator runs on this PR as the merge gate. |
| R8 | N/A — no production data or deployed system state (corpus publication only). |
| R9 | N/A — no vendor/schema code changes; config refs list + generated artifacts only. |
| R10 | Fresh disposable proof — generated from the branch tree, verified from the committed index (git diff --cached). |
| RDD | Doc-driven — corpus content derives entirely from the layer skill entities (single source of truth). |
| B14 | Honest tiers — docs/corpus change (tier `analysed on a live system`); the deplove gate breakage is disclosed above rather than masked. |
| B18 | Scope discipline — no other pin advanced, no other family touched; the diff is exactly the three skill-discipline projections. |

## Change classification

- Change class: **docs/corpus regeneration + refs pin bump**.
- Verification gate: **analysed on a live system** — full `marketplace generate` EXIT=0 with the current charly binary on the branch tree, diff audit + directive greps pasted above; the repo's deploy drift gate is pre-existing broken at CI (stale pinned charly, documented in #290).
- Attribution tier: analysed on a live system.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*