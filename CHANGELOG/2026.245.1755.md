# 2026.245.1755 — chore: refresh the refs list (layer-nvidia v2026.245.1541, pod-herdr v2026.245.1113)

## Summary

Daily refs-list refresh (the `scripts/refresh-refs.sh` mechanism — the ONE sanctioned way pins advance) + regeneration check.

**Pins advanced (2):**
- `layer-nvidia` v2026.236.116 → **v2026.245.1541** (omarchy distro arm + arch-family robust checks — the layer landed those in v2026.245.1450..1541)
- `pod-herdr` v2026.245.0944 → **v2026.245.1113**

(`layer-charly-check` and `layer-wf-recorder` were already current in the refs list via prior refresh PRs.)

## How tested

```
REFRESH_DRY_RUN=1 bash scripts/refresh-refs.sh
refresh-refs: 0 pin(s) advanced, 0 unresolved   # (post-advance verification; 2 advanced in the write run)
```

```
charly -C <marketplace> marketplace generate --root <marketplace> --out <marketplace>
EXIT=0
```

Post-regeneration `git status --porcelain` = **1 entry: the refs-list bump itself** — the regenerated corpus is byte-identical to the committed corpus (the advanced repos' `skill:` entities are unchanged or already reflected; `layer-wf-recorder`'s PAC doc line landed via the earlier #274 refresh and is present in `selkies/skills/wf-recorder/SKILL.md`). The drift gate (deploy.yml regenerates + `git status --porcelain`) stays green: CI regeneration reproduces this tree.

## Rulebook compliance

- **R0:** marketplace + git-workflow skills loaded; refresh-refs.sh is the single sanctioned refs mechanism (R3 — no hand-pins).
- **R1:** no anomalies; the regen no-op was verified empirically (skill's own test: "regeneration on a clean tree is a NO-OP; a diff means a refs/source change is in flight").
- **R2:** refs list only; the regenerated corpus ships in the same PR (drift gate).
- **R4:** no workaround — the refresh script does not commit by design ("the caller does both"); this PR is the caller step.
- **R6:** branch off origin/main; no force-push; no direct main.
- **R7:** regeneration + porcelain are the gate evidence.
- **R10:** live execution (regen EXIT=0, porcelain=1, refs diff shown). Tier: analysed on a live system.

## Change classification

- **Change class:** refs-list refresh (chore) — 2 pins advanced; corpus regen verified no-op
- **Verification gate:** refresh-refs report + marketplace generate EXIT=0 + post-regen porcelain
- **Attribution tier:** analysed on a live system

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*