# 2026.245.1503 — chore(marketplace): bump layer-wf-recorder v2026.245.1357 + regen

## Summary

Bumps the `layer-wf-recorder` refs pin `v2026.240.0122` → `v2026.245.1357` (the merged tag carrying the arch/omarchy package mapping + the updated skill entity) and regenerates the corpus — the `selkies/skills/wf-recorder/SKILL.md` diff reflects the layer's new content (PAC package line, arch/omarchy mapping note).

## How tested

```
$ charly marketplace generate --root . --out .    # on the bumped refs list
$ git status --porcelain
 M candy/charly-marketplace/charly.yml
 M selkies/skills/wf-recorder/SKILL.md            # the only diff — generated corpus matches the new ref content
```

The deploy drift gate (regenerate + `git status --porcelain` empty on the committed corpus) is the marketplace repo's own workflow gate; this change is the refs-driver of exactly that single regenerated file.

## Rulebook compliance

- R1: corpus change via PR to the owning repo; regen is the sanctioned verb (marketplace skill). R3: no hand-edits — the SKILL.md diff is GENERATED from the layer's entity. No core/pins beyond the ref bump.

## Change classification

- **Change class:** corpus regen (refs bump + generated skill)
- **Verification gate:** regen produces exactly the expected diff; deploy drift gate is the repo workflow
- **Attribution tier:** analysed on a live system (the refs change is CONFIG: the pin bump + the regeneration ran live on this host, producing exactly the two-file diff; the deploy drift gate (regen + porcelain-empty) is the repo workflow's own gate)

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*