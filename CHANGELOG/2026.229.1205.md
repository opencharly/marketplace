# 2026.229.1205 — three states means three, and a transcript that leaves the tree as it found it

**This is a REGENERATION, not an authored edit.**
`internals/skills/git-workflow/references/evidence-and-freshness.md` carries the
`GENERATED … DO NOT EDIT` banner; its source is the `skill:` entity in
`candy/charly-internals/charly.yml` in `charly`, landing as `opencharly/charly#289`.

Four corrections, all found by the `docs#68` validator and its cold reader in text the previous
legs added. Each is the same shape — **a correction applied where it was flagged rather than
everywhere the claim is stated** — which is the rule this page states, failing on the page that
states it.

## The count that was corrected in one place and not the other

The section establishes that `git status` distinguishes **three** dirty-submodule states — ` ?`
untracked, ` m` modified, ` M` moved gitlink — and demonstrates all three. Two later sentences still
said **both**: *"Both letters also appear on ordinary paths"* where three letters had just been
defined, and *"`--porcelain` collapses both to ` M`"* when it collapses **all three**. The second
also understated the section's own point, since `--porcelain` loses more than it claimed.

## A transcript that did not leave the tree as it found it

The three-state paste ended with `git -C plugins checkout -q --detach HEAD~1` and stopped. On a page
that tells readers to retype its commands and whose six-run session pastes every revert, that left a
reader's submodule detached one commit behind its gitlink. The paste now ends by restoring the pin
and showing `git status --porcelain | wc -l` → `0`; the whole sequence was re-executed as published
before being republished.

## Distances that the page's own rule condemns

The *put the safeguard where the reader acts* passage cited two offsets — `~700 lines`, `288 lines`
— measured inside the document that contains them. The rule this cutover added about counts applies
to them verbatim: **a line offset inside the document it measures is stale the next time that
document is edited**, which for this page has been every round. They are now stated as *far below*
and *sections away*, with the omission and its reason on the page.

## What is deliberately NOT fixed here

Nothing. The three members of `opencharly/charly#292` — this three-vs-both contradiction, the
`every figure below` quantifier scope, and the self-announcing-placeholder option space — were
batched on the reasoning that fixing one buys a projection chain and leaves two. This leg had to run
regardless, so **the chain that was bought closes the file rather than decrementing it**: all three
are fixed here and `#292` can close.

## Scope

Regeneration is a no-op (`charly marketplace drift` clean at 400 artifacts) and the page moves
`+14/-7` against `main` at `cc22d9d1`.

**The commit is narrower than the regeneration, deliberately.** Regenerating at this base also
deletes `ollama/skills/ollama-cli/SKILL.md` and reverts two sibling skills, whose superproject
source is on no branch and in no open PR — the orphaned producer recorded as
`opencharly/charly#291`. They are reverted and left alone. **A clean whole-tree `git status` is not
available to this leg**, and reaching for it is how a regeneration silently deletes another
cutover's landed work.

Placeholder CalVer — the `pr-validator` finalizes it at merge and rewrites this H1 to match.
**Verify that rewrite by reading the committed object**, never the working tree.
