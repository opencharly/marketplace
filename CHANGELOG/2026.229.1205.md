# 2026.229.1205 — three states means three, and a transcript that leaves the tree as it found it

**This is a REGENERATION, not an authored edit.**
`internals/skills/git-workflow/references/evidence-and-freshness.md` carries the
`GENERATED … DO NOT EDIT` banner; its source is the `skill:` entity in
`candy/charly-internals/charly.yml` in `charly`, landing as `opencharly/charly#289`.

Four corrections, all found by the `docs#68` validator and its cold reader in text the previous
legs added. Each is the same shape — **a correction applied where it was flagged rather than
everywhere the claim is stated** — which is the rule this page states, failing on the page that
states it.

## The count that was corrected in one place and not the other

The section establishes that `git status` distinguishes **three** dirty-submodule states — ` ?`
untracked, ` m` modified, ` M` moved gitlink — and demonstrates all three. Two later sentences still
said **both**: *"Both letters also appear on ordinary paths"* where three letters had just been
defined, and *"`--porcelain` collapses both to ` M`"* when it collapses **all three**. The second
also understated the section's own point, since `--porcelain` loses more than it claimed.

## A transcript that did not leave the tree as it found it

The three-state paste ended with `git -C plugins checkout -q --detach HEAD~1` and stopped. On a page
that tells readers to retype its commands and whose six-run session pastes every revert, that left a
reader's submodule detached one commit behind its gitlink. The paste now ends by restoring the pin
and showing `git status --porcelain | wc -l` → `0`; the whole sequence was re-executed as published
before being republished.

## Distances that the page's own rule condemns

The *put the safeguard where the reader acts* passage cited two offsets — `~700 lines`, `288 lines`
— measured inside the document that contains them. The rule this cutover added about counts applies
to them verbatim: **a line offset inside the document it measures is stale the next time that
document is edited**, which for this page has been every round. They are now stated as *far below*
and *sections away*, with the omission and its reason on the page.

## The batch, closed — including the two members a first draft of this entry only claimed to close

The three members of `opencharly/charly#292` were batched on the reasoning that fixing one buys a
projection chain and leaves two. This leg had to run regardless, so **the chain that was bought
closes the file rather than decrementing it.**

- the three-vs-both contradiction (above);
- the **`every figure below` quantifier**, which reached past its own `###` boundary to figures it
  never measured — now scoped to *this section*;
- the **self-announcing-placeholder option space**. The argument said such a sentinel is impossible
  because `UNSTAMPED-<slug>.md` fails the filename regex. True of that shape — but a stamp that is
  well-formed and impossible as a wall clock (`2026.229.9998.md`) is both self-announcing and
  regex-valid, and is already in live use here. The paragraph now says so, and says why it still
  does not help: **nothing rejects it**, so a leg skipping Phase 3 still merges a guessed filename,
  merely a conspicuous one. The post-merge check fails the merge; a convention only fails the eye.

**And the argument gained a correction of its own, from the validator that merged the previous
leg.** The first draft said the post-merge check is the fix where a convention is not. That check
compares the landed entry against the version MINTED — but it **lives inside Phase 3**, and nothing
inside a phase detects that phase being skipped. The invariant that survives a skipped Phase 3 is
external to it: **an entry on `main` with no matching `v<CalVer>` tag**, checkable by anyone at any
time without knowing what was minted. That is what catches the **plausible** placeholder — the
dangerous kind, where filename and H1 agree because both are the guess — while a conspicuous one is
caught by the reader's eye.

**And the invariant needed a scope clause, which a first measurement of it did not have.** Checking
the last 25 entries gave 25 for 25 and read as a property of the repo. Across all 203: **66 carry no
tag**, every one at or before `2026.215.1207`, with the earliest tagged entry at `2026.193.1934` —
inside that stretch, so the two regimes interleave. From `2026.215.1207` forward it is **67 for 67**.
Tagging every landing is a recent convention, and a check written without the clause returns 66 false
positives on history it was never about. **That is the wrong-population failure twice in one file in
one day** — the same shape as an H1 sweep that flagged 28 entries of which 25 predated the rule.
The page now carries the scope, and the converse caveat: 3 tags have no entry, which is not a defect
but fires any symmetric check.

Note for anyone auditing the batch: **that third member is not in this page.** It lives in the
`pr-validator` agent entity of the same candy and projects to `internals/agents/pr-validator.md`,
which is why this regeneration moves two files. `#292` said otherwise and is corrected.

**Three drafts of this entry made claims about states they had not read**: that all three batch
members were fixed when only the first was, that `drift` printed clean when it printed three stale
artifacts, and — in the commit message that shipped the second correction — that this entry had been
updated when only the page had. All three are the same statement
about the state I intended rather than the state that existed, caught by the `docs#68` validator
before it merged. It is named here because that is the defect this whole cutover exists to retire,
committed in the entry recording its own closure.

## Scope

**The gate for this leg is scoped byte-identity, and an earlier draft of this entry cited the wrong
one.** It claimed *"regeneration is a no-op — `charly marketplace drift` clean at 400 artifacts"*. At
this base `drift` reports **three stale artifacts**, all of them the orphaned ollama projection
described below; it cannot print `clean` from this source. The draft had read a working tree
carrying those sources uncommitted rather than the tree under review — **the vacuous-gate shape this
very page teaches: sound logic, wrong input.**

What holds instead is that each artifact this leg touches is byte-identical to `banner + entity
content` from the source, which is checkable without running the generator at all — and is therefore
the only gate that stays valid while a foreign projection sits in the base.

Against `main` at `cc22d9d1`: the reference page moves `+15/-8`, and
`internals/agents/pr-validator.md` moves `+36/-6`.

**The commit is narrower than the regeneration, deliberately.** Regenerating at this base also
deletes `ollama/skills/ollama-cli/SKILL.md` and reverts two sibling skills, whose superproject
source is on no branch and in no open PR — the orphaned producer recorded as
`opencharly/charly#291`. They are reverted and left alone. **A clean whole-tree `git status` is not
available to this leg**, and reaching for it is how a regeneration silently deletes another
cutover's landed work.

Placeholder CalVer — the `pr-validator` finalizes it at merge and rewrites this H1 to match.
**Verify that rewrite by reading the committed object**, never the working tree.
