# 2026.227.1107 — regenerate the charly-internals skills that #276's source change left stale

`opencharly/charly#276` ("sync the charly-internals candy skill content") edited
`candy/charly-internals/charly.yml` — a `skill:` source — and merged without a
companion regeneration here. That left superproject `main` with its source
running AHEAD of its generated tree: `charly marketplace drift` exits 1 on

- `internals/skills/git-workflow/references/multi-repo-coordination.md`
- `internals/skills/go/SKILL.md`

This is the projection catching up. Generated, never hand-edited; the diff is
exactly the two artifacts the drift gate names, and nothing else.

The failure class is the one the unit-4 pair spent six rounds on, arriving from
the opposite direction. There, a generated tree landed AHEAD of its source, so
the next regeneration silently reverted it. Here, a source landed ahead of its
projection, so the drift gate goes red for everyone until someone regenerates.
Both are the same underlying rule: a `skill:` source edit and its regeneration
are one cutover across two repos, and landing either half alone leaves `main`
inconsistent.

## Provenance of the evidence, and a correction

The runs that produced these two artifacts were executed against
`opencharly/charly#274`'s branch (`1ae04b4f` — superproject `main` merged, PLUS
#274's unmerged source), with `plugins` at `f6f39bbd`. An earlier revision of
this entry labelled them as having run at `main fff7bd06`. They did not, and the
difference matters:

- At `#274`'s branch, drift reports exactly these TWO artifacts.
- At real `main fff7bd06`, drift reports **SIX**. The four extras are the helm
  family (`.claude-plugin/marketplace.json`, `kubernetes/.claude-plugin/plugin.json`,
  `kubernetes/.codex-plugin/plugin.json`, `kubernetes/skills/helm/SKILL.md`),
  because the `helm-skill:` entity does not exist on `main` — it is inside
  unmerged #274.

**So regenerating at `main` would ALSO revert #168 and #169**: `helm/SKILL.md`
deleted, three JSON manifests rewritten. This commit escapes that only because
two files were committed by hand rather than the whole regenerated set. Anyone
following the earlier recipe literally would ship the revert — in the entry
written to warn about that exact failure. Corrected here rather than left to be
rediscovered.

**This PR does not, on its own, make `main` green.** After it merges, `main` is
still stale on the four helm artifacts until #274 lands its source. The verified
clean end state is superproject `1ae04b4f` + `plugins` at this PR's head →
`marketplace drift: clean (399 artifact(s))`. #276, #168/#169 and #274 are one
logical cutover split across landings, and `main` is transiently red in between
whichever order the halves take; they close it together, not separately.

Documentation-only change class. Placeholder CalVer — the pr-validator finalizes
it at merge.
