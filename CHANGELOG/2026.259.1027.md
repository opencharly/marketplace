# 2026.259.1027 — chore(refs): bump eval-omarchy to v2026.259.1014 + regenerate its distros skills

## Summary

Bump the `eval-omarchy` ref in `candy/charly-marketplace/charly.yml` from `v2026.257.0900` to **`v2026.259.1014`** (opencharly/eval-omarchy#78, merged + tagged), and ship the regenerated distros corpus in the same commit as the drift gate requires.

eval-omarchy#78 rewrote the lane's skills so the PR-eval record is emitted **schema-first** (the new `emit` stage) and graded by the **org pr-validator** criteria (with the lane running the R10 the pr-validator would otherwise assume). The ref bump makes those rewritten skills reach consumers of `marketplace/distros/skills`.

## How tested

**1. The pinned ref is a merged, tagged default-branch HEAD:**

```
$ git ls-remote --tags https://github.com/opencharly/eval-omarchy.git | grep 259.1014
25b22dcfeaaba3b3fffb9ce4733a96c61542f79c	refs/tags/v2026.259.1014
$ git -C eval-omarchy log --oneline origin/main -1
caf4ee7 docs: add CHANGELOG 2026.259.1014 for PR #78
```

**2. Regeneration (`charly marketplace generate`) changes EXACTLY the eval-omarchy projections** — the refs line + the 4 rewritten/deleted skills, nothing else:

```
$ git status --short
 M candy/charly-marketplace/charly.yml
 M distros/skills/omarchy-eval-cold-reader/SKILL.md
 D distros/skills/omarchy-eval-full-loop/SKILL.md
 D distros/skills/omarchy-eval-golden/SKILL.md
 D distros/skills/omarchy-eval-media/SKILL.md
 M distros/skills/omarchy-eval-oracle/SKILL.md
 D distros/skills/omarchy-eval-sequencing/SKILL.md
 D distros/skills/omarchy-eval-tests/SKILL.md
 D distros/skills/omarchy-eval-tiers/SKILL.md
 D distros/skills/omarchy-eval-work-lane/SKILL.md
 M distros/skills/omarchy-eval/SKILL.md
```

**3. Every deletion is a genuine orphan.** The 7 deleted skills have NO source `skill:` entity in the merged eval-omarchy (their v1 names were consolidated into the entry/oracle/cold-reader trio):

```
$ for f in omarchy-eval-full-loop omarchy-eval-golden omarchy-eval-media omarchy-eval-sequencing omarchy-eval-tests omarchy-eval-tiers omarchy-eval-work-lane; do
    echo -n "$f: "; git -C eval-omarchy show origin/main:candy/eval-pr/charly.yml | grep -c "name: $f"; done
omarchy-eval-full-loop: 0
omarchy-eval-golden: 0
omarchy-eval-media: 0
omarchy-eval-sequencing: 0
omarchy-eval-tests: 0
omarchy-eval-tiers: 0
omarchy-eval-work-lane: 0
```

So deleting them is the R5 sweep, not an accidental revert. The 4 modified skills carry the merged source verbatim (e.g. the new applicability map appears in the generated `omarchy-eval/SKILL.md`).

**4. Regeneration is the drift gate's own operation** (its deploy workflow re-runs `charly marketplace generate` and fails on any diff); the corpus committed here IS that output, so the gate is satisfied by construction.

## Rulebook compliance

- **R1** — the ref bump's source is a merged+tagged commit; the deletions are root-caused (orphaned v1 skill entities, verified 0-source above), not assumed.
- **R2/R5** — the ref bump and its regenerated corpus land as ONE commit (the drift gate requires it); the orphaned siblings are removed in the same change.
- **R3** — the corpus is the generated projection of eval-omarchy's `skill:` entities; no hand-edit.
- **R4** — a merged CalVer tag, never a branch.
- **R6** — PR-only; add-only.
- **R7** — the commands and outputs above.
- **R10** — the change class is generated-corpus + a ref pin; the marketplace deploy workflow's drift gate is the runtime equivalent, and the regeneration is its exact operation.

## Change classification

- Change class: refs-list pin bump + regenerated corpus (generated artifacts).
- Verification gate: the pinned tag is merged, and regeneration changes only the eval-omarchy projections (11 files) with orphan-verified deletions.
- Attribution tier: analysed on a live system — the generation ran live and its output is committed; the drift gate is the marketplace's own CI.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (analysed on a live system)*