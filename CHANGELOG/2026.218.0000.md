# 2026.218.0000 — K-wave 2: the R5 follow-up sweep (accurate repoints)

The plugins R5 follow-up for the K-wave 2 residue-ledger program. The super
pr-validator found the prior sweep's repoints INACCURATE — several named
wrong/nonexistent homes and the sweep was incomplete. This entry corrects every
repoint against the actual tree and completes the whole-tree grep.

- writeContextIgnore / baselineContextIgnore: repointed to
  candy/plugin-build/host_prep.go (`writeContextIgnore`) + charly/host_build_buildengine.go
  (`baselineContextIgnore`) — NOT sdk/deploykit/generate.go.
- remote-cache auto-migration: the seam is `migrateCacheViaPeer`
  (sdk/loaderkit/refs_seams_executor.go) — `RunProjectMigrations` does not exist.
- reconcile: `charly box reconcile` is candy/plugin-box/reconcile.go's
  `dispatchReconcile`; `IsRemoteCandyRef`/`StripVersion` live in
  sdk/deploykit/candy_ref.go (NOT sdk/kit/remote_ref.go, which re-exports
  ParsedRef/ParseRemoteRef/IsRemoteImageRef/StripURLScheme/ResolveBoxName);
  `GitLatestTag`/`RepoGitURL`/`CompareSemver` live in spec/refs/git.go;
  `ImageReconcileCmd`/`compareSemver` do not exist.
- loader populator: `populateCandyFromYAML` was ported to
  sdk/loaderkit/scan_candy.go (`ScanCandyManifest`/`ScanInlineCandy` →
  `scanFromParsed`) + sdk/deploykit/spec_candy_adapter.go — NOT load_unified.go.
- sidecar: types/merge/resolution live in candy/plugin-deploy-pod/sidecar_resolve.go
  (`spec.ResolvedSidecar`/`deploykit.ResolvedSidecar`) + config_setup*.go;
  sidecar_embedded.go holds only the embedded template library.
- Sweep completion: every deleted charly/*.go identifier repointed claim-keyed
  across the tree (unified loader → sdk/loaderkit, config/format_config →
  sdk/buildkit + spec, envfile → spec/hostenv, security/ports/quadlet →
  sdk/deploykit, deploy/provides → candy/plugin-deploy-pod + sdk/deploykit,
  build → candy/plugin-build, vm → candy/plugin-vm + sdk/deploykit +
  spec/sshx, k8s → candy/plugin-kube/k8sgen + spec, mcp → candy/plugin-mcp,
  status → candy/plugin-status/substrate, settings → candy/plugin-settings,
  feature/check → candy/plugin-check + sdk/deploykit, shell_profile →
  sdk/kit/profile.go, tunnel → sdk/deploykit/tunnel_resolve.go +
  candy/plugin-tunnel). Every repoint verified against the super branch's tree
  (the claimed home exists and holds the named symbol); genuinely-deleted
  symbols are marked DELETED-context.

CalVer finalized at merge by the fresh pr-validator.
