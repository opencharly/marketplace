# 2026.218.0000 — the bundle→fleet rename across the skill corpus

The `charly bundle` CLI word, command:bundle→command:fleet, plugin-bundle→
plugin-fleet, and BundleNode/BundleConfig→FleetNode/FleetConfig are renamed
across the skills corpus, following the superproject README's canonical
glossary. The informal 'collection of tools' senses become 'set'/'collection';
'full YAML documents' replaces 'bundle files'; stale source-file references
(bundle_add_cmd.go/bundle_members.go) updated. Migration help-text, the
bundled/bundles verbs, and the versa JS/npm 'bundle' sense stay. Placeholder
CalVer — the pr-validator finalizes it at merge.
