# 2026.218.0000 — K-wave 2: the residue-ledger skill sweeps

The plugins docs leg of the K-wave 2 residue-ledger program: the R1 claim-keyed
skill sweeps for every relocated subsystem. The program (super CHANGELOG
2026.218.0000) drove charly/ core to the kernel/plugin boundary law's end state,
measured by the per-file residue ledger (56/56 satisfied, tools/ledger-validate
PASS).

- egress: the egress-shim home repointed to candy/plugin-bundle/egress.go.
- install-plan: the host-build list drops the deleted seams (deploy-from-box,
  deploy-del-resolve, config-resolve, feature, retention-defaults, check-run,
  pod-config-setup/remove, arbiter-bracket); the check_image_preflight reference
  repointed to candy/plugin-check/preflight_images.go.
- source-map: the deleted charly/*.go rows marked DELETED with their new homes
  (enc → candy/plugin-pod/enc_cmd.go, devices → detect_devices.go, gpu_shim →
  the GPU-detection exception, checkspec/check_cmd/checkrun rows updated).
- local-infra, vm-deploy-target, check, clean, feature, kubernetes,
  authoring-recipes, boundary-law, local-spec, enc: the relocated homes named.

Placeholder CalVer — the pr-validator finalizes it at merge.
