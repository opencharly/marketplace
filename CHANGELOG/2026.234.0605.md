# 2026.234.0605 — spec de-submodule: regenerated skill pages
<!-- auto-merge-finalized: 2026.234.0605 -->

Regeneration from the charly superproject's de-submodule cutover
(opencharly/charly#371): the spec contract module resolves from the module proxy at the pinned require
version (v0.2026232.520, the spec strict-cleanup contract) — no submodule, no
local checkout. The 19-page regeneration covers the de-submodule claims AND the
strict-cleanup adoption claims (install_template removal, StepContract
constants, packaging migration, schema 2026.232.0520 re-stamp).

Updated (generated from `candy/charly-internals` + `candy/charly-build`):
- `internals/skills/go/references/source-map.md` — spec resolves from the
  module proxy at the pinned require version; `charly/go.mod` carries no
  `replace`; `go.work` has no spec entry; pluginsgen guards only the sdk
  submodule.
- `internals/skills/go/references/schema-change-recipe.md` — the schema-version
  bump stays cross-repo: the spec repo lands + tags first, this repo bumps the
  `github.com/opencharly/spec` require version (one shared pin, canonical gate)
  and appends the migrations entry.
- `build/skills/migrate/SKILL.md` — "Adding a future cutover" consumes the
  published module at the bumped require version.
- `internals/skills/egress/SKILL.md` — the superproject `task cue:gen`
  regenerates only the plugin params (spec types are generated in the spec
  repo).

Documentation-only: every page is a byte-perfect regeneration from the candy
sources.