# 2026.230.0039 — the section partition closes

**This is a REGENERATION, not an authored edit.** The file carries
`GENERATED by `charly marketplace generate` — DO NOT EDIT`; its source is the `skill:` entity in
`candy/charly-internals/charly.yml`, landing as `opencharly/charly#289`.

## What arrives

`internals/skills/git-workflow/references/evidence-and-freshness.md` — the page's section partition
now covers the page.

It has **13** `###` sections. The epistemic grouping names eight, in a table of eight nouns over
seven sections plus an explicit carve-out for the one deliberately absent. The mechanism grouping
named **four** in a closed list. Eight and four is twelve, and the thirteenth — *"A guard that cannot
fail is worse than no guard"* — was in neither.

**A closed enumeration makes that false rather than incomplete.** A count that leaves room for
unnamed members merely undercounts; a list that names its members asserts *these are all of them*.
Both groupings name theirs, so the page stated a trichotomy in which one branch was wrong. The
mechanism list now names five, and the guard section is one of them — mechanism by the page's own
test, since its vacuity finding is establishable only by running the guard.

## Why this is a leg of its own

The page's total was corrected once before, in `plugins#199`, which spent nine rounds and one full
revert on this family. **This correction is a single clause and it still needs a leg**, because a
generated file's fix is not landed until the projection carries it: the source and its generated
copies are one cutover, not two.

**Scoped to one file, deliberately.** A whole-tree regeneration at this base moves **fifteen** files,
and **fourteen of them revert `plugins#198`'s landed prose** — its projection is ahead of its
superproject leg, which is open as `charly#297`. Only the file this correction owns is committed.

Placeholder CalVer — the `pr-validator` finalizes it at merge and rewrites this H1 to match.
**Verify that rewrite by reading the committed object** (`git show <ref>:<path>`), never the working
tree.
