# 2026.234.2327 — docs: sweep remaining sdk-submodule claims from the git-workflow + go skills

## Summary

Follow-up sweep to the sdk de-submodule corpus regeneration (plugins#224): three more skill pages described `sdk/` as a submodule, a gitlink bump, and a `replace => ../sdk` resolution. All regenerated to the proxy reality — the sdk contract module resolves from the module proxy at the shared pinned require version (v0.2026234.347); no gitlink, no replace; schema cutovers adopt a tagged sdk release as the new shared pin; the go skill's project-layout tree no longer shows a checked-out `sdk/` directory.

- `internals/skills/git-workflow/SKILL.md` — frontmatter description ("sdk/plugins submodules" → "plugins/docs/box submodules, and the proxy-resolved sdk contract module")
- `internals/skills/git-workflow/references/multi-repo-coordination.md` — B2 step 0 ("submodule `sdk/`" → "NOT a submodule; consumed from the module proxy at the pinned require version") + step 3 ("sdk gitlink bump PLUS replace" → "adopt the tagged sdk release as the new shared pinned require")
- `internals/skills/go/SKILL.md` — project-layout tree drops the checked-out `sdk/` subtree; submodule convention states the sdk contract module is NOT a submodule

Generated deterministically by `charly marketplace generate` from the charly-internals candy entities (edited in the charly sdk de-submodule cutover PR, opencharly/charly#377).

## How tested

**Regeneration — executed (charly worktree, plugins submodule at the merged #224 head):**

```
$ ./bin/charly marketplace generate --root <charly-worktree>
exit=0
$ git -C plugins status --short
 M internals/skills/git-workflow/SKILL.md
 M internals/skills/git-workflow/references/multi-repo-coordination.md
 M internals/skills/go/SKILL.md
```

(parallel-bed-testing.md regenerates from pre-registered drift — reverted on this branch.)

**Corpus sweep — executed, zero surviving claims:**

```
$ grep -rln "sdk submodule|sdk/ submodule|use ./sdk|update --init sdk|bump the sdk submodule|sdk gitlink|=> ../../sdk|=> ../sdk|submodule .sdk.|Git submodule (github.com/opencharly/sdk)" --include="*.md" .
→ only the CHANGELOG/ history + package-path prose (sdk/kit etc.) remain
```

## Rulebook compliance

| Rule | How / N/A |
|---|---|
| R0 | git-workflow, cutover-policy loaded |
| R1 | the survivor claims were missed by the first corpus sweep (plugins#224 scoped to the 3 pages the entity edits touched); caught by the docs leg validator — RCA'd to the R5 sweep excluding `plugins/`; fixed in this leg |
| R2 | whole sweep in one PR |
| R3 | regeneration only — entities in charly#377 are the single source |
| R4 | N/A — no runtime surface |
| R5 | every remaining sdk-submodule claim in the corpus swept; executed grep above |
| R7 | regeneration + sweep outputs pasted |
| R10 | docs-only class — non-runtime standards |

## Change classification

- **Change class**: documentation-only (3 generated skill pages, 15+/14−)
- **Verification gate**: non-runtime standards; regeneration no-op verified
- **Attribution tier**: documentation reviewed
- **Landing**: fresh `charly/pr-validator`; tag-on-merge

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (documentation reviewed)*