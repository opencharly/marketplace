# 2026.230.1808 — `/charly-build:load` and `/charly-distros:nested-podman-socket`

Generated from the candy `skill:` sources by `charly marketplace generate`, together with
their two R0 dispatcher rows.

- **`/charly-build:load`** — `charly box load`, the container twin of `charly vm cp-box`:
  what it solves (a pod's nested store is genuinely separate, and neither a host-socket
  bind nor a registry hop is an acceptable way in), the socket prerequisite, the strict
  short-name resolution, and why the transfer is verified rather than trusted to the load
  exit code.
- **`/charly-distros:nested-podman-socket`** — why serving the socket is its own candy
  rather than part of `container-nesting`, and why the `/run/user/1000` named volume is
  load-bearing (a tmpfs cannot be uid-1000-owned: podman's `--tmpfs` rejects `uid=`).

Also regenerated: `/charly-image:layer`'s security example, which documented
`security.mounts:` where the schema field is `mount:` — the example could not have worked
if copied.
