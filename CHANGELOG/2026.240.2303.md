# 2026.240.2303 — docs: regenerate the corpus for the git-workflow reality fix

## Summary

The regenerated projection of `opencharly/charly#429`, which corrects five claims the
`git-workflow` skill made about the landing machinery that do not match the actual
`opencharly/.github` repo or the live repos.

Produced by `charly marketplace generate` — **no hand edits**; every file here carries a
DO-NOT-EDIT banner and is a projection of a candy's `skill:` entity.

## What the corrections are

| # | Was | Is |
|---|---|---|
| 1 | "feeds the `auto-merge` workflow that merges (squash) and CalVer-tags" | there is **no** `auto-merge` workflow — `pr-validator.yml` enables GitHub's own native auto-merge inline; `tag-on-merge` writes the tag + CHANGELOG |
| 2 | "GitHub branch protection (`enforce_admins`)" | a per-repo **ruleset**; the legacy API is unused and `branch-protection.sh` deletes it |
| 3 | "the `pre-push-gate` … is NOT wired under Claude Code" | it **is**, via `.claude/settings.json` PreToolUse hooks |
| 4 | the required check is `charly/pr-validator` | the **workflow** is; the **check** is `validate / validate` |
| 5 | corpus paths under `plugins/` | the corpus is `marketplace/` |

Plus four skill `Source:` paths the candy de-submodule invalidated, and two stale family names
(`charly-core`/`charly-build` → `core`/`build`).

Each is evidenced in `opencharly/charly#429`; the audit belongs there because that is where the
source lives.

## How tested

```
$ charly marketplace generate --root ../charly --out .
$ git status --porcelain | wc -l
19
```

**19 files, and nothing else moved.** That second fact matters twice: it shows the diff is only
these corrections, and it independently confirms the corpus was already in sync with the candies
before this change — a drifted corpus would have shown up here as unrelated churn.

Diff composition:

```
added lines containing marketplace/ : 37
removed lines containing plugins/   : 37
lines mentioning neither            : 13    ← exactly the three prose corrections
```

Symmetric, with no third category — so no path was rewritten into something new and no unrelated
content changed.

Every rewritten literal path was verified to resolve under `marketplace/` before regeneration
(19 resolve, 0 do not), and the **runtime** directory `/usr/lib/charly/plugins/` — a different
thing entirely — was excluded by construction rather than by luck.

## Rulebook compliance

- **R0** — `/charly-internals:git-workflow` and `/charly-internals:skills` loaded; the latter is
  what specifies that edits go to the candy entity and the corpus is regenerated, which is the
  shape of this pair.
- **R1** — no anomaly here; the underlying claims were verified in the source PR.
- **R2** — nothing deferred.
- **R3** — this repo holds **no** independent copy: it is a projection, regenerated, never
  hand-edited.
- **R5** — the stale paths are replaced, not annotated.
- **R6** — `git status` checked; branch off fresh `main`; no force-push, no bypass.
- **R7** — the generator was run and its output audited; both pasted above.
- **R8** — this PR *is* the emitted artifact.
- **R9/R10** — N/A: a generated documentation corpus. No image, no bed.

## Change classification

- **Class:** generated documentation corpus (projection).
- **Verification gate:** regeneration + diff composition audit + path-resolution check.
- **Attribution tier:** `analysed on a live system` — the generator ran live against the edited
  source with retained output.

## Ordering

Land after `opencharly/charly#429` (the source). Landing this first would make the corpus
describe candies that do not yet say it, and the next regeneration would revert it.

*Assisted-by: Claude Code Anthropic Opus 5 (analysed on a live system)*