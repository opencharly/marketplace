# 2026.213.1638 — #55 R1 doc-divergence sweep across plugins skills

Docs-only sweep correcting every plugins SKILL.md that described a deleted /
renamed HostBuild seam or a stale host-builder count as still live. This is the
plugins half of the R1 doc-divergence sweep (the spec half deleted the dead
wire types; the sdk + super halves swept the comment leg). Zero behavior change
— the Documentation-only change class.

## What was stale, and what's actually true now

- **`build-prep` → `buildengine-prep`** (`build/skills/build`, `build/skills/generate`,
  `internals/skills/install-plan`, `internals/skills/plugin`): the former fat
  `build-prep` host-builder seam (`hostBuildBuildResolve` / `build_resolve_host.go`,
  DELETED) was split into the 8 `buildengine-*` verbs (`buildengine-prep` et al. in
  `host_build_buildengine.go`). Every `HostBuild("build-prep", …)` citation is
  corrected to `HostBuild("buildengine-prep", …)`; the `build:box` / `build:generate`
  dispatch prose is updated likewise.
- **`bake-plugins` seam DELETED** (`build/skills/build`, `internals/skills/go`,
  `internals/skills/plugin`): `bake_plugin:` binary baking is now INLINE via
  `deploykit.EmitBakedPlugins` (wired directly by `NewRenderGeneratorFromProject`),
  so the `HostBuild("bake-plugins")` round-trip, the `hostBuildBakePlugins`
  host-builder, and `host_build_bake_plugins.go` are DELETED. The skills no longer
  list `bake-plugins` among the host-builder kinds.
- **`resolved-project` seam DELETED** (`core/skills/charly-status`,
  `internals/skills/install-plan`, `internals/skills/plugin`): the resolved-project
  envelope is fetched via `InvokeProvider("build","project", OpResolve)` (K1-unblock
  wave 1), so the former `HostBuild("resolved-project")` seam is DELETED. The
  status-collector + host-coupled step-emit prose now names the `InvokeProvider` fetch.
- **`config-persist` DELETED** (`internals/skills/install-plan`,
  `internals/skills/plugin`): VM config persist moved plugin-side to
  `candy/plugin-vm/vm_host_persist.go`; the `config-persist` host-builder is gone
  (only `config-resolve` remains).
- **`vm-build` host-builder DELETED** (`internals/skills/go`): the VM-disk build
  ENGINE resolve moved into the plugin (`candy/plugin-vm/vm_build_resolve.go`); the
  former `charly/vm_build.go` `HostBuild("vm-build")` host-builder is DELETED.
- **Host-builder count 24 → ~54** (`internals/skills/install-plan`,
  `internals/skills/plugin`): the skills hardcoded "24 registered kinds"; the count
  drifts per cone, so both now cite the authoritative `git grep
  'registerHostBuilder(' charly/*.go` resolving the `*BuilderKind` constants, and
  enumerate the current kind families (`buildengine-*`, `loader-*`, `pod-*`,
  `pod-config-*`, `deploy-*-resolve`, `arbiter-bracket-*`, …) instead of a frozen
  list.
- **`deploy-add` / `deploy-del` seam renames** (`internals/skills/install-plan`,
  `internals/skills/plugin`): the deploy-add seam was renamed `resolve-target-add`;
  deploy-del was split into `deploy-del-resolve` (LIVE) + `deploy-node-del-dispatch`.
  The host-builder kind lists now name the current `deploy-*-resolve` /
  `deploy-members-*` / `resolve-target-add` / `deploy-node-del-dispatch` family.

Companion to the prior R1-sweep commits on this branch (`EnsureCharlyInVenue` →
`EnsureCharlyInDeployVenue` / `EnsureCharlyInGuest`, the stale `check_cmd.go`
gather-engine claim, the `checkrun.go` / `check_members.go` row rewrites) — all
one R1 doc-divergence theme, landing as one plugins PR.

## Verification

- Non-runtime standards only (Documentation-only change class): adversarial
  consistency review against the actual `charly/` + `candy/` + `sdk/` diffs on
  `feat/55-coneA-units23` (the deleted seams confirmed absent: `build-prep` host-builder,
  `host_build_bake_plugins.go`, `resolved-project` seam, `config-persist`, `vm-build`
  host-builder); R5 grep self-test — `git grep 'HostBuild("build-prep"\|HostBuild("bake-plugins"\|HostBuild("resolved-project"\|HostBuild("vm-build"\|"config-persist"'` in this repo returns zero hits (every
  named seam is corrected repo-wide, including in this very fix); cross-reference
  validation (every corrected passage still names the correct owning files);
  markdown integrity (no broken tables/links introduced).

Assisted-by: Claude Code glm-5.2:cloud (documentation reviewed)