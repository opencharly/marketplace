# 2026.246.1720 — chore(refs): bump layer-charly-check to v2026.246.1622 (record: gif skill docs)

## Summary

Bump the `layer-charly-check` refs pin from `v2026.246.0617` to `v2026.246.1622` in `candy/charly-marketplace/charly.yml` and regenerate the corpus.

Why: `v2026.246.1622` (opencharly/layer-charly-check#9, merged) documents the **`record: gif` method** in the record skill (the agg renderer for terminal recordings — the omarchy PR-validation evidence capability). The corpus was pinned below that, so the generated `check/skills/record/SKILL.md` lacked the gif method. This is the corpus-drift fix (the skill is GENERATED; the source entity was updated, the corpus was not regenerated).

## How tested

Regeneration with the marketplace verb (the drift gate's own command):

```
$ charly -C <marketplace> marketplace generate --root <marketplace> --out <marketplace>
(regenerated corpus — 2 files changed)
```

The regenerated `check/skills/record/SKILL.md` now documents the gif method:

```
| Render a GIF | `record: gif` + `artifact:` (+ optional `record_name:`/`theme:`/`font_size:`/`speed:`/`idle_time_limit:`/`fps_cap:`/`select:`/`cols:`/`rows:`/`no_loop:`/`last_frame_duration:`/`renderer:`) | Render a STOPPED terminal recording (.cast) to an animated GIF with agg and copy it to the host artifact path |
```

The diff is exactly the refs pin + the regenerated skill (no unrelated corpus drift).

## Rulebook compliance

- **R1** — RCA: the corpus is GENERATED from the refs list; the layer-charly-check pin (v2026.246.0617) predated the gif docs (v2026.246.1622), so the generated record skill lacked the gif method. Fixed by the pin bump + regeneration.
- **R2** — N/A: no cutover; a refs advance.
- **R3** — No duplication: the skill source stays in layer-charly-check; the corpus is regenerated, not hand-edited.
- **R4** — No workarounds: sanctioned refs bump + regeneration (the drift gate's own path).
- **R5** — N/A: no legacy path removed.
- **R6** — Git safety: feat/ branch, no force-push, no direct push to main.
- **R7** — Gate: regeneration is the drift gate's command; the committed corpus now matches the regenerated output (the deploy drift gate will re-verify).
- **R8** — N/A: no pinning-policy change.
- **R9** — N/A: no R10 bed change.
- **R10** — The validated tree is the committed branch state.

## Change classification

- **Change class:** config + generated corpus — one refs pin advance + the regenerated skill.
- **Verification gate:** `charly marketplace generate` (the drift gate's command) — regenerated cleanly; the deploy drift gate re-verifies on the PR.
- **Attribution tier:** analysed on a live system — the regeneration ran live with retained output; the full CI gate has not yet passed.

*Assisted-by: pi ollama-cloud/deepseek-v4-flash:0731 (analysed on a live system)*