# 2026.260.1232 — feat: pin the ten serving-plugin command skills and regenerate the corpus

## Summary

The regeneration PR that lands the 2026-09-17 skill-coverage campaign: bumps the
generator to the merged dispatcher/index emitter, pins every serving-plugin repo
whose command skill merged, and regenerates the corpus so the new skills and the
R0 dispatcher table exist.

- **Generator bump:** `tools/marketplace-cli/go.mod` pins
  `plugin-marketplace/candy/plugin-marketplace v0.2026260.930`
  (opencharly/plugin-marketplace#11, tag `v2026.260.0930`).
- **`pod-charly-hooks`** → `v2026.260.1207` (the ten families incl. the
  `candy-cli` rename).
- **Ten serving-plugin pins** (`candy/charly-marketplace/charly.yml`):
  `plugin-cache@v2026.260.0836`, `plugin-pod@v2026.260.0929`,
  `plugin-review@v2026.260.0931`, `plugin-agent@v2026.260.0930`,
  `plugin-candy@v2026.260.1158`, `plugin-cardwire@v2026.260.0911`,
  `plugin-bpf@v2026.260.1008`, `plugin-feature@v2026.260.0712`,
  `plugin-pipeline@v2026.260.1209`, `plugin-authoring@v2026.260.1159`.
  (`plugin-review@v2026.260.0931` is a backfilled tag on its merged HEAD
  `7155d95` — tag-on-merge had skipped it, so no tag existed past its skill
  merge; the tag is add-only on the merged commit.)
- **Regenerated corpus** (the deploy drift gate requires it in the same PR).

## How tested

**Regeneration with the pinned generator (executed, verbatim):**
```
$ cd tools/marketplace-cli && go mod tidy && go build -o /tmp/mp-v4 ./cmd/serve
$ cd .. && /tmp/mp-v4 generate --root . --out .
marketplace generate: wrote 452 artifact(s) across 38 family(ies)
```

**The emitted DISPATCHER.md (82 rows, was 70) — verbatim grep, no abridgement:**
```
$ grep -c '^| ' DISPATCHER.md
82
$ grep -E 'charly-(cache|pod-verbs|bpf|cardwire|pipeline|review|agent|candy-cli|authoring|feature):' DISPATCHER.md
| `charly cache` / git-ref cache / stale @github ref resolution / pinned ref won't advance | `/charly-cache:cache` |
| `charly cp` / copy a file into a container / copy out of a container / podman cp | `/charly-pod-verbs:cp` |
| `charly restart` / restart a deployment / cycle a container | `/charly-pod-verbs:restart` |
| `charly volume` / list a deployment's volumes / reset a volume / wipe sidecar state / podman volume | `/charly-pod-verbs:volume` |
| `charly bpf` / eBPF readiness / BPF-LSM / kernel BPF config | `/charly-bpf:bpf` |
| `charly cardwire` / cardwire GPU manager / GPU block/unblock / cardwired | `/charly-cardwire:cardwire` |
| `charly pipeline` / agent workflow engine / kind:pipeline / pipeline run | `/charly-pipeline:pipeline` |
| `charly review` / review a PR / PR verdict / review plan | `/charly-review:review` |
| `charly tui` / agent TUI / interactively browse agent sessions | `/charly-agent:tui` |
| `charly candy` / candy set / candy add-pac/add-deb/add-rpm / edit a candy manifest | `/charly-candy-cli:candy` |
| `charly box set` / `charly box add-candy` / `charly box rm-candy` / `charly box write` / `charly box cat` / `charly box fetch` / `charly box refresh` | `/charly-authoring:authoring` |
| `charly feature` / feature list / feature pending / feature validate / ADE entity descriptions | `/charly-feature:feature` |
```

**The opencode index (332 skills):**
```
$ python3 -c "import json;print(len(json.load(open('.well-known/skills/index.json'))['skills']))"
332
$ python3 -c "import json;print([s['name'] for s in json.load(open('.well-known/skills/index.json'))['skills'] if s['name'] in ('review','pipeline','authoring','candy','cache','bpf','cardwire','tui','feature','restart','cp','volume')])"
['authoring', 'bpf', 'cache', 'candy', 'cardwire', 'cp', 'feature', 'pipeline', 'restart', 'review', 'tui', 'volume']
```

**No directory collision** (the reason for the `candy-cli` rename in
opencharly/pod-charly-hooks#7):
```
$ ls -d candy/.claude-plugin 2>/dev/null || echo 'no generated output under candy/'
no generated output under candy/
$ find candy-cli -name SKILL.md
candy-cli/skills/candy/SKILL.md
```

## Rulebook compliance

| Rule | How this PR satisfies it |
|---|---|
| R0 — skills first | marketplace, skills, git-workflow, check loaded. |
| R1 — RCA every anomaly | A first regen attempt failed with `skill "volume" declares family "pod-verbs" which the marketplace entity does not declare`; RCA: the refs list pinned pod-charly-hooks pre-rename — fixed by the pin bump. A second review found `plugin-review` tagged only BEFORE its skill merge — RCA: tag-on-merge had skipped the merge (`7155d95` untagged), fixed by the add-only backfill `v2026.260.0931`. Zero errors on the final run. |
| R2 — finish the whole cutover | Pin bumps + regenerated corpus land in ONE PR (drift gate fails closed otherwise). |
| R3 — no duplication | The corpus is generated from the refs-list pins; the generator stays the single emitter. |
| R4 — no workarounds | No hand-edited corpus; the pinned generator emits every artifact. |
| R5 — delete legacy | Regeneration prunes artifacts no source claims; the `candy` family dir is gone. |
| R6 — git safety | Branch-per-change PR; no force-push; the backfilled tag is add-only on the merged HEAD. |
| R7 — prove behaviour | The regeneration ran end-to-end; output pasted verbatim. |
| R8 — preserve emitted artifacts | The emitted rows are asserted (82 rows), the index (332 skills), and the absence of the collision dir. |
| R9 — binary equals source | Generator built from the pinned published module (`v0.2026260.930`). |
| R10 — fresh disposable proof | The regeneration IS the class gate (the deploy drift gate re-runs it on merge). |
| RDD/ADE/SDD | RDD: the generator/pin chain proven by execution. |
| Disposable-only gate | N/A — nothing created/destroyed. |
| Hooks doctrine | N/A — merge gate = org pr-validator + the deploy drift gate. |

## Change classification

- **Class:** generated corpus + refs-list pins + a Go module pin.
- **Verification gate:** the end-to-end regeneration (pasted verbatim) + the
  deploy drift gate on merge.
- **Attribution tier:** `analysed on a live system` — the regeneration ran live
  with retained output.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (analysed on a live system)*