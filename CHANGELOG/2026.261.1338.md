# 2026.261.1338 — docs: sync the per-bucket skill lists with the live corpus

## Summary of changes

Brings the marketplace README — which is hand-authored, so nothing regenerates it — back in sync with the live corpus and catalog. It had drifted in four ways.

**1. The top bucket summary table listed only 26 of 38 plugins.**
- `commands`: 4 → 14 (`charly-agent`, `charly-authoring`, `charly-bpf`, `charly-cache`, `charly-candy-cli`, `charly-cardwire`, `charly-feature`, `charly-pipeline`, `charly-pod-verbs`, `charly-review` added).
- `images`: 16 → 18 (`charly-agentteams`, `charly-crabbox` added).

**2. The per-bucket detail sections omitted 12 plugin rows** (the same 12, plus their one-line purposes): `charly-agent`, `charly-authoring`, `charly-bpf`, `charly-cache`, `charly-candy-cli`, `charly-cardwire`, `charly-feature`, `charly-pipeline`, `charly-pod-verbs`, `charly-review`, `charly-agentteams`, `charly-crabbox`.

**3. Five existing detail rows under-listed their skills:**
- `charly-build`: add `load`; `charly-check`: add `quickshell`, `jetkvm`.
- `charly-automation`: add `crabbox-deploy`, `herdr` (incl. `herdr-box`).
- `charly-internals`: add `go-quality`, `marketplace`.
- `charly-tools`: add `blogwatcher`, `charly`, `crabbox`, `cue`, `docs-site`, `dsh-cli`, `gifgrep`, `goplaces`, `vscode`.

**4. The "hand-authored files" list omitted `scripts/refresh-refs.sh`** (a real hand-authored tool this repo ships), and now notes that `DISPATCHER.md` carries generated-dispatcher markers rather than a `DO-NOT-EDIT` header.

**Closes / relates to:** documentation follow-ups to the skill/plugin additions that landed without a README refresh.

**Breaking change + rollback:** `none` — README prose. Revert the merge to roll back.

## Change class

`documentation-only` (`README.md`).

## How tested

Every claim is re-derived from the committed artifacts, not written from memory.

**All 38 catalog plugins have a bucket-summary entry AND a detail row, category-exact:**

```console
$ python3 - <<'PY'
import re, json
readme = open('README.md').read()
cat = json.load(open('.claude-plugin/marketplace.json'))
names = sorted(p['name'].replace('charly-','') for p in cat['plugins'])
rows = set(re.findall(r'^\| \*\*charly-([a-z0-9-]+)\*\* \|', readme, re.M))
print("detail rows:", len(rows), "catalog:", len(names))
print("catalog missing a row:", sorted(set(names)-rows))
for b in ['commands','kind','development','images']:
    m = re.search(r'\|\s*\*\*'+b+r'\*\*[^|]*\|[^|]*\|([^|]*)\|', readme)
    listed = set(re.findall(r'`charly-([a-z0-9-]+)`', m.group(1)))
    actual = set(p['name'].replace('charly-','') for p in cat['plugins'] if p.get('category')==b)
    print(f"{b}: summary={len(listed)} catalog={len(actual)} match={listed==actual}")
PY
detail rows: 38 catalog: 38
catalog missing a row: []
commands: summary=14 catalog=14 match=True
kind: summary=5 catalog=5 match=True
development: summary=1 catalog=1 match=True
images: summary=18 catalog=18 match=True
```

**Every skill token in the detail rows exists in its family's corpus tree:**

```console
$ for f in build check automation internals tools; do echo "$f: $(ls $f/skills | tr '\n' ' ')"; done
build: build charly-mcp-cmd docs generate inspect list load merge migrate new pull reconcile secrets settings validate
check: adb android appium cdp check check-sway-browser-vnc dbus jetkvm libvirt punktfunk quickshell record spice vnc wl wl-overlay
automation: agent alias crabbox-deploy enc herdr herdr-box openclaw-deploy sidecar tmux udev
internals: agents capabilities cloud-init-renderer cutover-policy disposable egress generate-source git-workflow go go-quality install-plan libvirt-renderer local-infra marketplace ovmf plugin root-cause-analyzer skills strict-policy vm-deploy-target vm-spec
tools: blogwatcher charly crabbox cue docs-site dsh dsh-cli gifgrep gogcli goplaces himalaya mcporter nano-pdf ordercli ripgrep sag sherpa-onnx songsee summarize vscode whisper xurl yay
```

**The 12 new detail rows' purposes are taken from each plugin's own `SKILL.md` description** (e.g. `charly-agent` = the `charly tui` agent-control TUI; `charly-crabbox` = the local Crabbox coordinator on `:8080`). The one token the checker flags, `check-sway-browser-vnc-pod`, is an R10 bed, not a skill — correctly labeled as such in the `charly-check` row.

**§4's claims, from executed commands:**

```console
$ git ls-files scripts/
scripts/refresh-refs.sh
scripts/squash_body.py
$ head -1 scripts/refresh-refs.sh
#!/usr/bin/env bash
$ grep -c "DO-NOT-EDIT\|GENERATED" scripts/refresh-refs.sh
0
$ head -2 DISPATCHER.md
<!-- BEGIN GENERATED SKILL DISPATCHER -->
| Trigger (what the user said or you're about to do) | Skill to load |
```

i.e. the repo ships two hand-authored scripts (both listed by §4), and `DISPATCHER.md` opens with the generated-dispatcher markers rather than a DO-NOT-EDIT line.

The generated corpus is untouched (this is the hand-authored `README.md`, explicitly outside the DO-NOT-EDIT set).

## Change classification

- **Change class:** documentation-only.
- **Verification gate:** the catalog-exactness and corpus-listing checks pasted above.
- **Attribution tier:** `documentation reviewed`.

## Harness rulebook compliance

- **R0 skills:** `/charly-internals:skills`, `/charly-build:docs`, `/charly-internals:git-workflow`.
- **R1 RCA + zero warnings:** root-caused to `README.md` being hand-authored while the corpus/catalog it describes are generated — no gate reads README prose, so it drifted across many additions. Corrected to the live artifacts and proven.
- **R2 no out-of-scope:** the plugin inventory, the five skill lists, and the hand-authored-files list edit are the whole change; no generated file touched.
- **R3 no duplication:** the README now mirrors the catalog/corpus, which remain the sources.
- **R4 no workaround:** every list is corrected to the full real set, not trimmed to whatever was already there.
- **R5 hard cutover + grep:** the pasted catalog/corpus checks prove every name.
- **R6 git safety:** fresh branch off current `origin/main`; no force push, no hooks bypass, no direct push to `main`.
- **R7 runtime gate:** `N/A — README prose`.
- **R8 artifact invariants:** the generated corpus is untouched.
- **R9/R10:** `N/A — no artifact/bed`.
- **CHANGELOG:** written by `tag-on-merge` from this body.

---
*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (documentation reviewed)*