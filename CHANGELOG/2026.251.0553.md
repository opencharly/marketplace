# 2026.251.0553 — chore(corpus): regen after layer-charly-distros v2026.251.0547 — drop omarchy-eval EXTERNAL NON-AUTHORITATIVE disclaimer wording

## Summary

Corpus regen cutover after **layer-charly-distros v2026.251.0547**: the omarchy-eval skill's EXTERNAL NON-AUTHORITATIVE disclaimer wording drop.

- **Upstream**: opencharly/layer-charly-distros PR #7 — "docs(skill): drop the EXTERNAL NON-AUTHORITATIVE disclaimer wording from omarchy-eval procedure" — MERGED via auto-merge (merge commit `8d57130b5116e034a5c021db90f53aa8c75a7c7e`); tag-on-merge produced **v2026.251.0547** — verified as the newest repo tag, on exactly that merge commit.
- **Pin bump** (`candy/charly-marketplace/charly.yml` line 81): `@github.com/opencharly/layer-charly-distros:v2026.250.1759` → `v2026.251.0547`.
- **Corpus projection** (`distros/skills/omarchy-eval/SKILL.md`): the disclaimer passage "rendered from it and carries its EXTERNAL, NON-AUTHORITATIVE (by opencharly.ai) disclaimer verbatim" is replaced by "rendered from it: what was tested and how it went, plainly." — 2 insertions, 3 deletions.
- Only these two files changed — the regen on the rest of the corpus is a byte-identical no-op.

## How tested

Pasted outputs (branch `feat/drop-eval-disclaimer-corpus`, fresh clone `/tmp/mkt3`):

```text
# regen — R10-verified charly binary /tmp/charly-r10/bin/charly (2026.250.1424):
# the PATH charly (2026.248.2159) rejects this repo's config schema (see B14 below).
$ /tmp/charly-r10/bin/charly -C /tmp/mkt3 marketplace generate --root /tmp/mkt3 --out /tmp/mkt3 > /tmp/mkt3-regen.log 2>&1; echo EXIT=$?
EXIT=0

$ tail -3 /tmp/mkt3-regen.log
  versa/skills/versatiles/SKILL.md
  vm/skills/omarchy-vm/SKILL.md
  vm/skills/vms-catalog/SKILL.md

$ git diff --stat
 candy/charly-marketplace/charly.yml  | 2 +-
 distros/skills/omarchy-eval/SKILL.md | 5 ++---
 2 files changed, 3 insertions(+), 4 deletions(-)

$ git status --porcelain            # no untracked, nothing staged; only the two intended paths
 M candy/charly-marketplace/charly.yml
 M distros/skills/omarchy-eval/SKILL.md

# the directive grep proving the drop carried through the regen:
$ grep -rn 'NON-AUTHORITATIVE' distros/skills/omarchy-eval/ ; echo grep-exit=$?
grep-exit=1     (0 matches — wording gone from the regenerated skill)
```

Generator reproducibility note: the repo's config schema is `2026.249.2125`. The PATH `charly` (2026.248.2159) errors before any comparison — `config schema 2026.249.2125 is newer than this charly supports (max 2026.248.1030)` — a binary/schema version gap, not this change; the available `/tmp/charly-r10/bin/charly` (2026.250.1424) loaded the schema and regenerated EXIT=0. The CI-time charly pin in `.github/workflows/deploy.yml` / `refresh.yml` is `v2026.241.1243` — the pre-existing repo-wide drift-gate breakage documented in #290/#291 (CI binary max schema 2026.240.1943 < repo config schema 2026.249.2125), unrelated to this change. The pr-validator is the merge gate for this PR.

## Rulebook compliance

| Rule | How |
|---|---|
| R1 | RCA per anomaly: the PATH charly schema rejection root-caused as a binary/schema version gap (max 2026.248.1030 < repo config 2026.249.2125), resolved with the available R10-verified binary 2026.250.1424 — not a blind retry. Regen-tree audit shows exactly the two expected paths; no other drift. |
| R2 | No conflict — one corpus per skill: only `distros/skills/omarchy-eval/SKILL.md` touched in its owning family. |
| R3 | No duplication — the projection is generated from the layer `skill:` entity (single source); the regen is the only writer of corpus files. |
| R4 | No workarounds — no sleeps/retries/manual corpus edits; a standard `charly marketplace generate` run. |
| R5 | Old content removed completely — the disclaimer wording deleted upstream (layer-charly-distros #7) and fully absent from the regenerated corpus (grep empty, exit 1). |
| R6 | Git safety — feat/ branch, PR-only landing, no force-push, no direct push to main. |
| R7 | Gate proof — regen EXIT=0 on the branch tree with diff audit + directive greps pasted above; drift-gate state documented in How tested; the pr-validator is this PR's merge gate. |
| R8 | N/A — no production data or deployed system state (corpus publication only). |
| R9 | N/A — no vendor/schema code changes; config refs list + generated artifact only. |
| R10 | Fresh disposable proof — regen ran in a fresh clone (/tmp/mkt3) from the branch tree; diff verified from git status, nothing from an edited state. |
| RDD | Doc-driven — corpus content derives entirely from the layer skill entity (single source of truth); the upstream skill edit merged first, this regen is the cutover half. |
| ADE | Acceptance-driven — acceptance criteria (regen exit 0; diff = pin + omarchy-eval projection only; NON-AUTHORITATIVE grep empty) all met and pasted above. |
| SDD | N/A — no schema/spec change; repo config schema untouched (2026.249.2125). |
| B14 | Honest binary availability + evidence — the regen needs a charly able to load config schema 2026.249.2125; PATH charly (2026.248.2159) cannot, /tmp/charly-r10/bin/charly (2026.250.1424) can and is available in this environment — so no B14 block; full command + log + EXIT pasted, no fake regen. |
| B15 | No stray artifacts — 0 untracked files after the regen; the commit contains exactly the two intended paths. |
| B16 | No unpasted claims — every statement carries its pasted output above (EXIT=0, diff --stat, status --porcelain, grep exit). |
| B17 | No sibling-branch reliance — the pin bumps a merged tag (v2026.251.0547 on merge commit 8d57130b, merged 2026-09-08T05:47:19Z), never a live PR branch. |
| B18 | Scope discipline — no other pin advanced, no other family touched; the diff is exactly the pin line + the omarchy-eval projection. |

## Change classification

- Change class: **docs/corpus regeneration + refs pin bump**.
- Verification gate: **analysed on a live system** — full `marketplace generate` EXIT=0 with the available charly binary on the branch tree, diff audit + directive grep pasted above; the repo's deploy drift gate is pre-existing broken at CI (stale pinned charly, documented in #290/#291).
- Attribution tier: analysed on a live system.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*