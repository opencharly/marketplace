# 2026.234.2305 — docs: regenerate skill pages for the sdk de-submodule cutover

## Summary

Regenerate the three skill pages that describe the sdk contract module's wiring, for the charly sdk de-submodule cutover (opencharly/charly: drop the `sdk/` submodule → `github.com/opencharly/sdk` resolves from the module proxy at the shared pinned require version v0.2026234.347):

- `build/skills/migrate/SKILL.md` — a schema cutover "in the superproject bump the sdk submodule" is now "adopt the tagged sdk release as the new shared pinned require (the `task mods:tidy` sweep + canonical-go.mod gate keep every module on the one pin)".
- `internals/skills/go/references/source-map.md` — the repo-root `go.work` carries NO entry for either contract module; pluginsgen guards nothing (the sdk bootstrap guard is deleted).
- `internals/skills/plugin/references/authoring-recipes.md` — pluginsgen emits `use ./charly` + `use ./candy/<name>` only (no `use ./sdk`); the sdk + spec contract modules resolve from the module proxy at the pinned require versions — no submodule, no checkout.

Generated deterministically by `charly marketplace generate` from the candy entities edited in the charly cutover PR (this repo's corpus is regeneration output; the charly PR carries the entity edits).

## How tested

**Regeneration — executed on the cutover tree (charly worktree, final committed state):**

```
$ ./bin/charly marketplace generate --root <charly-worktree>
exit=0
$ git -C plugins status --short
 M build/skills/migrate/SKILL.md
 M internals/agents/pr-validator.md
 M internals/skills/agents/references/agent-roster.md
 M internals/skills/agents/references/parallel-bed-testing.md
 M internals/skills/cutover-policy/SKILL.md
 M internals/skills/git-workflow/SKILL.md
 M internals/skills/git-workflow/references/branch-and-pr-loop.md
 M internals/skills/git-workflow/references/multi-repo-coordination.md
 M internals/skills/git-workflow/references/validator-and-calver.md
 M internals/skills/go/references/source-map.md
 M internals/skills/plugin/references/authoring-recipes.md
```

11 files regenerate; **this PR carries exactly the 3 files owned by the sdk cutover** (the other 8 are pre-registered drift — the auto-merge-flow corpus debt registered to its batch in charly's CHANGELOG/2026.234.0605.md and covered by `scripts/drift-baseline-skills.txt`; they are reverted on this branch).

**R5 sweep of the three regenerated pages — zero surviving sdk-submodule claims (executed):**

```
$ grep -n "sdk submodule\|sdk/ submodule\|use ./sdk\|update --init sdk\|bump the sdk submodule\|delivered sdk" \
    build/skills/migrate/SKILL.md internals/skills/go/references/source-map.md internals/skills/plugin/references/authoring-recipes.md
[build/skills/migrate/SKILL.md]
  (no sdk-submodule claims)
[internals/skills/go/references/source-map.md]
  (no sdk-submodule claims)
[internals/skills/plugin/references/authoring-recipes.md]
  (no sdk-submodule claims)
```

**Drift accounting (charly's `task skills:drift`, baselined):** the drift gate compares regenerated artifacts against the committed corpus minus `scripts/drift-baseline-skills.txt` (21 inherited entries, incl. `plugins/internals/skills/go/references/source-map.md`). NEW drift introduced by this cutover = `build/skills/migrate/SKILL.md` + `internals/skills/plugin/references/authoring-recipes.md` (2 files); source-map.md is a baseline-covered file whose content this cut legitimately replaces. The gate closes when this leg merges and charly bumps the gitlink (the landing sequence deletes the now-paid baseline line — self-liquidating per the baseline's own contract).

## Rulebook compliance

| Rule | How / N/A |
|---|---|
| R0 skills first | git-workflow, cutover-policy, go, plugin, strict-policy loaded before any tool call |
| R1 RCA | the 8 unrelated regenerated files are pre-registered corpus drift (auto-merge-flow pages, registered in charly CHANGELOG/2026.234.0605.md) — NOT folded into this PR; reverted |
| R2 whole cutover | the corpus leg regenerates all 3 cutover pages in one PR; the charly PR carries the entities |
| R3 no duplication | regeneration only — no hand-edits to generated pages; entities in the charly PR are the single source |
| R4 no workarounds | N/A — no runtime/code surface; no replacements or shims introduced |
| R4a fix product first | N/A — no documented-behavior/code divergence; the corpus mirrors the new (already merged-by-design) proxy reality |
| R5 delete legacy | every sdk-submodule claim in the three pages replaced; executed grep above proves zero survivors |
| R6 git safety | clean feat/ branch off origin/main; no force-push; status verified |
| R7 prove the gate | regeneration + sweep outputs pasted above; drift accounting documented |
| R8 emitted artifacts | the regenerated pages ARE the artifact; byte-for-byte regeneration is deterministic (marketplace generate exit=0) |
| R9 binary equals source | N/A — no binary touched |
| R10 fresh proof | docs-only change class — non-runtime standards only (no invented bed) |

## Change classification

- **Change class**: documentation-only (3 generated `.md` skill pages, 8+/8−, single commit)
- **Verification gate**: non-runtime standards; regeneration no-op verified with pasted output
- **Attribution tier**: documentation reviewed
- **Landing**: fresh `charly/pr-validator`; tag-on-merge

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (documentation reviewed)*