# 2026.216.0643 — six lines documented `--no-autodetect`, which has never worked


`charly shell --no-autodetect nonexistent-box` prints `Usage: shell <box>
[flags]` — Kong rejecting an unknown flag. The shipped spelling is
`--no-auto-detect`, and it parses: the same command with a hyphen between "auto"
and "detect" gets past parsing and fails on image resolution instead.

Six lines across three skills advertised the broken form:

  core/skills/charly-config/SKILL.md:71
  core/skills/charly-doctor/SKILL.md:109,132
  core/skills/shell/SKILL.md:23,107,202

Unlike the sibling cases in this cleanup, the CODE is right here and the docs
were wrong. `NoAutoDetect` yields `--no-auto-detect` under Kong's own
derivation, `--help` has always printed it, and
`plugins/vm/skills/arch-cloud-vm/SKILL.md:308` documents a runnable
`charly vm create arch --no-auto-detect`. So the flag stays and the prose moves
— the opposite call from `--candy`, where the code's own tag recorded an intent
it never delivered.

That asymmetry is the point worth keeping: "documentation disagrees with the
binary" does not by itself say which one is wrong. Here the evidence split
within this same repo — six lines saying one thing, one runnable line saying
another — and the tiebreaker was running both.

Whole-corpus sweep after the change: 539 files enumerated, 539 read, 0 read
failures, zero surviving `--no-autodetect`. (538 is the merge base; this
branch's own first commit adds the entry you are reading, which is why a figure
measured before it and reported after it disagrees by one.)

## Two more families in this cutover

**`build/skills/new/SKILL.md` — `--candies` became `--candy`.** `opencharly/charly`
#206 converted 328 go-flags `long:` tags to Kong's `name:`, renaming five flags
whose shipped spellings were Go field-name artifacts. This skill documented the
old `--candies` at four sites. `/charly-image:image` had documented `--candy`
all along — the author's intent, which the binary did not deliver until #206 —
so the two skills contradicted each other, each internally consistent. Sequenced
after #206 deliberately: naming `--candy` before the binary accepted it would
have published a command that exits 80.

**The same file documented a `name:` field that has never existed.** Both the
`box new box` output sample and the candy-scaffold description showed
`candy: { name: … }`. The scaffolder writes the NAME as the node key —
`hello: {candy: {base:, candy:}}` — and `sdk/kit/scaffold.go`'s own comment says
so. Verified by running the scaffolder and reading the file it produced. The
same skill got it right elsewhere, so the published page contradicted itself.

**`build/skills/secrets/SKILL.md` — the troubleshooting table quoted an error
the code stopped emitting.** #206 cleared two pre-existing ST1005 findings by
extracting a duplicated literal to one const and rephrasing it to lead
lowercase; the hard error is now `no Secret Service on D-Bus`. The old wording
survives only as a setup-time WARNING, so the table's row pointed at a milder
condition than the one a user hits. Both are now listed, separately, each
verified present in the producer.

