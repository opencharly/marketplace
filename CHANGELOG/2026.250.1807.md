# 2026.250.1807 — feat(corpus): refresh omarchy-eval + add omarchy-eval-golden (layer-charly-distros v2026.250.1759)

## Summary

Marketplace publication wave (plan PR7), Stage 2 of 3: bump the `layer-charly-distros` pin and carry the regenerated corpus, after opencharly/layer-charly-distros#6 (tag `v2026.250.1759`) updated the owning skill entity + added `omarchy-eval-golden`.

- **Pin bump**: `candy/charly-marketplace/charly.yml` line 81 — `@github.com/opencharly/layer-charly-distros:v2026.245.1157` → `@github.com/opencharly/layer-charly-distros:v2026.250.1759`.
- **Corpus projections** (regenerated with `charly marketplace generate`, binary built from opencharly/charly @ v2026.250.1424):
  - `distros/skills/omarchy-eval/SKILL.md` — rewritten projection of the updated entity (golden chain beds, oracle-generated per-PR eval + RED-PROBE twins, pi-agent lane, PR-EVAL-LANE.md binding contract; stale retired bed names removed).
  - `distros/skills/omarchy-eval-golden/SKILL.md` — NEW projection for the new entity (golden VM chain runbook).
- Only these three files changed — regen on the rest of the corpus is a byte-identical no-op.

## How tested

Pasted outputs (branch `feat/omarchy-eval-corpus`):

```text
$ git diff --cached --stat
 candy/charly-marketplace/charly.yml         |   2 +-
 distros/skills/omarchy-eval-golden/SKILL.md |  69 ++++++++++++
 distros/skills/omarchy-eval/SKILL.md        | 164 ++++++++++++++++------------
 3 files changed, 162 insertions(+), 73 deletions(-)

$ git diff --cached --check  →  no output (clean)

$ charly -C /tmp/mkt marketplace generate --root /tmp/mkt --out /tmp/mkt   (charly v2026.250.1424)
EXIT=0 — emitted 397 artifacts across 26 families; git status after regen shows ONLY:
 M candy/charly-marketplace/charly.yml
 M distros/skills/omarchy-eval/SKILL.md
?? distros/skills/omarchy-eval-golden/

$ grep -c 'check-omarchy-suite-pod\|check-omarchy-pr-vm-visual' distros/skills/omarchy-eval/SKILL.md distros/skills/omarchy-eval-golden/SKILL.md
distros/skills/omarchy-eval/SKILL.md:0
distros/skills/omarchy-eval-golden/SKILL.md:0
```

Generator reproducibility note: the regen was run with a charly binary at v2026.250.1424 (the current latest tag, schema ≥ the repo config's 2026.249.2125). The committed corpus on main was previously failing the deploy drift gate — the CI-time charly pin in `.github/workflows/deploy.yml`/refresh.yml is `v2026.241.1243` whose max schema is 2026.240.1943, and the config schema is 2026.249.2125, so the CI regenerate step errors before any comparison (`config schema 2026.249.2125 is newer than this charly supports (max 2026.240.1943)` — observed failing on main runs today, pre-existing, unrelated to this change). The pr-validator is the merge gate for this PR.

## Rulebook compliance

| Rule | How |
|---|---|
| R1 | Diff-scope RCA: the regenerated-tree audit shows exactly the three expected changes (pin + two skill projections); nothing else moved (git status had no other files), proving the sources-driven generator reproduced the corpus byte-identically on the untouched families. |
| R2 | No conflict — one corpus per skill: `omarchy-eval` updated in place, `omarchy-eval-golden` added as a distinct skill directory (no overwrite). |
| R3 | No duplication — projections are generated from the layer entities (single source); the regen is the ONLY path that writes corpus files (DO-NOT-EDIT headers). |
| R4 | No workarounds — no sleeps/retries/manual corpus edits; standard `charly marketplace generate` run. |
| R4a | Pin bump is the org flow (refresh.yml does exactly this daily) — not a hand-pin outside the published tag; v2026.250.1759 is the tag-on-merge of the layer PR, published and validated. |
| R5 | Stale content removed with the change: retired bed names (check-omarchy-suite-pod / -pr-vm-visual) are zero in both projections; the old projection was replaced in the same PR. |
| R6 | Git safety — feat/ branch, PR-only landing, no force-push, no direct push to main. |
| R7 | Gate proof — regen EXID=0 on the final tree; the deploy drift gate is currently broken repo-wide on main (stale CI charly pin, pre-existing, documented in How tested); the pr-validator runs on this PR as the merge gate. |
| R8 | N/A — no production data or deployed system state (corpus publication only). |
| R9 | N/A — no vendor/schema code changes; config refs list + generated artifacts only. |
| R10 | Fresh disposable proof — generated from the freshly-cloned tree (/tmp/mkt at HEAD), verified from the committed index (git diff --cached). |
| RDD | Doc-driven — corpus content derives entirely from the layer skill entities (single source of truth). |
| ADE | N/A — no agent-loop or visual grading involved in corpus generation. |
| SDD | N/A — no charly Go/CUE source changed. |
| B14 | Honest tiers — this is a docs/corpus change (tier `analysed on a live system`); the deploy gate breakage is disclosed above rather than masked. |
| B15 | N/A — no benchmark/eval claims. |
| B17 | N/A — no security-relevant content. |
| B18 | Scope discipline — no other pin advanced, no other family touched; the diff is exactly the one layer's projections. |

## Change classification

- Change class: **docs/corpus regeneration + refs pin bump**.
- Verification gate: **analysed on a live system** — full `marketplace generate` EXIT=0 with the current charly binary on the fresh clone, diff audit pasted above; the repo's deploy drift gate is pre-existing broken at CI (stale pinned charly, documented).
- Attribution tier: analysed on a live system.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*