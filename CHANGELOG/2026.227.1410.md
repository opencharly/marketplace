# 2026.227.1410 — R4a sweep: regenerated skills for the task-target residue cutover

Regenerated from the superproject's candy `skill:` entities by `charly marketplace generate`. The authoring change lives in `opencharly/charly`; this repo carries the generated surface.

Eight skill pages change:

- **`build/skills/build/SKILL.md`** — "Host Bootstrap (First Time)" becomes "Host Prerequisites (First Time)": the Install-page duplicate (a `task build:binary` / `task install-portable` / `task setup:builder` / `./bin/charly box build` block) is replaced by the native-package path, a pointer to the Install page for building from a source tree, and `charly box build` alone. Three troubleshooting entries — "charly not found", "YAML Unmarshal Error", "Stale `charly` binary produces stale Containerfiles" — drop their `task` rebuild recipes and checkout-only `git log` diagnostic in favour of `charly version` / `which charly`, which a reader can actually run.
- **`build/skills/migrate/SKILL.md`** — "Adding a future cutover" and "Standing rule" gain an audience declaration: they are maintenance this project performs on its own schema, from checkouts of the spec repo and the superproject. The user-facing surface stays the single unchanged command `charly migrate`.
- **`tools/skills/charly/SKILL.md`** — "Updating the Binary — dual-path gotcha" gains the same declaration. It is an images-bucket page that was telling readers to `cd charly && go build`; the section is this candy's own build contract, named as maintenance rather than as instructions.
- **`tools/skills/docs-site/SKILL.md`** — opens with a page-level audience declaration. The page describes the candy that builds this documentation site, so its `task docs:pin` / `docs:drift` / `docs:sync` mentions are gates this project runs over its own sources; unlike the `charly-internals` bucket the page carried no contributor framing of its own.
- **`tools/skills/cue/SKILL.md`, `internals/skills/egress/SKILL.md`** — `task cue:vendor` is removed. The target does not exist and never did; the real pipeline is the per-file `cue import` sequence the egress skill already lists. The section heading is now "The offline vendoring pipeline", with no target name.
- **`internals/skills/go/SKILL.md`, `internals/skills/agents/references/parallel-bed-testing.md`** — `task install-portable` corrected to its real name `build:install-portable`.

Residue on the non-development published pages drops from 44 occurrences across 10 files to 31, measured with a pattern keyed to the real Taskfile target namespace rather than a prose-matching shape (a shape-based pattern misses the colon-less `task install-portable`). The `charly-internals` bucket is out of R4a scope by published audience declaration — `recipes/index.md` in the docs mirror, "Development — contributor internals" — but the fictitious-target corrections cross that line, because the claim being swept there is "this command exists", not "this page is reader-facing". Bucket bounds the audience sweep; it does not bound a factual-accuracy sweep.

**Standing correction for future sweeps:** the docs-generation occurrences on these pages are reframed, never re-spelled as `charly docs generate`. That verb is unavailable to a binary-only reader (`plugin-docs` is in neither `compiled_plugins:` nor `pkg/host-command-plugins.txt`), so substituting it would replace a true-but-misaudienced command with a false one. Doc generation is repository maintenance, which R4a permits naming as such. Full reasoning in the superproject's entry for this CalVer.

Documentation-only change class at `documentation reviewed`; regeneration is idempotent. CalVer finalized at merge by the fresh `pr-validator`, which independently re-derived the 44 → 31 figure, confirmed `charly marketplace generate` reproduces all nine files byte-identically, confirmed zero whole-file deletions against this repo's `origin/main`, and verified by execution that `task cue:vendor`, `task install-portable` and `task setup:builder` do not exist.
