# 2026.218.2041 — regenerate the skill corpus from candy config

The plugins leg of the plugins->candies migration: the plugins/ skill corpus + agents +
plugin.json (Claude+Codex) + .mcp.json + marketplace.json + profiles.json + the setup
launcher are now GENERATED from the candies' skill:/hook:/marketplace: kind entities by
candy/plugin-marketplace (`charly marketplace generate`). Supersedes the hand-authored
content; README, CHANGELOG, CLAUDE.md, LICENSE and scripts/squash_body.py stay
hand-authored. scripts/validate_skills.py + tests/setup_test.sh are deleted (their roles
fold into the generator's drift gate + tests).
