# 2026.230.0833 — the carrier recipe stops naming a fixed /tmp path

Projection of `opencharly/charly#289`. One file — `internals/agents/pr-validator.md`.

The carrier-detection recipe published `git worktree add /tmp/carrier-<n>` and tore it down with
`worktree remove --force`. **The same source file mandates `mktemp`, not a fixed path, and pastes
the transcript of a fixed `/tmp` path destroying a peer's live worktree.** The recipe contradicted
the rule stated 5,000 lines above it, in the direction the rule exists to prevent.

It now uses `mktemp -d`, names its four inputs as shell variables, and carries no angle-bracket
placeholders — those parse as **shell redirections**, which is the defect this cutover's other leg
removed from two published guards.

**And two census figures are re-anchored.** They were keyed to `plugins@32f48d2`, contained in
**zero** remote branches and zero tags, so every figure was true and none was checkable from a fresh
clone. Re-derived at `plugins@01edd45e` and `docs@eb92dc24`, both on their submodules' main.

## The sweep re-anchored the labels and left the numbers

The ref citations moved to reachable trees; **every figure keyed to the retired ref stayed put**,
because a sweep on the ref token cannot reach a bare integer. Corrected against `plugins@01edd45e`
and `docs@eb92dc24`: `344 of 350` at line ≤ 15 is **98.3%**, not 96.6%; **two** hits above line 100,
not three; `6 of 340` / `7 of 899` become `2 of 348` / `902 of 905`; and `339 + 4 - 3 = 340` becomes
`346 + 6 - 4 = 348`.

**The claimed deep carrier does not exist.** *"Exactly ONE genuinely deep carrier, at line 1214"* —
at this ref the deepest banner is line **610**, and it is this same page quoting the banner in prose.
The rule now stands on a tail being **possible**, not on this tree having one.

## Two defects in the recipe itself

**`$prhead` was used twice and assigned never**, under a comment saying *"set these four first"* when
it needs five. That is a **regression in failure mode**: `<pr-head-sha>` visibly demanded
substitution, while an unset variable reads as handled — `git fetch origin ''` fails, `&&` skips the
checkout, the submodule stays on the pinned gitlink, and the block prints a confident verdict about
the wrong commit.

**The `cd "$probe"` was unguarded** while step 1 deletes a **relative** path. `mktemp` protected the
worktree path; nothing protected the working directory.

*Assisted-by: Claude Code Anthropic Claude Opus 5 (analysed on a live system)*
