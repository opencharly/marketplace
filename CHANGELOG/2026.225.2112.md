# 2026.225.2112 — codify the sandboxed-/tmp Go + golangci-lint environment constraints

Regenerated from the superproject's `candy/charly-internals/charly.yml` manifest edits via `charly marketplace generate`. Two skill files changed:

- `internals/skills/go/SKILL.md` — new "Environment constraints — sandboxed /tmp + Go temp dirs" section: some dev hosts (a sandboxed runner, a small tmpfs `/tmp`) cap writes to the system temp dir at a few tens of MB per command regardless of the free space `df` reports; Go builds and golangci-lint routinely exceed that, and the failures look like tool bugs ("disk quota exceeded", "parallel golangci-lint is running") when they are environmental. Run Go builds/tests with `GOTMPDIR=~/.cache/go-tmp`; golangci-lint v2 locks at `$TMPDIR/golangci-lint.lock` (a missing TMPDIR dir → ENOENT → the false "parallel golangci-lint is running" report); the pre-commit-gate hook already redirects its lint temp dirs to `~/.cache/charly-gate-lint/`. A follow-up adds the **cumulative-cap** finding: beyond the per-command cap, the sandbox also caps TOTAL /tmp usage (observed at ~80% of the tmpfs), at which point the Bash tool's output capture dies — every command writing to stdout/stderr exits 1 with no output (`python3` exits 120, the flush-failure code), while no-output commands still succeed, and the underlying error is `write error: Disk quota exceeded`. Fix: clear the accumulated `tmp.*` Go temp dirs in /tmp (17G had accumulated from lint runs) to drop usage below the cap; a session restart also resets it.
- `internals/skills/go-quality/SKILL.md` — the pre-commit-gate hook paragraph gains the root-fs lint temp-dir detail: the hook redirects its golangci-lint run to `~/.cache/charly-gate-lint/`, sets `GOTMPDIR`/`TMPDIR` to subdirs it creates there, and creates the subdirs before running golangci-lint because a missing TMPDIR makes the lock open fail with ENOENT — reported as "parallel golangci-lint is running", a false positive that blocks every commit on hosts where the dir does not pre-exist.

This is the `plugins` leg of a cross-repo docs cutover (plugins → superproject). Regeneration is a no-op on a clean tree. Placeholder CalVer — the pr-validator finalizes it at merge.
