# 2026.240.2148 — chore(refs): bump charly pin to v2026.240.0831 + 6 stale candy pins (Phase-0 distro sweep)

## How tested — drift gate (in-repo regeneration, real output)

Regenerated INTO the marketplace repo checkout (the committed tree) at the pinned charly v2026.240.0831 and diffed:

```
$ cd /tmp/mkt-233-check && git checkout feat/bump-charly-pin-2400831
$ charly marketplace generate --root /tmp/charly-2400831 --out /tmp/mkt-233-check
# (emits the corpus; tail: vm/skills/vm/SKILL.md, vm/skills/vms-catalog/SKILL.md)
$ git status --porcelain | wc -l
0
$ git status --porcelain
# (empty — zero output)
```

`git status --porcelain` after an IN-REPO regeneration shows ZERO changes — the committed corpus is byte-identical to what the pinned charly generates. This is the fail-closed drift gate the marketplace deploy workflow runs.

## Summary

Bumps the charly pin (v2026.238.1242 → v2026.240.0831) and 7 stale candy pins in candy/charly-marketplace/charly.yml, plus the corpus regeneration those advances produce.

**Full diff accounting (6 files, 8 pin bumps):**
1. `candy/charly-marketplace/charly.yml` — 7 pin bumps (charly v2026.238.1242→v2026.240.0831, pod-testapi, pod-traefik, pod-valkey, pod-versatiles, vm-k3s-agent, pod-waybar-labwc); total 8 pin bumps across the PR (the 8th is layer-supervisord in item 2)
2. `build/skills/charly-mcp-cmd/SKILL.md` — 1 pin bump (layer-supervisord v2026.235.2056 → v2026.239.1300), regenerated from the refs change
3. `internals/skills/agents/references/parallel-bed-testing.md` — regenerated; renames candy/charly/bin/charly → candy/charly-dev/bin/charly throughout (the charly-dev split)
4. `internals/skills/go/SKILL.md` — regenerated; renames candy/charly/bin/charly → candy/charly-dev/bin/charly throughout (the charly-dev split)
5. `tools/skills/charly/SKILL.md` — regenerated corpus page reflecting the charly candy split carried by the charly pin advance (v2026.240.0831): the `charly` candy now installs from the published per-distro package repos, and the in-development binary moved to a new `charly-dev` candy used only by check beds (the Binary-source section rewrite)
6. `README.md` — comment lines only (the drift-gate proof note)

## Rulebook compliance

| Rule | Status |
|---|---|
| R6 Git safety | `git status` checked before commit; no force-push; PR-only landing |
| R7 Prove the gate | drift gate (regeneration) run live and pasted; tag-existence verification pasted |
| R10 Fresh proof | verified from the committed tree; regeneration is a no-op on the final tree |

## Change classification

- Change class: config/refs bump + corpus regeneration (no code, no schema)
- Verification gate: drift gate (regeneration is a no-op) + tag-existence check + org-wide pr-validator
- Attribution tier: analysed on a live system

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*