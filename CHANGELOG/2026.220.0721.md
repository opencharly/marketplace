# 2026.220.0721 — Rename the Liberation sidebar claim

Regenerate the docs skill so its published-site table names the manifesto's sidebar entry “Liberation” instead of “Agents only” and its Usage section stays on the installed `charly docs` interface. Code-format the `name:type[:path]` grammar token in the generated config skill so Starlight does not misparse it as nested directives. Regenerate the config/deploy/remove skills to the corrected teardown semantics: `charly remove <image> -i <instance>` is the complete teardown (stops the container, deletes the quadlet file, reloads systemd), `charly config remove` only disables the quadlet service (the quadlet file remains), and `charly fleet reset` only removes the charly.yml entry (placeholder — the pr-validator finalizes the CalVer at merge).
