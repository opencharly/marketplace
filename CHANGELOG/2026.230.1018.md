# 2026.230.1018 — the ollama skill corpus, projected from its landing source

Regenerated by `charly marketplace generate` from the `skill:` entities in
opencharly/charly's `candy/ollama` and `candy/charly-ollama`, which land as
opencharly/charly#309. This is the B6a step-2 projection leg: for a skill edit the
projection lands FIRST, and the source follows in the same cutover.

## What arrives

- **New:** `ollama/skills/ollama-cli/SKILL.md` — the `charly ollama` management CLI
  provided by the new `command:ollama` plugin. It existed once before, added by #191 and
  then pruned by a later regeneration as an orphan, precisely because its source candy had
  never landed. #309 lands that source, so the projection is no longer orphaned.
- **Updated:** `ollama/skills/ollama/SKILL.md` and `ollama/skills/ollama-layer/SKILL.md` —
  the dual install path (packaged on Arch, upstream tarball elsewhere), the opt-in
  `ollama-cuda` / `ollama-rocm` backends, the corrected size axis (installed vs download),
  and the three host-side `charly ollama` checks now authored on the candy.

## Scope

Deliberately `ollama/` ONLY. A wholesale `charly marketplace generate` on this tree
rewrites 33 further artifacts and removes ~1188 lines; an RCA attributes 892 of those to
the plugins-side legs of opencharly/charly#289 and #297, whose source legs have not landed,
and ~59 to legitimately stale projection. Committing that wholesale output would delete
live, in-review documentation, so this commit carries only the family this cutover owns.

The systemic cause is that `task skills:drift` and `task docs:drift` both exist and are
wired to nothing — no CI job, no hook. Closing that gap is the next cutover's work, batched
with the CachyOS build-hygiene items.
