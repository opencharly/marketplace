# 2026.267.0550 — feat(marketplace): the nerdctl skills + the refs-list bump (Phase 6)

## Summary

Phase 6 of `plan/nerdctl-integration.md`: the two nerdctl-integration candies (`layer-nerdctl`, `plugin-nerdctl`) now own harness skills, and the marketplace refs list pins them so the generated corpus carries them. This is a **generated-corpus + refs-list** change; it does NOT touch the validator's own spec or the guardrail/ruleset surface (see the T4 note).

**The 7-file diff, every file accounted for:**

1. **`candy/charly-marketplace/charly.yml`** (`+5`) — the refs list gains `@github.com/opencharly/layer-nerdctl/candy/layer-nerdctl:v2026.266.1833` + `@github.com/opencharly/plugin-nerdctl/candy/plugin-nerdctl:v2026.266.1833`. This is the ONE generation input.
2. **`distros/skills/layer-nerdctl/SKILL.md`** (`+41`, new) — the generated skill projected from the layer-nerdctl candy's `skill:` entity.
3. **`internals/skills/plugin-nerdctl/SKILL.md`** (`+33`, new) — the generated skill projected from the plugin-nerdctl candy's `skill:` entity.
4. **`.well-known/skills/index.json`** (`+14`) — the generated skill index gains the two entries.
5. **`charly.yml`** (`+1/-1`) — the schema stamp migrated `2026.249.2125 → 2026.261.1747` (the pin-forced wave).
6. **`.github/workflows/deploy.yml`** + **`.github/workflows/refresh.yml`** (`+1/-1` each) — the charly clone pin `v2026.256.1316 → v2026.266.1654`. The prior **256-era** pin cannot load the migrated 261 stamp (`config schema 2026.261.1747 is newer than this charly supports`); `v2026.266.1654` is the tag carrying the schema-261 loader (the same bump `layer-charly` needed, merged as `layer-charly#7`).

**Commits:** one content commit (`b39e431`) that carries the whole 7-file diff, plus no-op body-refresh commits (empty, no content) that re-freeze the body against the moving head and re-trigger the validator after a cancelled/failed check. The current head is `2a9fe11cac3b9133cd411fddfff7ecb9a2c201a2`.

**T4 note (no self-modifying guardrail change):** an earlier revision of this work (the closed `#328`) carried a re-projected `internals/agents/pr-validator.md` + `internals/skills/git-workflow/*`. Those guardrail files are now **already merged on `origin/main`** (via the org-ruleset cutover), so they are NOT in this diff — this PR only adds the two nerdctl skills, their index entries, the two refs-list pins, the schema stamp, and the CI pins. `git diff --quiet origin/main -- internals/agents/pr-validator.md internals/skills/git-workflow/SKILL.md ...` confirms SAME for each (pasted below).

## How tested

**Gate A — the corpus regeneration produces these files and is idempotent** (the drift gate `deploy.yml` runs):

```
$ charly marketplace generate --root . --out .
marketplace generate: wrote 456 artifact(s) across 38 family(ies)
  .well-known/skills/index.json
  agent/skills/tui/SKILL.md
  ...
  distros/skills/layer-nerdctl/SKILL.md
  ...
  internals/skills/plugin-nerdctl/SKILL.md
  ...
$ echo $?
0
$ git status --short          # after regen on the committed tree
(empty — the committed corpus IS the generator's output)
```

The two new skills land + the index gains two `nerdctl` entries:

```
$ ls distros/skills/layer-nerdctl/SKILL.md internals/skills/plugin-nerdctl/SKILL.md
distros/skills/layer-nerdctl/SKILL.md
internals/skills/plugin-nerdctl/SKILL.md
$ grep -c nerdctl .well-known/skills/index.json
2
```

**Gate B — every changed pin exists at its pinned tag:**

```
$ git ls-remote --tags https://github.com/opencharly/layer-nerdctl v2026.266.1833
3700a15dc2ed3b8f29b5a2eacc685f90649ef100	refs/tags/v2026.266.1833
$ git ls-remote --tags https://github.com/opencharly/plugin-nerdctl v2026.266.1833
0bfb146f3ae914608026d1e9bfdc5de911d2d400	refs/tags/v2026.266.1833
$ git ls-remote --tags https://github.com/opencharly/charly v2026.266.1654
d7dc2b9955e4cbfd5feeeb6fd4242480cdab4894	refs/tags/v2026.266.1654
```

**Gate C — the guardrail files are untouched (T4):**

```
$ for f in internals/agents/pr-validator.md internals/skills/git-workflow/SKILL.md \
           internals/skills/git-workflow/references/branch-and-pr-loop.md \
           internals/skills/git-workflow/references/validator-and-calver.md; do
    git diff --quiet origin/main -- "$f" && echo "SAME: $f"; done
SAME: internals/agents/pr-validator.md
SAME: internals/skills/git-workflow/SKILL.md
SAME: internals/skills/git-workflow/references/branch-and-pr-loop.md
SAME: internals/skills/git-workflow/references/validator-and-calver.md
```

**Gate D — no direct hand-edit of a generated file.** The two `SKILL.md` files are `charly marketplace generate` output (they carry the `GENERATED … DO NOT EDIT` banner), projected from the two candies' `skill:` entities at their pinned tags.

## Project-rulebook rule-compliance

- **R0** — `/charly-internals:marketplace`, `/charly-internals:git-workflow` loaded first.
- **R1** — the closed `#328`'s findings were root-caused: the `gen=0` opacity (fixed by pasting the real 456-artifact output), the BEHIND/CLOSED state (re-opened from a fresh branch), the T4 self-modifying-guardrail concern (resolved — the guardrail files are already on `main` and absent from this diff), and the INCONCLUSIVE run (root-caused to a provider stream error `INTERNAL_ERROR; received from peer`, remediated by a new commit that cleared the red required check).
- **R2** — the change is complete; the two skills are generated, not deferred.
- **R3** — the refs list is the ONE generation input; the corpus is its projection; no hand-edit.
- **R4** — no workaround; the sanctioned `charly marketplace generate` path.
- **R5** — no identifier is removed; this PR only adds skills + pins. (`charly/pr-validator` and `validate / validate` are the workflow name vs the required status-check context, both live in the merged corpus — not renamed here.)
- **R6** — no force-push; a `git checkout HEAD -- .` that briefly reverted staged files was recovered by re-applying from the source branch (no loss).
- **R7** — `charly marketplace generate` reproduced the committed corpus (Gate A) and is idempotent (clean `git status`).
- **R8** — the emitted `index.json` is the generator's output, validated by the drift gate.
- **R9** — the generated corpus is the artifact; it was regenerated live.
- **R10** — a fresh `charly marketplace generate` on the final tree, exit 0, idempotent.
- **Disposable-only autonomy** — no destroy performed.

## Change classification

Generated marketplace corpus (2 new skills + index) + config (refs-list pins + schema stamp) + 2 CI workflow pins. Verification gate: `charly marketplace generate` (exit 0, reproduces the committed corpus, idempotent) + the pinned-tag existence. Attribution tier: `fully tested and validated`.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (fully tested and validated)*